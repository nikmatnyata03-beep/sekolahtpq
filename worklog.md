# Worklog — SIMADJI: Sistem Informasi Manajemen TPQ Darul Jinan

Project: Fullstack web adaptation of the uploaded blueprint PDF ("Blueprint Sistem Digital Terpadu untuk TPQ").
Original blueprint targeted Expo+Firebase mobile; we adapt to Next.js 16 + Prisma/SQLite + shadcn/ui.
Constraint: user-visible app lives at a single route `/` (SPA with view switching). APIs live under `/api/*`.

---
Task ID: 1
Agent: main (Z.ai Code)
Task: Project bootstrap — schema, seed, APIs, UI integration, QA.

Work Log:
- Read uploaded blueprint PDF; extracted feature set (see below).
- Created worklog.md.

Stage Summary:
- Feature set from blueprint to implement:
  1. PUBLIC PORTAL: Tentang Kami (visi/misi/sejarah), Struktur Organisasi, Kurikulum (Kemenag aligned), Kegiatan/Berita (blog mini), Biodata Guru (professional portfolio, Kemenag-style data), PPDB online registration with document info + status tracking.
  2. ADMIN DASHBOARD: user management (ADMIN/GURU/ORANG_TUA), santri management, class & session management, attendance (QR-code concept simulated via session code check-in), hafalan progress recording, teaching materials module, billing & payments, announcements, WhatsApp notification log.
  3. PARENT (WALI) PORTAL: child attendance history, hafalan progress, bills + simulated payment gateway (Midtrans-style: create pending tx -> pay -> webhook -> success + WhatsApp notification), announcements.
- Tech mapping: Expo→Next.js 16 SPA, Firebase Auth→demo email/password auth API, Firestore→Prisma/SQLite, Firebase Storage→mock asset URLs, WhatsApp BSP→Notification log simulation, Midtrans webhook→simulated pay endpoint.
- Design: emerald green (Islamic) + amber gold accents, Indonesian language UI, sticky footer, responsive, shadcn/ui components.
- Single route `/`: role switcher (Portal Publik | Wali Santri | Guru | Admin) with demo login.

## API CONTRACT (implemented & verified via curl)
Shared types: `src/lib/types.ts` — fetch helpers: `src/lib/api-client.ts` (apiGet/apiSend/formatRupiah/formatDate/formatShortDate).
- POST /api/auth/login {email,password} -> {user: AuthUser}
- GET /api/stats -> DashboardStats (students, teachers, classes, registrationsPending, hafalanCount, materialCount, revenue, outstanding, pendingCount, successCount, attendanceToday{HADIR,IZIN,SAKIT,ALPA}, attendanceRate, activeSessions[{code,className,topic,date}], attendanceTrend[{date,hadir,total,rate}], recentRegistrations, recentNotifications, recentPayments, recentHafalan)
- GET/POST/PUT/DELETE /api/users (DELETE ?id=)
- GET/POST/PUT/DELETE /api/teachers (JSON-string fields: formalEducation, nonFormalEducation, certifications; optional account via {email,password})
- GET/POST/PUT/DELETE /api/classes (GET returns studentCount)
- GET/POST/PUT/DELETE /api/students (?parentId= ?classId=; auto-NIS if missing)
- GET/POST/PUT /api/registrations (POST public PPDB form -> regNumber + WA notif; PUT {id,status,reviewNote,classId?} -> DITERIMA auto-creates santri+parent account)
- GET/POST/PUT /api/sessions (POST {classId,topic} generates code + closes old active; ?active=1)
- GET/POST /api/attendance (POST {sessionId, records:[{studentId,status,note}]} bulk upsert + WA notif to parents; GET ?sessionId= ?classId= ?studentId=)
- POST /api/attendance/checkin {code, studentId} -> QR-simulated check-in + WA notif
- GET/POST/DELETE /api/hafalan (POST triggers WA notif with grade)
- GET/POST/DELETE /api/materials
- GET/POST/PUT /api/payments (POST creates invoice + WA notif; PUT {id,status,method} = simulated gateway webhook -> WA confirmation)
- GET/POST/PUT/DELETE /api/posts (?published=1)
- GET/POST/DELETE /api/announcements (POST {priority:'PENTING',broadcast:true} broadcasts WA to all parents)
- GET/POST /api/notifications (?userId=) ; PUT /api/notifications/read {userId}
- GET /api/portal/parent?userId= -> ParentPortalData (students w/ attendanceSummary, attendances, hafalans, payments, billing; notifications; announcements)

Demo accounts (seeded): admin@daruljinan.sch.id/admin123 · ustadzah.fatimah@daruljinan.sch.id/guru123 · budi.santoso@gmail.com/ortu123

## Stage 1 complete: backend fully working. Stage 2: parallel UI agents (5-a public, 5-b admin, 5-c parent+login).

---
Task ID: 5-c
Agent: parent-portal (frontend-styling-expert)
Task: Login dialog + parent portal UI

Work Log:
- Read worklog.md API contract, types.ts, api-client.ts; verified live route shapes (portal/parent, sessions, attendance/checkin, payments PUT, notifications/read, auth/login) against component code.
- Skimmed ui primitives (dialog, tabs, dropdown-menu, card, progress, table, radio-group, badge, alert, button, input, label, skeleton, separator) and hooks/use-toast; confirmed lucide-react 0.525 icons (Mosque unavailable -> Landmark/MoonStar used).
- Created src/components/auth/login-dialog.tsx: controlled dialog, emerald gradient brand header (Landmark + custom DialogClose), email/password with show/hide Eye toggle, Loader2 submit, destructive Alert on failure (401 "Email atau password salah"), 3 demo quick-fill chips (Admin/Guru/Wali, Wali featured in emerald with amber WALI tag), note "Data demo — password default per peran". POST /api/auth/login -> onSuccess(user).
- Created src/components/parent/child-panel.tsx: exports ChildPanel + ParentStudent type. Per-child grid (grid-cols-1 lg:grid-cols-2, rounded-2xl white cards): (a) summary card w/ initials avatar, NIS/class badges, jadwal, ustadz, HADIR emerald/IZIN amber/SAKIT orange/ALPA red chips + Progress hadir/total; (b) attendance history max-h-72 scroll w/ formatShortDate + status badges; (c) hafalan list w/ grade circle (>=85 emerald, >=70 amber, <70 red), TAHFIDZ/TAHSHIN teal/MURAJAAH amber badges, italic teacherNote, avg grade + count header; (d) Tagihan card: amber outstanding strip, table (invoiceNo/title/amount/status/paidAt), PENDING -> "Bayar", FAILED -> "Coba Bayar Lagi"; PaymentDialog: invoice summary, RadioGroup QRIS(QrCode)/GoPay(Smartphone)/VA_BCA(Landmark), "Bayar Sekarang" -> 1.8s processing state ("Menghubungi gateway pembayaran...", buttons/dialog-close disabled) -> PUT /api/payments {id,status:'SUCCESS',method} -> success view (CircleCheck + "WhatsApp konfirmasi terkirim ke wali") -> onPaid refresh; timer cleanup on unmount, processing blocks dismiss; (e) QR check-in mini-card: kode input + POST /api/attendance/checkin {code, studentId} w/ toast result, hint chips of active codes.
- Created src/components/parent/parent-portal.tsx: exports ParentPortal({user,onLogout}). Sticky white/blur header (mini brand, refresh, bell DropdownMenu w/ unread amber badge + list + "Tandai semua dibaca" -> PUT /api/notifications/read, user avatar chip, logout), emerald hero "Assalamu'alaikum, {parent.name}" + first-2 announcements (PENTING on amber cards), child Tabs (one per santri, amber dot if pending tagihan), "Kotak Notifikasi WhatsApp" inbox card (*bold* segments rendered via split('*') -> <strong>, relative time, unread amber highlight, mark-all-read). GET /api/portal/parent?userId= with initial Skeleton screen, silent refresh, destructive error Alert + "Coba Lagi"; guarded empty-students state "Belum ada santri terdaftar pada akun ini".
- Decision: portal API students carry className but not classId, so active check-in code hints come from GET /api/sessions?active=1 fetched once in ParentPortal and filtered per child by className match (non-blocking, fails silently to []).
- Verified: bun run lint -> clean; bunx tsc --noEmit -> zero errors in src/components/auth|parent (remaining errors are 5-b admin files + pre-existing skills/prisma seed, untouched).

Stage Summary:
- Files created: src/components/auth/login-dialog.tsx (export LoginDialog), src/components/parent/parent-portal.tsx (export ParentPortal), src/components/parent/child-panel.tsx (exports ChildPanel, type ParentStudent). No other files touched.
- Design: emerald-700 primary / amber-500 accents / white rounded-2xl cards / stone-50 bg, fully Bahasa Indonesia, all money/dates via formatRupiah/formatDate/formatShortDate, responsive stacking, loading skeletons + empty states + retry alerts throughout.
- Integration: page.tsx (main agent) should render <LoginDialog open onOpenChange onSuccess> for auth and <ParentPortal user onLogout> for role ORANG_TUA; Toaster assumed mounted at app root.

---
Task ID: 5-a
Agent: public-portal (frontend-styling-expert)
Task: Public portal UI components

Work Log:
- Read worklog.md, types.ts, api-client.ts contract + verified API response shapes (registrations, sessions, checkin, students, announcements, curriculum, posts) against route.ts implementations before coding.
- Skimmed shadcn/ui components (button, card, dialog, select, tabs, badge, checkbox, sheet, alert, input, skeleton) to match exact APIs; confirmed Toaster is mounted in root layout and @/hooks/use-toast exists.
- Created 11 client components under src/components/public/: portal (composition + sticky header w/ smooth-scroll nav + mobile Sheet hamburger + Masuk/Cek-in actions), hero (emerald gradient + inline-SVG 8-point star lattice pattern + Bismillah + CTA + /api/stats stat chips with skeleton), announcements-ticker (framer-motion infinite marquee, PENTING in amber, click -> detail Dialog), about-section (visi/misi, 2005-2025 timeline, struktur organisasi hierarchy with connector lines, 4 keunggulan cards), curriculum-section (fetch curriculum+classes, subject-grouped cards TAJWID/HAFALAN/IBADAH/AKHLAK with distinct colors + Capaian line, class schedule chips, scroll area w/ custom webkit scrollbar), teachers-section (initials avatar gender-colored, years-from-joinDate badge, biodata Dialog with safe JSON.parse of formalEducation/nonFormalEducation/certifications, filosofi quote, kelas diampu), materials-section (category chips + class Select filter, type icons PDF/SLIDE/VIDEO/AUDIO, Unduh via <a target=_blank>), news-section (Tabs Semua/BERITA/KEGIATAN/ARTIKEL, featured card + grid, plain <img> with onError fallback over gradient, article Dialog with \n\n paragraph splitting), ppdb-section (2-col validated form, document checkboxes -> documents[], POST /api/registrations, success Dialog with big regNumber + copy + WA next-steps, right column Cek Status widget with status badges + 3-step timeline hint), checkin-section (santri Select grouped by class?.name, active session cards from /api/sessions?active=1 (click fills code), POST /api/attendance/checkin with success/already/error toasts + session refresh, QR-simulation info Alert), footer (emerald-950, 3 columns: brand+address+phone/WA+email, tautan cepat, jam operasional, (c) 2025 line).
- Design system enforced: emerald-700/800 primary, amber-500 gold accents, stone/white alternating section backgrounds, rounded-2xl cards with hover -translate-y-0.5 + shadow-lg, section eyebrow+3xl title+description pattern, py-16 max-w-6xl px-4 containers, scroll-mt-20 on sections, decorative Arabic snippets in font-serif, lucide-react icons only.
- QA: `bun run lint` -> 0 errors 0 warnings (removed one unused eslint-disable directive); `bunx tsc --noEmit` -> 0 errors in src/components/public (remaining 40 errors are pre-existing in prisma/seed.ts, src/components/admin, examples/, skills/ — other agents' scope).

Stage Summary:
- Files created (11): src/components/public/{portal,hero,announcements-ticker,about-section,curriculum-section,teachers-section,materials-section,news-section,ppdb-section,checkin-section,footer}.tsx — all 'use client', all UI text Bahasa Indonesia.
- Exports: PublicPortal({onOpenLogin}) (portal.tsx), Hero, StarLattice (reused by about/footer), AnnouncementsTicker, AboutSection, CurriculumSection, TeachersSection, MaterialsSection, NewsSection, PpdbSection, CheckinSection, Footer({onNavigate}).
- Decisions: framer-motion used for hero entrance + ticker loop (no inline <style> tags); marquee duplicated content x0%->-50% for seamless loop; ticker renders nothing when no announcements; curriculum/materials lists capped with max-h + custom thin webkit scrollbar via tailwind arbitrary selectors; <img> kept for post covers (next/image domain config avoided) with onError gradient fallback; teacher JSON fields parsed via safeParseArray with try/catch; PPDB status check fetches /api/registrations and matches regNumber case-insensitively; section anchors: beranda, tentang, kurikulum, guru, materi, berita, ppdb, checkin; smooth scroll via scrollIntoView (no globals.css change).

---
Task ID: 5-b
Agent: admin-dashboard (frontend-styling-expert)
Task: Admin dashboard UI components

Work Log:
- Read worklog.md API contract, src/lib/types.ts, src/lib/api-client.ts, and skimmed shadcn ui components (dialog/table/badge/sheet/tabs/switch/progress/alert-dialog/alert) + verified deps (recharts 2.15.4, qrcode.react 4.2.0, lucide-react) and exact response shapes of /api/sessions, /api/attendance, /api/posts, /api/announcements, /api/registrations routes before integrating.
- Created src/components/admin/dashboard.tsx: AdminDashboard({user,onLogout}) shell — emerald sidebar (hidden on md, Sheet hamburger on mobile) with 12 icon+label sections, sticky top bar (section title, Portal Publik link, user chip w/ role badge, logout), content area on stone-50. Role filter: ADMIN sees all; GURU only [Ringkasan, Kelas, Absensi, Hafalan, Materi] via GURU_ALLOWED allowlist; default section Ringkasan.
- Created overview.tsx: 6 KPI cards (Santri/Guru/Kelas/Pendaftar/Pemasukan/Tunggakan w/ formatRupiah), Kehadiran Hari Ini card (4 status dots + Progress attendanceRate), Sesi Aktif card (QRCodeSVG size=64, mono code, copy-to-clipboard toast), recharts emerald LineChart of attendanceTrend rate %, 4 recent lists (registrations w/ status badges, payments, hafalan w/ grade colors, WhatsApp notifications). Exports shared statusBadgeClass().
- Created registrations-admin.tsx: status filter Tabs w/ counts, table (regNumber, childName+gender, birthDate, parent+phone, documents JSON-parsed file chips, status badge), actions dropdown: Verifikasi (PUT VERIFIKASI), Terima dialog (optional kelas select → PUT {status:DITERIMA, classId} + toast re: auto-created santri+akun wali), Tolak dialog (reviewNote textarea), Hapus AlertDialog.
- Created students-admin.tsx: search (name/NIS) + class filter + Tambah/Edit dialog (fullName, gender, birthDate date input, address, optional class & parent selects w/ 'none' sentinel), status change menu (AKTIF/NONAKTIF/LULUS), _count chips (hafalan/tagihan/absen), Hapus confirm.
- Created teachers-admin.tsx: card grid w/ avatar initials, expertise badge, kelas diampu chips, isActive switch (PUT), big scrollable dialog form incl. repeatable EduListEditor rows (level/institusi/tahun) for formalEducation/nonFormalEducation/certifications sent via JSON.stringify; create dialog has optional account (email/password); delete shows API error toast (e.g. masih mengampu).
- Created classes-admin.tsx: card grid (level badge colors IQRA/TAHFIDZ/AL_QURAN, schedule, room, teacher, studentCount), Tambah/Edit dialog (name, level select, schedule, room, teacher select), Hapus confirm.
- Created attendance-admin.tsx: QR flow — left "Buka Sesi Kelas" (class select + topic + date default today → POST /api/sessions → BIG code + QRCodeSVG size=128 + copy + Tutup Sesi PUT isActive:false), active sessions list w/ hadir/total; right "Catat Kehadiran" (session select → loads /api/students?classId + /api/attendance?sessionId → per-student HADIR/IZIN/SAKIT/ALPA toggle buttons + note input for IZIN/SAKIT → Simpan POST {sessionId, records} + WA toast); below recent attendance log table.
- Created hafalan-admin.tsx: Catat Setoran card (student select w/ class, surah input + datalist of 31 Juz-30 surah names, ayatRange, type TAHFIDZ/TAHSHIN/MURAJAAH, grade 0-100, teacherNote → POST + WA toast), history table w/ grade coloring (>=85 emerald, >=70 amber, <70 red), delete confirm.
- Created materials-admin.tsx: Unggah Materi form (title, type PDF/SLIDE/VIDEO/AUDIO, url "Tautan file (URL)", category, optional class, teacher select required & locked to own teacherId for GURU via user prop), table w/ type icons, external-link open, delete confirm.
- Created payments-admin.tsx: summary cards (revenue SUCCESS sum, tunggakan PENDING sum+count), search + status filter, Buat Tagihan dialog (student, title "Iuran SPP Bulan Ini" placeholder, amount → POST + WA toast), table (invoiceNo, method chip, status badge, paidAt) w/ dropdown Tandai Berhasil (PUT {status:SUCCESS, method:MANUAL}) / Tandai Gagal / Hapus.
- Created content-admin.tsx: Tabs [Berita & Artikel | Pengumuman] — posts table (category badge, author, published Switch PUT, edit + Tulis Artikel dialogs w/ category/coverImage/content/published checkbox/author select from teachers, delete confirm); announcements cards (PENTING amber badge, delete), Buat Pengumuman dialog w/ priority select + conditional "Broadcast WhatsApp ke semua wali" checkbox → POST {broadcast:true}.
- Created users-admin.tsx (guarded non-ADMIN): table w/ role badges, change-role dropdown (PUT role), reset password dialog (PUT password), Tambah Pengguna dialog, Hapus confirm surfacing API error (last-admin case).
- Created whatsapp-log.tsx (guarded non-ADMIN): teal explainer alert "Simulasi WhatsApp Business API", message cards w/ phone, *bold* → <strong> rendering via split regex, SENT badge + relative time, Kirim Pesan Manual dialog (phone+message → POST /api/notifications), refresh button.
- QA: `bunx eslint src/components/admin/ --max-warnings=0` → clean; `tsc --noEmit` → zero errors in admin files (fixed TS18047 null-narrowing in attendance roster effect and TS2783 spread overwrite in setRecord); did not touch page.tsx/layout.tsx/globals.css/API routes/other agents' files; no new npm packages; did not run dev server.

Stage Summary:
- Files created (exactly 13, all 'use client'): src/components/admin/{dashboard,overview,registrations-admin,students-admin,teachers-admin,classes-admin,attendance-admin,hafalan-admin,materials-admin,payments-admin,content-admin,users-admin,whatsapp-log}.tsx
- Export: AdminDashboard({user,onLogout}) in dashboard.tsx; sections exported as OverviewSection, RegistrationsAdmin, StudentsAdmin, TeachersAdmin, ClassesAdmin, AttendanceAdmin, HafalanAdmin, MaterialsAdmin({user}), PaymentsAdmin, ContentAdmin, UsersAdmin({user}), WhatsAppLog({user}). MaterialsAdmin accepts optional user to lock teacher select for GURU; users/whatsapp re-guard ADMIN internally.
- Section visibility: ADMIN = all 12; GURU = Ringkasan, Kelas, Absensi, Hafalan, Materi (allowlist GURU_ALLOWED; admin-only items unreachable via nav since not rendered).
- Decisions: shared statusBadgeClass lives in overview.tsx (no extra files created beyond the 13 allowed); VERIFIKASI badge = teal (no blue anywhere; sky replaced by teal); Radix Select 'none' sentinel for optional FK selects; documents/education JSON parsed via safe helpers w/ try/catch; sessions fetched once and active list derived client-side (API verified); every fetch has skeleton + destructive alert w/ retry + empty state; all mutations refetch after success, disable submit with Loader2, and confirm destructive via AlertDialog; tables wrapped overflow-x-auto w/ min-w on inner table; long lists use max-h + overflow-y-auto + thin webkit scrollbar utility classes.

---
Task ID: 6-7
Agent: main (Z.ai Code)
Task: Integration + end-to-end browser QA

Work Log:
- Integrated page.tsx: session restore from localStorage, role-based routing (ORANG_TUA→ParentPortal, ADMIN/GURU→AdminDashboard, default→PublicPortal), LoginDialog wiring, loading screen.
- Added `onOpenPublic` SPA prop to AdminDashboard + ParentPortal ("Portal Publik" button switches views without reload).
- layout.tsx: metadata rebranded to SIMADJI — TPQ Darul Jinan, lang="id", favicon /logo.svg.
- Fixed lint error react-hooks/set-state-in-effect in page.tsx (deferred session restore via timer callback).
- Fixed seed.ts TS types (Prisma Teacher/User/Student[] annotations).
- Agent-browser E2E QA (all PASSED):
  1. Public portal full render (hero/tentang/kurikulum/guru/materi/kabar/PPDB/checkin/footer) — desktop 1280px + iPhone 15 emulation.
  2. Login dialog with demo chips: admin, wali.
  3. Admin dashboard: KPI cards, kehadiran hari ini, sesi aktif with QR.
  4. Absensi flow: created session "Ujian Bab Iqra 1" (Iqra 1) → QR rendered → marked Zahra HADIR + Rizky IZIN (note "Acara keluarga") → saved → toast "notifikasi WhatsApp terkirim".
  5. WhatsApp log: shows simulated BSP messages with *bold* rendering.
  6. Parent portal (Budi Santoso): 2 children tabs, outstanding Rp 75.000, announcements.
  7. Payment simulation: QRIS → "Menghubungi gateway pembayaran..." → success → status SUCCESS in table → WA confirmation (bell 1→2).
  8. QR check-in: duplicate prevented ("sudah tercatat IZIN").
  9. PPDB public form: validation toast on missing date → filled → success dialog with regNumber PPDB-2025-0004.
  10. PPDB admin acceptance: Terima + assign Iqra 1 → auto-created santri DJ-2025-009 + parent account ridwan.hakim@gmail.com (verified via API) + WA notif.
  11. Revenue updated live Rp 400.000 → Rp 475.000. Zero console/page errors; dev.log clean.

Stage Summary:
- FULL FEATURE-COMPLETE MVP per blueprint: public portal + PPDB + QR attendance + hafalan + materials + billing w/ simulated gateway + WhatsApp notification engine + parent portal + role-based dashboards.
- Demo accounts: admin@daruljinan.sch.id/admin123 · ustadzah.fatimah@daruljinan.sch.id/guru123 · budi.santoso@gmail.com/ortu123
- Known minor polish items for next iteration: mobile admin KPI card height could be tightened; date input could get friendlier Indonesian formatting hint; no real QR-scan camera (by design, code-typed simulation).

---
Task ID: 9-a
Agent: public-portal-enh (frontend-styling-expert)
Task: Public portal round-9 enhancements

Work Log:
- Read worklog.md, portal.tsx, hero.tsx, ui/accordion.tsx; grep-verified every existing section root already carries scroll-mt-20 (tentang/kurikulum/guru/materi/berita/ppdb/checkin) — no edits needed there; verified runtime Intl output "27 Rabiulawal 1448 H" (includes " H") and lucide exports (MoonStar/Star/Quote/CircleHelp) before coding.
- Created src/components/public/hijri-date.tsx: export HijriDate({className, variant:'dark'|'light'}). Zero-dependency Hijri date via Intl.DateTimeFormat('id-ID-u-ca-islamic-umalqura', day/month-long/year-numeric), computed once in useState initializer, try/catch -> renders null if calendar unsupported; normalizes suffix (appends " H" only if missing via /\bH$/i test); chip: MoonStar icon + text, dark = white/10 backdrop-blur border-white/20 text-amber-200 (hero), light = white card border-emerald-200 text-emerald-800 (header); suppressHydrationWarning guards server/client ICU drift.
- Integrated HijriDate: dark variant in hero under the Bismillah/TPQ-badge area; light variant in portal header next to brand, wrapped in hidden xl:block so lg/md headers stay uncluttered.
- Hero count-up: added local useCountUp(target, duration=1200) — useEffect + requestAnimationFrame, ease-out cubic (1-(1-p)^3), final frame pins Math target exactly (no drift), rAF cleanup on unmount; CountUpValue renders tabular-nums to stop layout jitter; chips value typed number|null (stats?.x ?? null); skeleton fallback when loading and "—" when fetch fails preserved.
- Created src/components/public/testimonials-section.tsx: export TestimonialsSection(), id="testimoni" scroll-mt-20 bg-stone-50, eyebrow "Testimoni" (amber) + title "Kata Wali Santri" + description matching section pattern; 4 static realistic Indonesian testimonials (Bpk. Hendra — Tahfidz, Ibu Ratna — Iqra 3, Bpk. Ahmad Fauzi — Al-Qur'an B, Ibu Dewi — Tahsin) mentioning mandiri mengaji, Portal Wali monitoring, akhlak; card: 5 filled amber Stars (role="img" aria-label), italic serif blockquote with &ldquo;&rdquo;, initials avatar alternating emerald-700/amber-500, name + program role, alternating md:rotate-±1 with hover:-translate-y-1 hover:rotate-0 hover:shadow-lg; grid gap-5 sm:grid-cols-2 xl:grid-cols-4.
- Created src/components/public/faq-section.tsx: export FaqSection(), id="faq" scroll-mt-20 bg-white; grid lg:grid-cols-5 — left lg:col-span-2 sticky (lg:top-24) intro: eyebrow "FAQ", title "Pertanyaan yang Sering Diajukan", description, emerald-gradient mini-card "Masih ada pertanyaan?" with tel:081234567890 + wa.me/6281234567890 buttons (amber CTA); right lg:col-span-3 shadcn Accordion type="single" collapsible, 7 Q&As (SPP Rp 75.000–100.000 tanpa uang pangkal; jadwal Senin–Sabtu 15.00–18.00; usia 4–15 tahun; program Iqra/Al-Qur'an/Tahfidz; cara daftar via form PPDB + dokumen saat verifikasi; pembayaran QRIS/GoPay/VA via Portal Wali + WA otomatis; laporan perkembangan: kehadiran/hafalan/tagihan + rapor); items as rounded-xl bordered cards px-5 with CircleHelp emerald icon; no fetch, all static.
- portal.tsx: composed order ...NewsSection → TestimonialsSection → FaqSection → PpdbSection → CheckinSection (bg alternation white→stone-50→white→stone-50→white preserved); added header HijriDate slot + imports; header NAV intentionally unchanged (footer covers new anchors, avoids lg clutter).
- footer.tsx: QUICK_LINKS + Testimoni ('testimoni') and FAQ ('faq') between Berita and PPDB.
- QA: bunx eslint on the 6 touched files --max-warnings=0 → clean; bunx tsc --noEmit → zero errors in src/components/public (remaining errors pre-existing in examples/, skills/, prisma/ — out of scope); dev server not run.

Stage Summary:
- Files created: src/components/public/hijri-date.tsx (export HijriDate, variants dark/light, null fallback), src/components/public/testimonials-section.tsx (export TestimonialsSection, static), src/components/public/faq-section.tsx (export FaqSection, shadcn Accordion single collapsible).
- Files edited: src/components/public/hero.tsx (useCountUp+CountUpValue count-up, HijriDate dark under badge, chips value null-safety), src/components/public/portal.tsx (composition order + xl header hijri slot), src/components/public/footer.tsx (quick links + Testimoni/FAQ). Section scroll-mt-20 audit done — all existing sections already compliant; new sections include scroll-mt-20.
- Decisions: header hijri chip shown only ≥xl to keep nav uncluttered; 7 FAQ items (task listed 7 topics though said "6"); testimonials background stone-50 for alternation; count-up pure rAF (no framer-motion needed), ends exactly at fetched value; no new npm packages, no APIs/layout/globals.css touched.
- New anchors for future agents: #testimoni, #faq (both scroll-mt-20, wired into footer quick links).

---
Task ID: 9-b
Agent: admin-enh (frontend-styling-expert)
Task: Admin dashboard round-9 enhancements

Work Log:
- Read worklog.md contract + the 3 target files (students-admin, payments-admin, overview) and types.ts; verified POST /api/notifications shape ({phone, message, userId?} — phone+message required, userId optional) and that lucide-react has BellRing/Download; verified Payment.student.parent carries {id,name,phone} and Student.parent carries phone.
- overview.tsx: (a) added shared client-side CSV helpers exported alongside statusBadgeClass (same established pattern, no new files): csvCell (`;` separator, `"`/`;`/newline values wrapped in quotes with `"` doubled), csvDate (dd/mm/yyyy from ISO, '' for null/invalid), csvFileStamp (local YYYY-MM-DD for filenames), downloadCsv (BOM `\uFEFF` prefix, text/csv;charset=utf-8 Blob, URL.createObjectURL + temp <a> click + revoke); (b) mobile KPI polish: KPI grid `grid-cols-2 gap-3 lg:grid-cols-3 lg:gap-4` (was grid-cols-1 → sm:2 → xl:3; 6-card structure intact), KpiCard padding p-3 sm:p-4, icon container size-9 sm:size-10 with size-4 sm:size-5 icon, value text-lg sm:text-xl, muted hint `hidden sm:block` on very small screens — desktop (sm+) rendering unchanged, loading-skeleton grid mirrored.
- students-admin.tsx: added "Ekspor CSV" outline Download button next to "Tambah Santri" (disabled while loading/exporting/0 filtered rows, Loader2 spin while exporting); exportCsv() builds header + currently-FILTERED rows with columns NIS;Nama Lengkap;Jenis Kelamin (Laki-laki/Perempuan);Tanggal Lahir (dd/mm/yyyy);Kelas;Wali;No HP Wali;Status;Alamat → downloadCsv(`santri-darul-jinan-YYYY-MM-DD.csv`) + success toast with row count.
- payments-admin.tsx: (a) "Ekspor CSV" outline button near filters exporting FILTERED rows: No Invoice;Santri;Keterangan;Nominal (plain integer, no Rp);Metode;Status;Tanggal Bayar;Tanggal Dibuat (dd/mm/yyyy) → `keuangan-darul-jinan-YYYY-MM-DD.csv` + row-count toast; (b) per-row "Kirim Reminder WA" DropdownMenuItem (BellRing, amber) visible only for PENDING/FAILED, disabled when p.student.parent.phone missing, sends POST /api/notifications {phone: parent.phone, userId: parent.id, message} with exact template `Pengingat: Tagihan *{title}* sebesar Rp {amount id-ID} ({invoiceNo}) untuk {studentName} masih menunggu pembayaran. Bayar mudah via Portal Wali SIMADJI (QRIS/GoPay/VA). Terima kasih. — TPQ Darul Jinan` (buildReminderMessage module fn), toast "Reminder terkirim ke {parent name}", row spinner via busyId; (c) bulk "Ingatkan Semua" amber outline BellRing button next to "Buat Tagihan" (disabled when loading/bulkSending/0 remindable pending), AlertDialog confirm "Sistem akan mengirim pengingat ke N tagihan tertunda…" (+ note when some pending lack parent phone), then sequential for-loop over loaded PENDING payments with parent phone: per-item POST with 250ms await between sends, per-item try/catch continues on failure, Loader2 + "Mengirim…" label during send, final toast "{ok} reminder terkirim" with skipped/failed counts.
- QA: `bun run lint` clean (0 errors/warnings); `bunx tsc --noEmit` → zero errors under src/components/admin (remaining errors pre-existing in examples/ + skills/ scaffolds only); did not touch API routes, page.tsx/layout/globals.css, or any file outside the 3 listed; no new npm packages; did not run dev server.

Stage Summary:
- Files touched (exactly 3): src/components/admin/overview.tsx (+4 exported CSV helpers: csvCell, csvDate, csvFileStamp, downloadCsv; mobile KPI polish), src/components/admin/students-admin.tsx (CSV export), src/components/admin/payments-admin.tsx (CSV export + per-row & bulk WhatsApp reminders).
- CSV format: `;` separator (Excel id-ID friendly), RFC-quoted escaping, BOM for Excel UTF-8, dd/mm/yyyy dates, plain-integer nominal (75000), filenames with local-date stamp; exports always reflect the currently filtered rows; empty result disables the button.
- Reminders reuse the existing POST /api/notifications route — zero backend changes; per-row items gated to PENDING/FAILED and disabled without parent phone; bulk flow is sequential with small awaits (gentle on the simulated BSP), counts successes and reports skipped (no phone) / failed in the summary toast.
- Mobile KPI decision: grid is 2-col from base breakpoint (killing the tall 1-col mobile stack), 3-col from lg; hint text hidden <sm; all sm+ styles identical to previous desktop rendering.

---
Task ID: 9-c
Agent: parent-enh (frontend-styling-expert)
Task: Parent portal Rapor Santri printable report

Work Log:
- Read worklog.md (API contract), child-panel.tsx (650 lines: PaymentDialog, QrCheckinCard, ChildPanel), parent-portal.tsx, types.ts (ParentPortalData/Hafalan/Payment shapes), api-client.ts (formatRupiah/formatShortDate), ui/dialog.tsx (data-slot attrs), ui/table.tsx (TableFooter export), button/card variants; verified lucide-react 0.525 icon availability (FileText/Printer/ClipboardCheck/BookOpenCheck/Wallet) via node probe; reused public footer WA contact (0812-3456-7890 / wa.me/6281234567890).
- Added RaporDialog in child-panel.tsx: Dialog (max-h-[80vh] overflow-y-auto, sm:max-w-2xl) with formal report #rapor-santri-print — emerald letterhead (Landmark medallion + "TPQ DARUL JINAN" + "Laporan Perkembangan Santri — SIMADJI" + amber "Rapor {year}" chip, 2px emerald border frame), 2-col identity grid (Nama Lengkap, NIS, Kelas, Program & Jadwal, Ustadz/Ustadzah Pengampu, Tanggal Cetak via formatShortDate(new Date())), "Ringkasan Kehadiran" (4 status cards w/ colored dots reusing ATT_COUNT_KEY + big bold % Tingkat Kehadiran w/ "dari N pertemuan tercatat" note + mini progress bar), "Progres Hafalan" table (Surah/Ayat/Jenis badge reusing HAFALAN_BADGE/Nilai text-colored >=85 emerald >=70 amber else red via new gradeTextClass/Catatan whitespace-normal so notes print fully) + TableFooter "Rata-rata Nilai" (avg of non-null grades or "–"), "Keuangan" 2 cards (Total Dibayar = SUCCESS count+sum emerald, Tunggakan = PENDING count+sum amber, formatRupiah), italic auto-generation footer + WA sekretariat line; button row class no-print: outline "Tutup" + emerald "Cetak / Simpan PDF" (Printer) -> window.print().
- PRINT implementation: scoped <style dangerouslySetInnerHTML> mounted only while dialog open (@media print): body>*:not(:has(#rapor-santri-print)) display:none (removes app shell/toast portals from layout -> no blank pages, degrades to classic visibility approach on :has-less engines), html/body overflow:visible + height:auto !important (defeats Radix react-remove-scroll inline overflow:hidden that would clip pagination), body * visibility:hidden + report subtree visibility:visible, dialog-overlay/dialog-close/.no-print display:none, [data-slot=dialog-content] reset to static/untransformed/no-max-h/no-padding so the report flows from page top and paginates, print-color-adjust:exact keeps emerald/amber fills in PDF.
- Wired button into child summary card: CardAction now flex flex-wrap items-center justify-end gap-2 holding outline-emerald sm Button "Rapor Santri" (FileText, h-8 rounded-lg border-emerald-300) beside existing "Kehadiran N%" badge (mobile-safe wrapping); RaporDialog state raporOpen in ChildPanel, rendered alongside PaymentDialog (no parent-portal.tsx changes needed — data all from child prop).
- Empty-state guards: hafalans -> "Belum ada setoran tercatat..."; payments -> "Belum ada tagihan tercatat untuk santri ini."; attendanceSummary.total === 0 -> "–" + "Belum ada pertemuan tercatat."; classSchedule/teacherName fallback "—".
- QA: bun run lint -> clean; bunx tsc --noEmit -> 0 errors in src/components/parent (remaining errors pre-existing in examples/, skills/); did not run dev server; touched only child-panel.tsx.

Stage Summary:
- File changed: src/components/parent/child-panel.tsx only (+~300 lines; parent-portal.tsx untouched, no API/prop changes, no new deps).
- New exports-internals: RaporDialog + RaporSectionTitle + gradeTextClass + RAPOR_ATT_DOT + RAPOR_PRINT_CSS; ParentStudent/ChildPanel API unchanged.
- Decisions: kept single-file scope by sourcing everything from the child prop (attendances/hafalans/payments/attendanceSummary/nis/className/classSchedule/teacherName); computed SUCCESS/PENDING sums from payments[] rather than billing{} so count+sum semantics match the spec; reused existing badge/grade maps for visual consistency; Landmark used as mosque icon (Mosque glyph unavailable in lucide 0.525, per task 5-c precedent); report table Catatan column uses whitespace-normal to avoid ellipsis-truncation in print.

---
Task ID: 9 (cron review round)
Agent: main (Z.ai Code) + 3 subagents (9-a/9-b/9-c)
Task: Round-9 review — QA sweep, new features, styling polish

Work Log:
- Reviewed worklog; project state = stable feature-complete MVP (rounds 1-7), zero known bugs.
- QA sweep via agent-browser: public portal anchor navigation OK (no sticky-header overlap), admin login + Keuangan regression OK, zero console/page errors, dev.log clean. No bugs to fix → proceeded to feature additions.
- [9-a subagent] Public portal: NEW HijriDate widget (zero-dep Intl islamic-umalqura, dark/light variants, in header + hero), count-up animation for hero stats (rAF ease-out), NEW TestimonialsSection (4 wali santri quotes w/ amber stars), NEW FaqSection (7 Q&As accordion + sticky contact card w/ WhatsApp chat button), footer quick links updated, all sections carry scroll-mt-20.
- [9-b subagent] Admin: CSV export buttons in StudentsAdmin + PaymentsAdmin (client-side BOM CSV, `;` separator Excel-id friendly, filtered rows, dd/mm/yyyy dates), per-row "Kirim Reminder WA" for PENDING/FAILED invoices + bulk "Ingatkan Semua" with AlertDialog + sequential send summary toast; overview KPI grid now grid-cols-2 on mobile (tighter cards, desktop unchanged).
- [9-c subagent] Parent portal: NEW "Rapor Santri" printable report dialog — formal letterhead, identity block, attendance summary w/ rate, hafalan table w/ grade colors + average row, keuangan summary (paid vs outstanding), auto-generation footer + WA line; scoped @media print CSS (hides app shell, keeps report full-flow, print-color-adjust exact); empty-state guards.
- [main] globals.css polish: smooth scroll, emerald ::selection, thin emerald global scrollbar, emerald :focus-visible outline (a11y).
- E2E verified in browser: Hijri chip "27 Rabiulawal 1448 H" (header+hero), testimonials render, FAQ accordion opens, CSV export toast "9 data santri tersimpan", reminder toast "Reminder terkirim ke Ahmad Hidayat" (INV-2025-1006), Rapor Santri shows correct data (Rp 150.000 dibayar / Rp 0 tunggakan) + print CSS mounted (1065 chars), zero errors after all interactions.

Stage Summary:
- Round 9 shipped: Hijri date widget, testimonials, FAQ, count-up stats, CSV exports (santri+keuangan), WA payment reminders (single+bulk), mobile KPI polish, printable Rapor Santri, global a11y/scrollbar/selection polish. Lint+tsc clean.
- Verification status: all new features browser-verified; print flow CSS-verified (window.print not triggered in headless).
- Next-step candidates (priority order): (1) guru-specific landing view in AdminDashboard (today's sessions of own classes, quick setoran), (2) announcement detail page/section on public portal (currently ticker+dialog only), (3) hafalan progress chart in parent portal (juz-30 coverage map), (4) settings page (admin change own password), (5) seed a hafalan record for Rizky/Alika so Budi's demo shows non-empty rapor hafalan table.

---
Task ID: 10-c
Agent: settings-password (frontend-styling-expert)
Task: Settings section + self-service password change

Work Log:
- Read worklog.md (API contract + round-9 "next-step candidates" that listed settings/password), src/lib/api.ts, src/lib/api-client.ts, auth/login route pattern, prisma/schema.prisma (User model), types.ts (AuthUser), admin/overview.tsx + users-admin.tsx + dashboard.tsx (card style rounded-2xl border-stone-200 shadow-sm, roleBadgeClass/roleLabel, initials), auth/login-dialog.tsx (Eye toggle pattern), hooks/use-toast.ts, ui primitives (avatar/alert/input/label/button); verified all lucide 0.525 icons used (KeyRound/Eye/EyeOff/User/Mail/Phone/ShieldCheck/Info/Loader2/Fingerprint) via node_modules grep; confirmed destructive toast variant precedent.
- Created src/app/api/auth/password/route.ts: PUT handler — body {userId, currentPassword, newPassword}; 400 'Data tidak lengkap' when incomplete; 400 'Password baru minimal 6 karakter' when <6; 404 'Pengguna tidak ditemukan'; 401 'Password saat ini salah' on plaintext mismatch (demo auth); db.user.update + ok({success:true}); catch -> 400 'Gagal mengubah password'. Doc comment header with full contract.
- Created src/components/admin/settings-section.tsx ('use client', export SettingsSection({user}: {user: AuthUser})): (a) Profil Akun card — size-12 Avatar emerald-700 initials fallback, name + role badge (exact dashboard.tsx roleBadgeClass classes), per-role description, read-only dl rows (Email/Mail, Nama Lengkap/User, No. HP or '—'/Phone, Peran/ShieldCheck+Badge, ID Pengguna truncated mono w/ title attr/Fingerprint); (b) Ubah Password card — 3 controlled password fields (h-11 inputs) each with inside-wrapper show/hide toggle (size-11 button, aria-label "Tampilkan password"/"Sembunyikan password", aria-pressed), helper "Minimal 6 karakter", inline red-600 role="alert" errors cleared per-field on typing; client validation (all required, new>=6, new===confirm) blocks submit; emerald submit (h-11, KeyRound) with Loader2 spin + disabled while saving; raw fetch PUT /api/auth/password JSON {userId: user.id, currentPassword, newPassword}; success -> toast "Password berhasil diubah" + clears all fields/show-flags/errors; failure -> parses res.json() .error into destructive toast, and 401 additionally sets inline error under "Password Saat Ini"; amber Alert note "Demo: password disimpan sebagai teks biasa untuk keperluan demo. Pada produksi gunakan hashing (bcrypt/argon2)." (c) Akun Demo card — 3 rows (role badge + mono email + mono password chip): Admin admin@daruljinan.sch.id/admin123, Guru ustadzah.fatimah@daruljinan.sch.id/guru123, Wali budi.santoso@gmail.com/ortu123 + Info note "Gunakan akun ini untuk mencoba berbagai peran." Layout: grid lg:grid-cols-2 (left column stacks Profil + Akun Demo, right column Ubah Password), items-start.
- QA: bun run lint -> clean; bunx tsc --noEmit | grep "settings-section|auth/password" -> empty (zero errors in new files); dev server NOT run; no dashboard.tsx or other existing files touched; no new npm packages.

Stage Summary:
- Files created (exactly 2): src/app/api/auth/password/route.ts, src/components/admin/settings-section.tsx.
- API contract: PUT /api/auth/password {userId, currentPassword, newPassword} -> 200 {success:true} | 400 (incomplete/<6 chars/catch) | 401 wrong current password (message 'Password saat ini salah') | 404 user not found. Errors as {error: string}.
- Component contract: export SettingsSection({ user }: { user: AuthUser }) from '@/components/admin/settings-section' — main agent wires into dashboard.tsx (suggested: new 'pengaturan' section key, non-admin-only, e.g. Settings icon, render <SettingsSection user={user} />).
- Decisions: plain controlled inputs (no react-hook-form) matching codebase style; 44px touch targets everywhere (h-11 inputs, size-11 toggles, h-11 submit); roleBadgeClass/roleLabel/initials re-declared locally (same values as dashboard.tsx) to avoid editing existing files; Fingerprint icon used for ID Pengguna row (verified in lucide 0.525); raw fetch instead of apiSend so the 401 wrong-current-password case can set the inline field error; sr-only aria-live region announces form errors; emerald/amber/stone palette only (no indigo/blue).

---
Task ID: 10-a
Agent: guru-overview (frontend-styling-expert)
Task: Guru landing view component

Work Log:
- Read worklog.md context; confirmed API shapes by reading route files (classes returns ClassRoom[] with teacherId+studentCount; sessions?active=1 returns SessionItem[] w/ classId/className/code/total/hadir; hafalan returns Hafalan[] w/ student{id,fullName,nis,className} but NO classId and take:100; students returns Student[] with classId+status+_count); verified apiGet signature, useToast, Badge/Button internals (tw-merge overrides OK), qrcode.react ^4.2.0, and all lucide icons via node probe (QrCode/BookMarked/Copy/CalendarCheck/Users/GraduationCap/RefreshCw/AlertCircle/Inbox/BookOpen all true).
- Created src/components/admin/guru-overview.tsx ('use client') ONLY (no other file touched, per constraint — dashboard.tsx wiring left to main agent). Exports GuruOverview({user, onNavigate}) + type GuruOverviewSection = 'attendance' | 'hafalan' | 'classes'.
- Data: ONE useEffect → async load (useCallback, mountedRef cleanup guard for setState-after-unmount) with Promise.all of the 4 GETs into single GuruData state; loading skeleton mirrors final layout (hero + 4-KPI grid + 2-col cards); error = Alert destructive + Coba Lagi (re-calls load), same pattern as overview.tsx.
- Client-side ownership filtering: ownClasses by teacherId===user.teacherId → ownClassIds Set → ownSessions (classId ∈ set), ownStudents (classId ∈ set), ownStudentCount (status AKTIF), ownHafalan via studentId ∈ ownStudentIds (reliable path since /api/hafalan student has no classId); hafalanCapped = raw hafalans.length >= 100 → KPI hint "Dari 100 terakhir".
- UI (Indonesian, emerald+amber): greeting hero card (gradient from-emerald-700 via-emerald-800 to-emerald-900, white text, decorative BookOpen watermark, date via toLocaleDateString('id-ID') with suppressHydrationWarning, motivational line, h-11 (44px) buttons "Buka Absensi" (amber bg-amber-400 text-emerald-950) + "Catat Hafalan" (white/40 outline) — buttons hidden when onNavigate undefined); KPI row grid-cols-2 lg:grid-cols-4 with KpiCard copied/adapted from overview.tsx (Kelas Saya emerald BookOpen, Santri Saya amber Users, Sesi Aktif teal QrCode, Setoran Hafalan stone BookMarked); two-column grid: (a) "Sesi Aktif Kelas Saya" — per-row QRCodeSVG size 56 in white tile, className + AKTIF badge + hadir/total emerald outline chip, topic, mono code tracking-[0.25em], formatShortDate, size-11 copy button (aria-label per class) → useToast "Kode disalin", thin-scrollbar max-h-72 scroll area, empty state dashed border + "Buat Sesi" → onNavigate('attendance'); (b) "Setoran Hafalan Terbaru" — 8 rows: student name, surah+ayat, type badge (Tahfidz emerald/Tahsin teal/Murajaah amber), grade badge (>=85 emerald/>=70 amber/else red, null → "—" with aria-label), timeAgo (local helper, falls back to formatShortDate, same as whatsapp-log.tsx), max-h-80 scroll, empty "Belum ada setoran hafalan tercatat", footer full-width outline button "Lihat Semua / Catat Baru" → onNavigate('hafalan').
- No-assignment case (user.teacherId null or ownClasses empty): greeting hero + centered card (GraduationCap in emerald pastel, "Anda belum ditugaskan sebagai pengajar kelas" + contact-admin description); KPI/sessions/hafalan sections hidden.
- QA: `bun run lint` → clean (0 errors/warnings); `bunx tsc --noEmit | grep guru-overview` → empty (4 remaining project errors are pre-existing in examples/skills scaffolds, none under src/); no new npm packages; no dev server run.

Stage Summary:
- File created: src/components/admin/guru-overview.tsx (only file touched). Exports: `GuruOverview({ user, onNavigate }: { user: AuthUser; onNavigate?: (section: 'attendance' | 'hafalan' | 'classes') => void })` and `type GuruOverviewSection`. Internal helpers (module-scoped, not exported): KpiCard, GreetingHero, gradeBadgeClass, timeAgo, HAFALAN_TYPE map, SCROLL_AREA utility string.
- Contract for wiring (main agent): render <GuruOverview user={user} onNavigate={(s) => setSection(s)} /> in dashboard.tsx GURU branch for section 'ringkasan'; all onNavigate targets ('attendance' | 'hafalan' | 'classes') are already-existing sections; component is self-contained (own fetch + toast), safe to drop in without prop changes.
- Decisions: single GuruData state object to keep setState count minimal in the Promise.all flow; hafalan ownership via students join (API has no classId on hafalan rows); KPI "Santri Saya" counts only AKTIF students per spec; copy icon buttons sized size-11 (44px) and all CTAs h-11 to meet the 44px touch-target rule; QR size 56 (within 56-64 spec) to fit hadir chip rows; no new colors beyond existing emerald/amber/teal/stone palette.

---
Task ID: 10-b
Agent: hafalan-chart (frontend-styling-expert)
Task: Parent portal hafalan progress visualization

Work Log:
- Read worklog.md (tasks 1, 5-c, 9-c for parent portal context), child-panel.tsx (951 lines), types.ts (Hafalan shape), ui/card.tsx + ui/progress.tsx APIs, api-client.ts date helpers; verified recharts 2.15.4 exports ReferenceLine (node probe) and its Tooltip labelFormatter typing (payload[0].payload is any, safe access); inspected admin/overview.tsx existing LineChart conventions (colors #047857/#78716c/#e7e5e4, margin, dot sizes) for visual consistency; cross-checked prisma/seed.ts + live SQLite hafalan rows for real surahName spellings ("An-Naba", "An-Nazi'at", "Abasa", "Al-Fatihah", "Al-Lail").
- Created src/components/parent/hafalan-chart.tsx ('use client'): exports HafalanProgress({ hafalans }) rendering grid gap-4 lg:grid-cols-2 with two rounded-2xl border-stone-200 shadow-sm cards. (a) "Peta Hafalan Juz 30": static 37-entry list (no. 1 Al-Fatihah + 78-113 Juz 30, Indonesian transliterations), match via norm = s.toLowerCase().replace(/[^a-z0-9]/g,'') applied to both list names and h.surahName (so "An-Naba'" ~ "An-Naba", "An-Nazi'at" ~ "An-Naziat"); per-surah setoran count via useMemo Map (any TAHFIDZ/TAHSHIN/MURAJAAH record counts); header "X dari 37 surah tercapai" + shadcn Progress (h-2 bg-stone-200 [&>div]:bg-emerald-600, same as child-panel usage) + % label; tiles = flex flex-wrap gap-1.5 text-[11px] px-2 py-1 rounded-full chips in max-h-56 overflow-y-auto pr-1 [scrollbar-width:thin]; tercapai & count===1 -> solid bg-emerald-600 text-white + lucide Check (size-3 strokeWidth 3), tercapai & count>1 -> border-emerald-400 bg-emerald-100 text-emerald-900 + "×N" setoran-count badge (bg-emerald-700/15), belum -> border-stone-200 bg-stone-50 text-stone-400; each chip title="QS X — sudah disetorkan Nx / belum disetorkan"; legend (emerald/stone dots) + hint "Peta mengikuti cakupan Juz 30 (hafalan surat pendek)."; a11y role="img" aria-label coverage summary on the map container. (b) "Tren Nilai Hafalan": graded records (grade !== null, type-predicate filter to GradedHafalan) sorted ASC by createdAt -> TrendPoint {date 'dd/MM' zero-padded, nilai, surah}; <2 graded -> Inbox empty-note "Belum cukup data nilai untuk menampilkan tren." + count note; else h-56 w-full ResponsiveContainer LineChart matching admin chart style: CartesianGrid #e7e5e4 dasharray 3 3 vertical=false, XAxis date tick 11/#78716c tickLine/axisLine=false, YAxis domain [0,100] ticks [0,25,50,75,100], Tooltip formatter [value,'Nilai'] + labelFormatter -> "QS {surah}" from payload[0].payload, contentStyle radius 12, Line monotone dataKey nilai #047857 strokeWidth 2.5 dot r3 activeDot r5, ReferenceLine y=70 #d97706 dasharray "4 4" (KKM) + explanatory dashed-amber legend line under chart; "Rata-rata: NN" pill badge (>=85 emerald-100/emerald-800, >=70 amber, else red).
- Edited src/components/parent/child-panel.tsx minimally (2 edits only): added named import { HafalanProgress } from './hafalan-chart' after the types import; inserted guarded block {child.hafalans.length > 0 && (<div className="lg:col-span-2"><HafalanProgress hafalans={child.hafalans} /></div>)} between the Progres Hafalan Card (c) and Tagihan Card (d) — spacing inherited from parent grid gap-4, lg:col-span-2 wrapper so both cards sit side-by-side on desktop / stacked on mobile; empty-table "Belum ada catatan hafalan." behavior untouched; nothing else changed in the file.
- QA: bun run lint -> clean (no output); bunx tsc --noEmit -> zero errors mentioning hafalan-chart/child-panel (remaining tsc errors pre-existing in examples/ + skills/ per task 9-c); did not run dev server; node sanity checks: 37 unique surah entries (no.1 + 78..113), all 5 seeded DB surahNames resolve MATCH under the normalizer.

Stage Summary:
- Files: NEW src/components/parent/parent/hafalan-chart.tsx (~215 lines, sole new export HafalanProgress); EDIT src/components/parent/child-panel.tsx (+1 import, +6 lines render block). No API/prop changes, no new deps, parent-portal.tsx untouched.
- Matching strategy: lowercase + strip non-alphanumerics on both sides; JUZ30 list precomputed to a ReadonlyMap at module level; count per surah drives tile variant (solid emerald w/ Check = 1x, outlined emerald w/ ×N badge = multiple, stone outline = belum) — murajaah/tahshin records count toward coverage by design.
- Decisions: named export + named import (spec asked for named function export); guarded render at call site to satisfy "only when > 0" (component still degrades gracefully); KKM=70 shared const reused by ReferenceLine, avg badge threshold and chart caption; chart styling copied from admin overview LineChart for cross-portal consistency; trend shows one point per graded record (duplicate dd/MM labels possible but honest); a11y via role="img" + aria-labels on both visualization containers and title tooltips per surah chip.

---
Task ID: 10 (cron review round)
Agent: main (Z.ai Code) + 3 subagents (10-a/10-b/10-c)
Task: Round-10 review — QA sweep, guru landing, hafalan analytics, settings, demo seed

Work Log:
- Reviewed worklog (round 9 complete, zero known bugs). QA sweep: lint + tsc clean, dev.log clean, browser E2E (public portal render, guru login, parent portal rapor state, zero console errors). No bugs → proceeded to features.
- [10-a subagent] NEW src/components/admin/guru-overview.tsx: GuruOverview({user, onNavigate}) — emerald greeting hero (Indonesian date + quick actions "Buka Absensi"/"Catat Hafalan"), 4 KPI (Kelas Saya/Santri Saya/Sesi Aktif/Setoran Hafalan via client-side filtering of /api/classes + /api/sessions?active=1 + /api/hafalan + /api/students by teacherId→classId→studentId ownership), Sesi Aktif Kelas Saya w/ QRCodeSVG + copy, Setoran Hafalan Terbaru w/ grade badges, skeletons/error-retry/empty states, no-assignment fallback card.
- [10-b subagent] NEW src/components/parent/hafalan-chart.tsx (HafalanProgress({hafalans})): "Peta Hafalan Juz 30" coverage map (norm-matched chips: solid emerald 1x / ×N badge multi / stone belum + Progress %) + "Tren Nilai Hafalan" recharts LineChart (sorted asc, KKM 70 dashed ReferenceLine, avg badge); wired in child-panel.tsx after Progres Hafalan card when child.hafalans.length > 0.
- [10-c subagent] NEW src/app/api/auth/password/route.ts (PUT {userId, currentPassword, newPassword}: 400 short/incomplete, 401 wrong current, 404 unknown) + NEW src/components/admin/settings-section.tsx (SettingsSection({user}): Profil Akun card, Ubah Password form w/ eye toggles + inline 401 error + toasts, Akun Demo card, plaintext-security note).
- [main] Wired dashboard.tsx: SectionKey + 'pengaturan' (Settings icon, all roles, added to GURU_ALLOWED), ringkasan renders GuruOverview for GURU else OverviewSection, onNavigate→setActive; role-aware header copy (GURU ringkasan → "Ikhtisar kelas dan aktivitas mengajar Anda").
- [main] NEW prisma/seed-hafalan.ts (idempotent, safe re-run; only inserts when student has 0 records): Rizky DJ-2024-001 5 setoran (Al-Fatihah ×2 incl. murajaah, An-Nas, Al-Falaq, Al-Ikhlas), Alika DJ-2023-021 6 setoran (An-Naba', An-Nazi'at, Abasa, At-Takwir ×2 incl. tahshin, Al-Ikhlas) with grades 77-94 + teacher notes; ran it → 11 records.
- [main BUGFIX] hafalan-chart.tsx Juz-30 list was missing Surah 84 Al-Insyiqaq → all numbers after Al-Mutaffifin off by one (An-Nas shown as 113, total 37 instead of 38). Added Al-Insyiqaq (84) and renumbered through An-Nas (114); map now "X dari 38 surah tercapai".
- E2E verified: guru landing (hero + KPIs 1 kelas/2 santri/0 sesi/1 setoran + quick-nav works), Pengaturan renders + password API (401 wrong → success → revert), parent portal Rizky (4/38, Al-Fatihah ×2, avg 86 + KKM line) & Alika (5/38, At-Takwir ×2, avg 85), admin overview unchanged w/ Pengaturan nav, mobile 390px guru view clean, zero console/page errors, lint + tsc clean.

Stage Summary:
- Round 10 shipped: role-aware guru landing (GuruOverview), parent hafalan analytics (Juz-30 map + grade trend), self-service Pengaturan + password change API, 11 seeded demo hafalan records (idempotent prisma/seed-hafalan.ts), guru header copy polish. Data-accuracy bug fixed in Juz-30 list (missing Al-Insyiqaq 84).
- New API: PUT /api/auth/password {userId, currentPassword, newPassword} → {success:true} | {error}.
- New wiring: dashboard 'pengaturan' section (all roles incl. GURU); GURU ringkasan → GuruOverview(user, onNavigate).
- Demo data: Budi's children now have non-empty rapor/hafalan tables, coverage maps, and grade trends (seed script re-runnable safely).
- Next-step candidates (priority order): (1) announcement detail section on public portal (currently ticker+dialog only), (2) guru weekly schedule card (parse class.schedule into visual timetable), (3) hafalan export CSV for guru/admin, (4) parent portal notification bell for new hafalan setoran (Notification rows already exist), (5) admin mass-invoice generator per class (monthly SPP batch).

---
Task ID: 11-a
Agent: announcements-section (frontend-styling-expert)
Task: Public "Pengumuman" browsable announcements section on the public portal

Work Log:
- Read worklog.md (API contract + round-9/10 next-step candidate #1), api-client.ts (apiGet/formatDate/formatShortDate), types.ts (Announcement = {id,title,content,priority:'NORMAL'|'PENTING',createdAt}), portal.tsx, news-section.tsx (data-fetch/section-header/card/dialog patterns), announcements-ticker.tsx (priority badge + dialog conventions), testimonials-section.tsx (bg alternation + eyebrow variants), verified /api/announcements route (orderBy createdAt desc, take 20), ui/badge.tsx internals, and lucide 0.525 icon availability via node probe (ArrowRight/BadgeCheck/AlertTriangle/AlertCircle/CalendarDays/ChevronRight/Megaphone/RefreshCw/MessageCircle/History all true).
- Created src/components/public/announcements-section.tsx ('use client', export AnnouncementsSection): section id="pengumuman" scroll-mt-20 bg-stone-50 py-16 (opposite of NewsSection's bg-white) aria-labelledby="pengumuman-heading"; header follows the exact section pattern — amber eyebrow pill "Pengumuman Resmi", h2 "Pengumuman Terbaru" with inline emerald count Badge "{n} Pengumuman" (Megaphone icon) shown when data exists, muted description; fetch via apiGet<Announcement[]>('/api/announcements') with load useCallback + error state + destructive-red retry alert (news-section pattern).
- Cards: first 3 announcements as grid gap-4 md:grid-cols-3 md:gap-6 feature cards — article role="button" tabIndex=0 + Enter/Space keydown (news-section a11y pattern), aria-label "Baca pengumuman: {title}", rounded-2xl border-stone-200 bg-white shadow-sm hover:-translate-y-0.5 hover:shadow-md focus-visible:ring-2 ring-emerald-600, PENTING cards get border-l-4 border-l-amber-400; PriorityBadge: PENTING = solid amber-500 + AlertTriangle, NORMAL = emerald outline + BadgeCheck; line-clamp-2 title with group-hover:text-emerald-800, whitespace-normalized line-clamp-3 content preview, footer border-t row with CalendarDays amber date (formatShortDate, id-ID) + "Baca selengkapnya" + ArrowRight group-hover:translate-x-0.5.
- Riwayat: announcements[3..] as compact list in rounded-2xl bordered white container, "Riwayat Pengumuman" uppercase label with History icon, ul max-h-80 overflow-y-auto divide-y with thin emerald webkit scrollbar + [scrollbar-width:thin]; rows are full-width min-h-11 buttons (≥44px touch target) with priority icon (AlertTriangle amber / BadgeCheck emerald), truncate title (PENTING = font-semibold text-amber-800), sm+ date, ChevronRight.
- Dialog: shadcn sm:max-w-lg p-0 overflow-hidden — header with PriorityBadge + CalendarDays formatDate(createdAt), DialogTitle emerald-950, DialogDescription "Pengumuman resmi dari TPQ Darul Jinan."; content max-h-[60vh] overflow-y-auto whitespace-pre-line thin scrollbar; emerald-50/60 footer bar with MessageCircle + "Dibagikan melalui Portal SIMADJI — TPQ Darul Jinan" (WhatsApp-share style line per spec).
- States: loading = skeleton grid of 3 h-56 rounded-2xl cards + h-44 list skeleton; empty (zero announcements) = centered dashed-border white card with Megaphone icon + "Belum ada pengumuman." (section still renders so nav anchor works); error = red AlertCircle card + "Coba Lagi".
- Edited src/components/public/portal.tsx (3 minimal edits only): added import './announcements-section' after NewsSection import, added { id: 'pengumuman', label: 'Pengumuman' } to NAV_ITEMS after 'berita', rendered <AnnouncementsSection /> between <NewsSection /> and <TestimonialsSection />. Nothing else touched.
- Verified seeded DB has 3 announcements (1 PENTING "Libur Idul Adha", 2 NORMAL) so demo shows 3 feature cards; Riwayat list appears automatically when >3.
- QA: `bun run lint` → clean (no output); `bunx tsc --noEmit 2>&1 | grep -E "announcements-section|portal"` → empty (grep exit 1); full tsc shows only the 4 pre-existing errors in examples/ + skills/ scaffolds; dev server NOT run.

Stage Summary:
- Files: NEW src/components/public/announcements-section.tsx (sole export: `AnnouncementsSection()`, no props — self-fetching); EDIT src/components/public/portal.tsx (import + NAV_ITEMS 'pengumuman' + render between berita and testimoni). No other files touched, no new deps.
- New nav anchor: #pengumuman (nav item between Berita and PPDB); section bg alternation: berita bg-white → pengumuman bg-stone-50 → testimoni bg-stone-50 (testimoni was already stone-50 and out of scope to change).
- Decisions: reused news-section's article-as-button + Enter/Space keyboard pattern and ticker's priority badge language ("Penting" solid amber / "Normal" emerald outline) for visual continuity; dates use formatShortDate on cards/rows and formatDate in dialog (both id-ID via api-client); content preview whitespace-normalized before line-clamp-3 so multi-paragraph announcements clamp cleanly; count chip uses singular-form label "{n} Pengumuman" per spec example; all interactive rows/cards ≥44px with aria-labels and emerald focus-visible rings; emerald/amber/stone palette only (no indigo/blue).

---
Task ID: 11-c
Agent: hafalan-export (frontend-styling-expert)
Task: Add CSV export of hafalan records to admin/guru Hafalan section

Work Log:
- Read worklog.md (conventions + round-9 CSV pattern), hafalan-admin.tsx fully (365 lines), round-9 exportCsv in students-admin.tsx + payments-admin.tsx, shared helpers csvCell/csvDate/csvFileStamp/downloadCsv in admin/overview.tsx (BOM \uFEFF, `;` separator Excel-id friendly, quotes escaped by doubling, dd/mm/yyyy dates), types.ts Hafalan/Student shapes, prisma schema (ayatRange required, grade Int?, teacherNote String?).
- Edited src/components/admin/hafalan-admin.tsx ONLY: added `Download` lucide import + `import { csvDate, csvFileStamp, downloadCsv } from './overview'` (same shared helpers as round 9, no duplication); added module-level TYPE_LABELS map (TAHFIDZ→Tahfidz, TAHSHIN→Tahshin, MURAJAAH→Murajaah, fallback raw) for the Jenis column; added `exporting` state; added async exportCsv mirroring round-9 exactly (setExporting guard, 200ms delay, downloadCsv, finally reset) — filename `data-hafalan-${csvFileStamp()}.csv`, headers [Tanggal, Santri, NIS, Kelas, Surat, Ayat, Jenis, Nilai, Catatan Ustadz], rows mapped from `hafalans` (the same state the Riwayat table renders), grade null → '', teacherNote/null → '' (csvCell quotes+escapes newlines/semicolons), toast `Ekspor CSV berhasil` / `${hafalans.length} data hafalan tersimpan di ${filename}.`; Riwayat card header now wraps buttons in `flex items-center gap-2` with new `<Button variant="outline" size="sm" disabled={loading || exporting || hafalans.length === 0}>` Ekspor CSV (Loader2/Download swap while exporting) inline before existing ghost "Muat Ulang" (Button primitive ships transition-colors + focus-visible ring).
- Note: hafalan-admin.tsx has NO history filter (form's Santri/Jenis selects are record-creation inputs, not list filters; GET /api/hafalan takes no query filters per round-1 contract) — "currently filtered rows" therefore = the full list state `hafalans` the table displays; export reads that same state so a future filter only needs to swap the mapped array.

Stage Summary:
- File changed (only): src/components/admin/hafalan-admin.tsx (+~40 lines: 2 imports, TYPE_LABELS const, exporting state, exportCsv fn, toolbar button group). No other files touched, no new deps, dev server not run.
- QA: `bun run lint` → clean (no output); `bunx tsc --noEmit | grep hafalan-admin` → empty (zero errors). Zero behavior changes to existing flows (form submit, delete, refresh untouched).
- Decisions: filename prefix `data-hafalan-` per task + `csvFileStamp()` (YYYY-MM-DD) per round-9 convention — the "20260910" example in the task brief was treated as illustrative; mirroring the shared helper exactly took precedence (payments uses `keuangan-darul-jinan-…`, students `santri-darul-jinan-…`). Button = variant outline size sm (matches neighbor "Muat Ulang" size and round-9 export buttons' outline variant); disabled when loading/exporting/no rows (round-9 pattern) instead of hidden.

---
Task ID: 11-e
Agent: batch-invoices (full-stack-developer)
Task: Admin mass-invoice generator ("Tagihan Massal") — batch SPP API + payments-admin dialog

Work Log:
- Read worklog.md (API conventions + prior rounds), src/lib/api.ts (db/ok/bad/sendWhatsApp), payments/route.ts (single-invoice POST: invoiceNo `INV-2025-${1000+count+1}`, WA "Tagihan baru" message style), prisma/schema.prisma (Payment/Student/Class), payments-admin.tsx (581 lines, fully), api-client.ts, types.ts; grep-verified NO OPTIONS handlers exist anywhere under src/app/api → skipped OPTIONS in new route (matches project convention); verified lucide 0.525 exports Layers/Users/Info.
- Created src/app/api/payments/batch/route.ts: POST-only route handler with doc-comment contract header. Validation order: missing classId/title/amount → 400 'Kelas, keterangan, dan nominal wajib'; Number(amount) not integer or <=0 → 400 'Nominal harus angka positif'; unknown class → 404 'Kelas tidak ditemukan'; zero AKTIF students in class → 400 'Tidak ada santri AKTIF di kelas this'. Duplicate guard: fetch PENDING/SUCCESS payments of those students (select studentId+title), normalized key `${studentId}::${title.trim().toLowerCase()}` in a Set (Prisma mode:'insensitive' unsupported on SQLite → JS-side compare, case-insensitive + trim). Sequential invoice numbering inside loop from db.payment.count() snapshot: `INV-2025-${1000+currentCount+i}` (i starts at 1) — no mid-loop recount, matches existing pattern. Each created invoice triggers sendWhatsApp to parent (exact single-POST message style incl. invoiceNo + "Bayar via Portal Wali. — TPQ Darul Jinan"). Response { created, skipped, invoices: Payment[] } with student {id,fullName,nis,parent{id,name,phone}}. try/catch → 400 'Gagal membuat tagihan massal'. month accepted in contract as informational (title carries the month).
- Edited src/components/admin/payments-admin.tsx (single file, additive edits only): (1) icons Layers/Users/Info + ClassRoom type import; (2) module-level ID_MONTHS (Indonesian months) + BatchInvoiceResult interface; (3) 7 new states (batchOpen/batchClassId/batchTitle/batchAmount/batchSubmitting/classes/classesLoading); (4) loadClasses() (apiGet /api/classes, toast on error) + openBatchDialog() (resets defaults: title "Iuran SPP {NamaBulan} {Tahun}" from ID_MONTHS[now], amount '75000', fetches classes once) + submitBatchInvoices() (client validation → apiSend POST /api/payments/batch → toast "${created} tagihan dibuat, ${skipped} dilewati (sudah ada)" + existing load() refetch + close; catch → destructive toast with API error); (5) memos batchAktifCount (students.filter classId===batchClassId && status==='AKTIF' — reuses already-loaded students data, no extra fetch) + batchEstTotal; (6) toolbar button "Tagihan Massal" (Layers icon, emerald outline: border-emerald-300/text-emerald-700/hover:bg-emerald-50, consistent with existing amber outline reminder button) placed between "Buat Tagihan" and "Ingatkan Semua"; (7) shadcn Dialog with: Kelas Select (h-11, 'none' sentinel, items show name + studentCount, max-h-64 scroll), Keterangan Input (h-11, id=batch-title), Nominal number input (h-11, step=1, aria-describedby → live hint "Akan tertulis sebagai Rp 75.000 per santri."), emerald preview strip (Users icon, role="status": "Tagihan akan dibuat untuk X santri AKTIF · est. total Rp Y"), amber info Alert "Santri yang sudah memiliki tagihan dengan keterangan sama akan dilewati otomatis.", footer h-11 Batal/Terbitkan Semua (emerald, Loader2 while submitting, disabled until kelas chosen). All labels wired via htmlFor, shadcn focus-visible rings retained, Escape-safe via shadcn Dialog default.
- QA evidence (dev server was found down — nothing listening on :3000, no next/bun dev process; restored it with the same `bun run dev` the system manages, then ran curl suite): T1 {} → 400 {"error":"Kelas, keterangan, dan nominal wajib"}; T2 amount:-5 → 400 {"error":"Nominal harus angka positif"}; T3 classId:"clx-does-not-exist" → 404 {"error":"Kelas tidak ditemukan"}; T4 Iqra 1 (3 AKTIF santri) title "Uji Massal QA" amount 75000 → 200 {"created":3,"skipped":0,invoices:[INV-2025-1009 Muhammad Rizky Ramadhan, INV-2025-1010 Salman Alfarizi Pratama, INV-2025-1011 Zahra Nur Aini], status PENDING, method null}; T5 identical re-run → 200 {"created":0,"skipped":3,"invoices":[]}; T5b case-insensitive variant "  uji massal qa  " → 200 {"created":0,"skipped":3}. Verified 3 WA Notification rows created (one per invoice, phone 081300000001/0815000000099.../081300000004). Cleanup via bun Prisma script: deleteMany payments title='Uji Massal QA' (3) + containing 'Uji Massal QA' notifications (3) → payment count back to baseline 8, demo data clean. dev.log shows all 6 batch requests with expected codes (400/400/404/200/200/200), no runtime errors. `bun run lint` clean; `bunx tsc --noEmit | grep -E "payments|batch"` empty.

Stage Summary:
- New API: POST /api/payments/batch {classId, title, amount, month?} → 200 {created:number, skipped:number, invoices:Payment[] (student+parent include)} | 400/404 {error}. Duplicate-skip = PENDING/SUCCESS payment with same normalized (trim+lowercase) title for that student; sequential invoiceNo INV-2025-{1000+count+i}; WA per created invoice. No OPTIONS handler (project has none).
- UI contract: in PaymentsAdmin toolbar, emerald-outline "Tagihan Massal" button (Layers icon) opens Dialog → POST /api/payments/batch → success toast "X tagihan dibuat, Y dilewati (sudah ada)" + list refetch via existing load(); error toast surfaces API message. Preview uses already-loaded /api/students data; classes fetched once via /api/classes on first open.
- Files: NEW src/app/api/payments/batch/route.ts; EDIT src/components/admin/payments-admin.tsx (+~160 lines, no removals, no other files touched). QA: lint clean, tsc clean (payments/batch scope), 6/6 curl scenarios verified, duplicate guard incl. case/trim variants, demo data restored (8 payments, 0 test rows).

---
Task ID: 11-b
Agent: weekly-schedule (frontend-styling-expert)
Task: Guru weekly teaching timetable ("Jadwal Mengajar Mingguan") parsed from class.schedule strings.

Work Log:
- Read worklog.md (round-10 "next-step candidates" listed this feature), guru-overview.tsx (card/KPI tile patterns, ownClasses filtering, two-column grid placement point), types.ts (ClassRoom: level/schedule/room/teacherId/studentCount), eslint.config.mjs + page.tsx (set-state-in-effect precedent → timer-deferred setState), classes-admin.tsx level list; verified live DB schedules via bun+PrismaClient: exactly "Senin, Rabu — 15.30-17.00" (Iqra 1/IQRA/Ruang Serambi), "Selasa, Kamis — 15.30-17.00" (Iqra 4), "Senin-Kamis — 16.00-18.00" (Tahfidz A/TAHFIDZ/Musholla Utama), "Senin, Rabu, Jumat — 15.30-17.30" (Al-Qur'an A/AL_QURAN/Ruang Utama); verified lucide 0.525 exports CalendarDays/MapPin/AlertTriangle.
- Created src/components/admin/weekly-schedule.tsx ('use client', named exports WeeklySchedule + parseWeeklySchedule + type ParsedSchedule). Parser: normalize em/en/minus dashes → "-", locate the unique "HH.MM-HH.MM" (dots or colons) time range first, dayPart = prefix with leading/trailing dash runs stripped (fixes "Senin-Kamis - " residue from the em-dash separator); tokens split on [,;] — each either single day (normalizeDayName: lowercase, strip apostrophes incl. U+2019 + non-letters, alias map for senen/slasa/rebo/kemis/jum'at→jumat/jumad/jumuat/sabt/ahad/minggu) or inclusive range "Senin-Kamis" expanded with %7 wrap (Sabtu-Senin → Sabtu,Ahad,Senin); any unrecognized token → null (caller surfaces it, never drops). Component: Card rounded-2xl border-stone-200 shadow-sm, CardHeader with CalendarDays in size-9 rounded-lg bg-emerald-100 text-emerald-700 tile, title "Jadwal Mengajar Mingguan", description "Keterampilan jadwal kelas yang Anda ampu"; day grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 (lg:grid-cols-7 when Ahad column appears) role="list" + role="listitem" aria-label "Senin — 2 kelas"; day header text-xs font-bold uppercase tracking-wide + emerald underline bar (h-0.5 w-7 bg-emerald-500); chips rounded-xl border-stone-200 border-l-4 (IQRA border-l-emerald-600 / TAHFIDZ border-l-amber-500 / AL_QURAN border-l-teal-600, unknown → stone-400) bg-stone-50 hover:bg-emerald-50 p-2.5 with name text-xs font-semibold, time font-mono tabular-nums text-[11px] "15.30-17.00", room MapPin size-3 text-[11px], title tooltip "Iqra 1 · 15.30-17.00 · Ruang Serambi"; chips sorted by start minutes then name; empty day dashed border-stone-200 h-16 with centered "—" text-stone-300; today highlight effect-gated (useState<DayKey|null>(null) + useEffect setTimeout(0) setState — SSR/first paint fully neutral, mirrors page.tsx hydration pattern) → column bg-emerald-50/60 ring-1 ring-emerald-200 + amber-100/amber-800 "Hari ini" pill; unparsed classes render in amber-50/amber-200 AlertTriangle "Format jadwal tidak dikenali" list (name + raw mono schedule); legend row border-t stone-100 with level dots (only levels present, canonical order) + emerald-100 "N sesi/minggu" chip (N = total class-day sessions); returns null when classes empty.
- Edited src/components/admin/guru-overview.tsx minimally (2 edits, +4 lines): added `import { WeeklySchedule } from './weekly-schedule'`; rendered `<WeeklySchedule classes={ownClasses} />` after the lg:grid-cols-2 grid (Sesi Aktif + Setoran Hafalan) as full-width block with comment. No other changes.
- QA: parser sanity via `bun -e` importing the real module — all 4 DB schedules parse correctly ("Senin, Rabu — 15.30-17.00" → Senin,Rabu 15.30-17.00; "Selasa, Kamis — 15.30-17.00" → Selasa,Kamis; "Senin-Kamis — 16.00-18.00" → Senin,Selasa,Rabu,Kamis 16.00-18.00; "Senin, Rabu, Jumat — 15.30-17.30" → 3 days) plus robustness cases (en-dash, Jum'at, colon times, no-space "Senin-Kamis-16.00-18.00", Minggu→Ahad, wrap Sabtu-Senin; "garbled text"/"Senin, Bulan"/""/null → null). One parser bug found & fixed during QA (trailing em-dash residue broke range form → dayPart dash-trim). `bun run lint` → clean (eslint . zero output); `bunx tsc --noEmit | grep weekly-schedule|guru-overview` → empty; only the 4 pre-existing errors in examples/+skills/ remain. Dev server NOT run; no other files touched; no new packages.

Stage Summary:
- Files: NEW src/components/admin/weekly-schedule.tsx (exports WeeklySchedule({ classes }: { classes: ClassRoom[] }), plus parseWeeklySchedule(raw): ParsedSchedule|null and type ParsedSchedule for testability); EDIT src/components/admin/guru-overview.tsx (+1 import, +3 lines render — WeeklySchedule mounts only in the has-assignment branch, after the Sesi Aktif/Setoran Hafalan grid).
- Decisions: pure client component fed with already-loaded ownClasses (no fetches); DayKey 0=Senin…6=Ahad with getDay()+6)%7 conversion; Ahad column auto-appears only when used (grid switches to lg:grid-cols-7); today highlight effect-gated via timer callback to satisfy react-hooks 7 set-state-in-effect and hydration safety (SSR HTML = neutral columns); TAHFIDZ chip color = amber-500 per task spec (classes-admin badge uses violet — chip accent follows the timetable spec, badge untouched); level border classes resolved through a LEVEL_STYLES map with stone-400 fallback for future levels; sessions/minggu counts class-day pairs (e.g. Tahfidz A Senin-Kamis = 4).
- QA results: bun run lint clean; tsc clean for both files; parser verified live against all 4 seeded schedules + 8 robustness cases; no dev server, no other files edited.

---
Task ID: 11 (cron review round)
Agent: main (Z.ai Code) + 4 subagents (11-a/11-b/11-c/11-e)
Task: Round-11 review — QA sweep, public announcements section, guru weekly timetable, hafalan CSV export, mass-invoice generator, sidebar role-label fix

Work Log:
- Reviewed worklog (round 10 complete, candidates listed). QA sweep: lint clean, tsc clean (src only), dev.log clean, browser E2E (public portal, guru login, parent portal Juz-30 map + trend + notification badge, logout; zero console errors). App STABLE → proceeded to features.
- [ain BUGFIX] dashboard.tsx sidebar brand hardcoded "TPQ Darul Jinan · Admin" for every role → now role-aware: "TPQ Darul Jinan · {Admin|Guru|Wali Santri}" via existing roleLabel().
- [11-a subagent] NEW src/components/public/announcements-section.tsx: public "Pengumuman" section (id=pengumuman, nav item added after Berita, wired between News + Testimonials). Section header w/ amber eyebrow + count badge; 3 most-recent as hover-lift feature cards (PENTING = amber left border + AlertTriangle badge, NORMAL = emerald outline + BadgeCheck; CalendarDays id-ID date; line-clamp-3 preview); rest in max-h-80 thin-scrollbar "Riwayat" list; detail Dialog (priority badge, date, scrollable content, WA-style footer); skeleton/empty/error-retry states; a11y (role=button cards w/ keyboard handlers, focus-visible rings, ≥44px targets).
- [11-b subagent] NEW src/components/admin/weekly-schedule.tsx: "Jadwal Mengajar Mingguan" timetable in GuruOverview (full-width, after Sesi Aktif/Setoran grid). Robust schedule-string parser (comma days, Senin-Kamis inclusive ranges w/ %7 wrap, em/en-dash + colon tolerance, Jum'at/Minggu aliases, unknown → amber fallback list; parseWeeklySchedule exported + 12-case tested); 6/7-day grid (Senin-first, Ahad auto-appears), level-accented chips (IQRA emerald/TAHFIDZ amber/AL_QURAN teal left borders, mono time, MapPin room, tooltips), effect-gated today highlight ("Hari ini" amber pill + emerald ring — hydration-safe), legend + "N sesi/minggu" chip, role=list/listitem semantics. guru-overview.tsx +4 lines wiring.
- [11-c subagent] hafalan-admin.tsx: "Ekspor CSV" toolbar button reusing round-9 shared helpers (overview.tsx csvDate/csvFileStamp/downloadCsv — BOM + ';' + sanitizer). Exports Riwayat rows: Tanggal/Santri/NIS/Kelas/Surat/Ayat/Jenis(label)/Nilai/Catatan; filename data-hafalan-<stamp>.csv; toast "Ekspor CSV berhasil — 19 data hafalan tersimpan…"; disabled-when-empty/loading, Loader2 swap.
- [11-e subagent] NEW src/app/api/payments/batch/route.ts: POST {classId,title,amount,month?} → {created,skipped,invoices[]}; validation chain (400 missing/non-positive, 404 class, 400 no-AKTIF-students), JS-side duplicate guard (studentId::trim+lowercase title vs PENDING/SUCCESS — SQLite has no insensitive filter), sequential invoiceNo INV-2025-{1000+count+i}, WA notify per created invoice. payments-admin.tsx: "Tagihan Massal" emerald-outline toolbar button + Dialog (Kelas select w/ studentCount, Keterangan prefilled "Iuran SPP {Bulan} {Tahun}", Nominal 75000 + Rp hint, live preview "X santri AKTIF · est. total Rp Y", amber duplicate-skip Alert, h-11 controls, Loader2 submit) → success toast "X tagihan dibuat, Y dilewati (sudah ada)" + list refetch. curl suite 6/6 pass (400×2/404/200/200 dup-skip/200 case-insensitive dup-skip); test rows cleaned; WA rows verified.
- E2E verified in browser: sidebar shows "· Guru"/"· Admin" per role; Pengumuman section + nav + detail dialog (PENTING badge, content); guru timetable (Iqra 4 chips on Selasa/Kamis, Rabu "Hari ini" highlight, legend "2 sesi/minggu", time text correct); admin Hafalan Ekspor CSV toast "19 data hafalan tersimpan"; Keuangan Tagihan Massal full flow (Iqra 1 → preview "3 santri AKTIF · est. total Rp 225.000" → submit → toast "3 tagihan dibuat, 0 dilewati" + INV-1009/1010/1011 PENDING rows appear + Tunggakan KPI updates); mobile 390px Pengumuman stacks cleanly w/ amber PENTING border; zero console/page errors; lint + tsc clean after integration.
- Note: 3 browser-test SPP invoices (INV-2025-1009..1011, Iqra 1, "Iuran SPP September 2026", PENDING) intentionally KEPT as realistic demo data (exercises reminder flow + PENDING KPIs).

Stage Summary:
- Round 11 shipped: public Pengumuman section (+nav), guru Jadwal Mengajar Mingguan timetable, hafalan CSV export, POST /api/payments/batch + Tagihan Massal dialog, sidebar role-label fix. All features browser-verified; lint+tsc clean; zero console errors.
- New API: POST /api/payments/batch {classId, title, amount, month?} → {created, skipped, invoices[]} | {error} (400/404).
- Demo data state: 8→11 payments (3 new PENDING SPP Sept for Iqra 1), announcements 3 (1 PENTING), hafalan 19.
- Known notes: batch duplicate-guard is per-title (case/trim-insensitive), not per-month — intended; dev.log may contain one 404 probe from subagent QA (expected).
- Next-step candidates (priority order): (1) parent portal "Konfirmasi Terima" for announcements read-state or light per-user read tracking, (2) admin PPDB → auto-create User+Student on accept (currently registration accept creates student only — verify), (3) session attendance recap per student in parent portal (percentage chip already exists; add monthly breakdown), (4) dark-mode audit of new sections, (5) hafalan target planning (set target surah per student, show % in guru timetable card or santri detail).

---
Task ID: 12-b
Agent: attendance-recap (frontend-styling-expert)
Task: Parent portal "Rekap Kehadiran Bulanan" — monthly stacked-bar attendance recap component (standalone, unwired).

Work Log:
- Read worklog.md in full (round 9/11 styling language + round-11 next-step candidate #3 = this feature); inspected child-panel.tsx (ATTENDANCE_CHIP/ATTENDANCE_BADGE/RAPOR_ATT_DOT status maps, "Riwayat Kehadiran — 30 pertemuan terakhir" card, ~line 766), hafalan-chart.tsx card language, ui/card.tsx primitives, weekly-schedule.tsx size-9 emerald icon-tile header pattern; verified lucide 0.525 exports CalendarRange + BarChart3 via bun probe; confirmed @types/react ^19 → return type uses `import type { JSX } from 'react'` (global JSX namespace removed in React 19 types).
- Created src/components/parent/attendance-recap.tsx ('use client') ONLY — zero existing files touched. Exports: AttendanceRecap({attendances}) + AttendancePoint/MonthAgg types + aggregateMonthly(attendances) helper.
- Rendering: Card rounded-2xl border-stone-200 bg-white shadow-sm; CardHeader with CalendarRange in size-9 rounded-lg bg-emerald-100 text-emerald-700 tile + title "Rekap Kehadiran Bulanan" + description "Ringkasan per bulan dari 30 pertemuan terakhir."; per-month rows (w-20 text-xs font-semibold label, flex h-3.5 rounded-full overflow-hidden bg-stone-100 stacked bar w/ flexGrow∝count + min-w-[2px] segments, right column rate chip + "hadir/total" text-[11px] text-stone-500); row hover bg-stone-50 rounded-lg transition + title tooltip (long month + total + rate); bar title tooltip "HADIR n · IZIN n · SAKIT n · ALPA n"; a11y role="img" + aria-label "September 2026: 12 hadir, 1 izin, 1 sakit, 0 alpa — 86% kehadiran"; legend (size-2 dots, Hadir/Izin/Sakit/Alpa labels, only statuses present); footer note "Persentase = hadir ÷ total pertemuan tercatat bulan tersebut."; returns null when attendances empty (also when all dates unparseable).
- Aggregation via useMemo → aggregateMonthly: monthKeyOf prefers the ISO string "YYYY-MM" prefix (deterministic + timezone-safe, full-ISO timestamps and date-only both handled), invalid dates skipped; labels via new Date(y, m-1, 1, 12).toLocaleDateString('id-ID', {month:'short'|'long', year:'numeric'}); sorted newest-first, capped at 6 most recent months with data; rate = Math.round(hadir/total*100); chip thresholds ≥85 emerald-100/emerald-800, ≥70 amber-100/amber-800, else red-100/red-800.
- STATUS COLOR MAPPING (reused EXACTLY from child-panel.tsx RAPOR_ATT_DOT, overriding the brief's bg-amber-400/stone-400/teal suggestions per the brief's own "reuse EXACTLY those hues" rule): HADIR=bg-emerald-500, IZIN=bg-amber-500, SAKIT=bg-orange-500, ALPA=bg-red-500 (teal rejected — SAKIT is orange in the parent portal; orange already established there, no indigo/blue introduced).
- aggregateMonthly sanity via `bun -e` importing the real module: T0 empty→[]; T1 3 synthetic months (unsorted) → newest-first [{Okt 2026, hadir 1, total 1, rate 100 (single-record edge)}, {Sep 2026, hadir 12 izin 1 sakit 1 alpa 1, total 15, rate 80 (mixed)}, {Agu 2026, hadir 14, total 14, rate 100 (all-HADIR, full-ISO timestamps)}]; T2 8 months → capped to 6 newest ["2026-08".."2026-03"] w/ correct id-ID labels (Agu/Jul/Jun/Mei/Apr/Mar); T3 invalid-date records skipped gracefully. All assertions passed.
- QA: `bun run lint` → clean (eslint . zero output, exit 0); `bunx tsc --noEmit 2>&1 | grep attendance-recap` → empty (exit 1, zero errors in new file). Dev server NOT run; no existing file edited; no new deps.

Stage Summary:
- File created (sole file touched): src/components/parent/attendance-recap.tsx. Export contract for wiring: `AttendanceRecap({ attendances }: { attendances: AttendancePoint[] }): JSX.Element | null` + `interface AttendancePoint { id: string; status: 'HADIR'|'IZIN'|'SAKIT'|'ALPA'; date: string; topic: string|null; className: string }` + `aggregateMonthly(attendances): MonthAgg[]` (+ MonthAgg/AttendanceStatus types). Pure presentational client component — no fetch, no toast, no effects.
- Suggested wiring (main agent): in child-panel.tsx Riwayat Kehadiran card (item b), render `{child.attendances.length > 0 && <AttendanceRecap attendances={child.attendances} />}` — either inside that CardContent above the scroll list or as a sibling card in the lg:grid-cols-2 grid; child.attendances (ParentStudent.attendances) matches AttendancePoint shape (status/date/topic via session, className via class name) — topic/className fields are accepted but only status+date are consumed; empty data renders null so no guard is strictly required.
- Decisions: segment hues copied verbatim from child-panel RAPOR_ATT_DOT (emerald-500/amber-500/orange-500/red-500) for cross-card consistency; legend labels "Hadir/Izin/Sakit/Alpa" per child-panel's status-label convention; month key from date-string prefix to avoid UTC-vs-local drift and hydration mismatch (server/client ICU identical since input depends only on data); single-record months render full-width segment via flexBasis 0 + flexGrow 1 + min-w-[2px]; unknown/invalid statuses count toward total but leave a stone-100 gap (honest representation of "tercatat").
- QA results: aggregateMonthly 4/4 bun-e scenarios pass (empty / all-HADIR / mixed 12-1-1-1 / single-record / cap-6 / invalid-date skip); lint clean; tsc clean for attendance-recap; zero files besides the new component modified.

---
Task ID: 12-a
Agent: hafalan-target (full-stack-developer)
Task: Target Hafalan planning — ustadz sets a Juz-30 target surah per santri; parents see progress toward it in the child panel.

Work Log:
- Read worklog.md (head 120 + tail 150), prisma/schema.prisma, students + portal/parent routes, types.ts, students-admin.tsx (fully), hafalan-chart.tsx, child-panel.tsx wiring point; verified lucide 0.525 exports Target/CheckCircle2 and dev server live on :3000.
- Schema: Student.hafalanTarget String? added after photoUrl (comment: target surah Juz 30). `bun run db:push` OK (14ms, generate ran). Temp verify script scripts-tmp/verify-hafalan-target.ts read 3 students with hafalanTarget:null then DELETED (dir removed).
- API: students/route.ts POST data + `hafalanTarget: b.hafalanTarget || null`; PUT `...(b.hafalanTarget !== undefined && { hafalanTarget: b.hafalanTarget || null })` (explicit null clears; '' also maps to null). portal/parent/route.ts students.map now returns `hafalanTarget: s.hafalanTarget,`. GET /api/students returns it automatically (scalar).
- Types: Student interface + ParentPortalData.students[] (child-summary w/ attendances/hafalans/payments/billing) both gained `hafalanTarget: string | null` (non-optional scalars, matches API reality after include/map).
- students-admin.tsx: JUZ30_TARGET_OPTIONS const (exact 38 names per task: Al-Fatihah + 37, incl. An-Nazi'at/Al-Kausar/Al-Lahab spelling); StudentFormState.hafalanTarget ('none' sentinel); Edit dialog prefills s.hafalanTarget ?? 'none'; payload sends `form.hafalanTarget === 'none' ? null : form.hafalanTarget` (POST & PUT share payload); dialog field "Target Hafalan" = shadcn Select (id/aria-label, h-11 trigger ≥44px, helper caption "Surah Juz 30 yang ditargetkan — progresnya tampil di Portal Wali.", Radix native scroll for 38 items); table Aktivitas cell gains amber chip shown only when set: Badge outline border-amber-200 bg-amber-100 text-amber-800 + Target icon size-2.5 + "Target: {name}" + transition-colors hover (matches neighbor count-badge style); exportCsv appends 'Target Hafalan' column (s.hafalanTarget ?? '').
- hafalan-chart.tsx: HafalanProgress({ hafalans, target?: string | null }) — backward compatible. Added NORM_ALIASES (alkausar→alkautsar for the target-Select spelling, allahab→almasad for Al-Lahab, aththariq→attariq for the hafalan-admin datalist variant) + resolveNorm(); setoranCount keys now resolveNorm (strictly additive — all existing matches unchanged, verified DB surahNames still match). Target logic: targetInfo = findIndex in JUZ30_SURAHS via resolveNorm (null → strip skipped entirely, e.g. non-Juz-30 target); reached = distinct setoran among list entries slice(0, position) (position = idx+1, before-or-equal); amber strip first child of "Peta Hafalan Juz 30" CardContent: rounded-xl border-amber-200 bg-amber-50, Target tile + "Target: {name}" + emerald-100 CheckCircle2 "Tercapai" chip when the target surah itself has setoran, right-aligned "{reached}/{position} surah", thin Progress h-1.5 bg-amber-200 [&>div]:bg-amber-500 + "{pct}% progres menuju target", role="status" + aria-labels; tile map target chip gets ring-2 ring-amber-400 on top of existing variant + tiny amber Target icon overlay + "· TARGET hafalan" in title; legend gains "Target hafalan" ring swatch when target set. Hydration-safe: everything derived from props via useMemo (no Date/random).
- child-panel.tsx: one-line edit — `<HafalanProgress hafalans={child.hafalans} target={child.hafalanTarget} />`.
- Dev-server note (no manual start/stop): after db:push the RUNNING next-server kept the pre-change Prisma Client (old DMMF → PUT 400 "Gagal memperbarui santri", GET rows lacked the key). Verified a temp db.ts global-bypass did NOT help (module graph stale, reverted). Cleanest non-invasive fix: touched next.config.ts (added then removed a comment line) — Next 16 dev performed its own managed restart both times ("Found a change in next.config.ts. Restarting the server to apply the changes... ✓ Ready"), same process tree `bun run dev` intact; new client loaded; .env also byte-restored after probing (Next 16 did not restart on .env touch). Server healthy (GET / 200, /api/stats 200) before/after.
- curl evidence (dev.log lines 814/834/840 = PUT 200×3): T1 PUT {id: cmtufisd1000sq12wyuomvtbq (Muhammad Rizky Ramadhan), hafalanTarget:"An-Naba"} → {"fullName":"Muhammad Rizky Ramadhan","nis":"DJ-2024-001","hafalanTarget":"An-Naba"}; T2 GET /api/portal/parent?userId=cmtufiscv000lq12wlmr3cq2v (budi) → students:[{Muhammad Rizky Ramadhan, hafalanTarget:"An-Naba", hafalanCount:5},{Alika Syakira, hafalanTarget:null}]; T3 PUT hafalanTarget:null → hafalanTarget:null; T4 PUT hafalanTarget:"" → null; T5 GET /api/students?parentId=<budi> → both children hafalanTarget:null; T6 GET /api/portal/parent → both null (baseline restored). Pre-fix 400s (dev.log 560/636/678/679) document the stale-client diagnosis.
- bun -e simulation against Rizky's real 5 setoran (Al-Fatihah×2, An-Nas, Al-Falaq, Al-Ikhlas) with target An-Naba → position 2, reached 1/2 = 50%, targetDone false (strip shows 1/2 + 50%, no Tercapai chip) — matches component logic.

Stage Summary:
- Contract: Student.hafalanTarget String? (surah NAME string, e.g. "An-Naba", null = no target). POST /api/students accepts hafalanTarget (default null); PUT accepts hafalanTarget (undefined = keep, null/'' = clear); GET /api/students + GET /api/portal/parent?userId= return hafalanTarget on every student/child. ParentPortalData.students[] + Student types now include hafalanTarget: string | null.
- UI contract: StudentsAdmin form field value uses 'none' sentinel → null; HafalanProgress accepts optional target?: string | null (all existing call sites unaffected). Admin Select stores the task's exact 38-name spelling (Al-Kausar/Al-Lahab/An-Nazi'at); chart resolves via NORM_ALIASES (alkausar/allahab/aththariq) so it also matches the chart's canonical list AND existing hafalan-admin datalist variants — resolveNorm is now used for setoran matching too (pure superset of previous behavior).
- Files changed (7): prisma/schema.prisma, src/app/api/students/route.ts, src/app/api/portal/parent/route.ts, src/lib/types.ts, src/components/admin/students-admin.tsx, src/components/parent/hafalan-chart.tsx, src/components/parent/child-panel.tsx (1 line). Transient & fully reverted: src/lib/db.ts, next.config.ts, .env (see note above). NOT touched: attendance-recap.tsx and everything else.
- QA: db:push OK; lint clean; bunx tsc --noEmit (minus examples/skills) empty; 6/6 curl scenarios pass incl. set→parent-visible→reset-null; DB baseline restored (all targets null); dev server running & healthy on :3000; chart math simulated against real data (An-Naba → 1/2 = 50%).
- Decisions: target chip lives in the Aktivitas badge group (first position, amber-100 per task); CSV column appended last ('Target Hafalan') to avoid shifting existing column indices; HafalanProgress strip renders only when target resolves inside Juz 30 (graceful skip otherwise); child-panel keeps its existing `hafalans.length > 0` guard around the analytics block (task asked for the one-line wiring only).

---
Task ID: 12-c
Agent: ppdb-check (full-stack-developer)
Task: Public "Cek Status Pendaftaran" — secure no-login PPDB status lookup (new anti-enumeration check API + ppdb-section UI).

Work Log:
- Read worklog.md (head 120 + tail 150, re-read tail before append — 12-a landed in parallel), api/registrations/route.ts, prisma/schema.prisma (Registration model), ppdb-section.tsx (fully), api-client.ts, src/lib/api.ts (ok/bad), admin overview.tsx statusBadgeClass (PENDING amber / VERIFIKASI teal / DITERIMA emerald / DITOLAK red) and ppdb's own STATUS_STYLES/STATUS_LABELS (same hue mapping); probed lucide 0.525 (FileSearch/Search/Quote/ShieldCheck/RotateCcw all true).
- NEW src/app/api/registrations/check/route.ts: GET ?regNumber&phone, doc-comment contract header. regNumber trim+uppercase → findUnique (stored "PPDB-2025-XXXX" uppercase, so uppercased compare = case-insensitive); phone digits via replace(/\D/g,'') — match requires input ≥5 digits AND last-5 of input === last-5 of stored (full or partial phone accepted). 400 'Nomor pendaftaran dan nomor HP wajib diisi' when either missing; 404 GENERIC 'Pendaftaran tidak ditemukan. Periksa kembali nomor Anda.' when regNumber unknown OR phone mismatch (never reveals which field failed); 200 SAFE FIELDS ONLY {regNumber, childName, parentName, status, reviewNote, createdAt, updatedAt}; try/catch → 400 'Gagal memeriksa status'.
- EDIT src/components/public/ppdb-section.tsx (only this file): (1) section header gains centered emerald-outline pill toggle "Sudah mendaftar? Cek Status" (FileSearch, min-h-11 = 44px, aria-expanded/aria-controls) toggling checkOpen; (2) NEW collapsible card #ppdb-status-check below the header (rounded-2xl border-emerald-200 bg-white p-6/md:p-8 max-w-3xl) with 2-field form — Nomor Pendaftaran (mono uppercase, placeholder "PPDB-2025-0001") + No. HP (type/inputMode tel, maxLength 20, placeholder "5 digit terakhir nomor HP"), labels htmlFor, error-clears-on-type, full-width emerald submit "Periksa Status" (min-h-11, Loader2 "Memeriksa Status…" state); anti-enumeration note text-[11px] text-stone-500 + ShieldCheck "Masukkan nomor pendaftaran dan 5 digit terakhir nomor HP yang digunakan saat mendaftar."; (3) results region wrapped aria-live="polite": role="alert" red inline error (surfacing the API's 400/404 message); success = rounded-xl border card: Calon Santri "An. {childName}" + "Wali: {parentName}", status Badge reusing existing STATUS_STYLES+STATUS_LABELS (admin-mapping: PENDING amber/VERIFIKASI teal/DITERIMA emerald/DITOLAK red), 3-col info grid (mono regNumber, "Diajukan" + "Diperbarui" via formatShortDate id-ID), DITOLAK → prominent red panel (ShieldAlert "Pendaftaran Belum Dapat Kami Terima" + quoted reviewNote), any other status with reviewNote → amber border-l-4 quote block ("Catatan Pengurus", Quote icon), DITERIMA → emerald line "Alhamdulillah! Silakan menunggu informasi kelas dan akun Portal Wali via WhatsApp.", PENDING/VERIFIKASI → mini 3-step timeline (CHECK_STEPS Diajukan→Verifikasi→Keputusan, size-4 dots: done = solid emerald + Check, current = emerald + ring-4 emerald halo, upcoming = stone outline; connecting lines emerald-500 when passed else stone-200; simple flex -mt-2 alignment); footer h-11 outline "Periksa Lagi" (RotateCcw) → resetCheck clears number/phone/result/error; (4) REMOVED the old right-column "Cek Status" widget + its single-field insecure logic (it fetched the FULL /api/registrations list and matched client-side — leaks all parents' phone/email/address to any visitor and is superseded by the phone-verified API; single-field UX cannot satisfy the new phone guard). Right column keeps the "Butuh Bantuan?" card; header container mb-12→mb-8 to seat the toggle (grid spacing preserved via card mb-12). Success dialog / form / other sections untouched.
- QA: baseline GET /api/registrations → PPDB-2025-0004/081500000099 DITERIMA, -0003 DITERIMA, -0002 VERIFIKASI, -0001 PENDING. curl suite: (a) no params & only regNumber → 400 "Nomor pendaftaran dan nomor HP wajib diisi"; (b) PPDB-2025-9999+00099 → 404 generic; (c) right regNumber+12345 → 404 generic (lowercase regNumber variant too); <5-digit phone → 404 generic; (d) PPDB-2025-0004+00099 → 200 safe fields; (d2) "+62 815-0000-0099" (non-digits stripped) → 200; (e) full 081500000099 → 200; (f) lowercase ppdb-2025-0001+00001 → 200 PENDING. Leak grep over all 200 bodies for "phone|email|address|documents|note|id|gender|birthDate" → 0 matches. Browser E2E (agent-browser): toggle opens/closes panel (a11y snapshot shows labeled textboxes + Periksa Status), DITERIMA flow renders badge+dates+WA line, wrong-phone shows role=alert generic message, PENDING flow renders "Menunggu Verifikasi" badge + 3-step timeline; fresh-session page load + full interaction → errors:[] zero console/page errors (transient mid-edit HMR "SearchCheck is not defined" traces were from my own two-batch edit window — final served chunk contains no SearchCheck, verified via curl of the chunk + grep of source). dev.log: every /api/registrations/check line shows expected 400/404/200, no runtime errors; unrelated PUT /api/students 400 lines are 12-a's concurrent QA. bun run lint clean; bunx tsc --noEmit | grep -E "ppdb|registrations" → empty.
- Note: browser E2E was run read-only against existing demo registrations — no data created/deleted; no test rows to clean.

Stage Summary:
- New API: GET /api/registrations/check?regNumber&phone → 200 {regNumber, childName, parentName, status, reviewNote, createdAt, updatedAt} | 400 (missing params / catch-all) | 404 generic (no match). Anti-enumeration: BOTH required, phone matched by last-5 digits ignoring non-digits (min 5 digits), case-insensitive regNumber, generic 404 never reveals failing field, response excludes phone/email/address/documents/note/gender/birthDate/id.
- UI contract: PpdbSection header has a persistent emerald-outline "Sudah mendaftar? Cek Status" toggle (aria-expanded/controls="ppdb-status-check") revealing an inline collapsible card with the 2-field check form, aria-live results region, admin-consistent status badge colors, Indonesian dates (formatShortDate), quoted reviewNote (red-prominent for DITOLAK), DITERIMA next-step line, PENDING/VERIFIKASI 3-step timeline (Diajukan→Verifikasi→Keputusan), and "Periksa Lagi" reset. No Dialog used (inline card matches the section's card rhythm; Escape-safety N/A).
- Files: NEW src/app/api/registrations/check/route.ts; EDIT src/components/public/ppdb-section.tsx only. registrations/route.ts, parent/*, admin/* all untouched.
- QA results: 9/9 curl scenarios pass incl. leakage grep (0 hits); lint clean; tsc clean in scope; browser E2E zero errors; dev server untouched & healthy on :3000.
- Decisions: collapsible inline card over Dialog (section's visual rhythm is inline cards; better mobile flow on the tall section, keeps result visible next to the form); old right-column single-field widget REMOVED as redundant + privacy-leaking (full-list fetch defeats anti-enumeration; it cannot satisfy the new phone requirement); STATUS_STYLES/STATUS_LABELS reused from the file itself (already replicates admin statusBadgeClass mapping) for visual continuity; header button styled rounded-full emerald-outline to echo the section's eyebrow pills; timeline shows only for PENDING/VERIFIKASI per spec (DITERIMA/DITOLAK get dedicated outcome blocks instead).

---
Task ID: 12 (cron review round)
Agent: main (Z.ai Code) + 3 subagents (12-a/12-b/12-c)
Task: Round-12 — QA sweep, hafalan target planning, parent monthly attendance recap, public PPDB status check, privacy fix

Work Log:
- Reviewed worklog (round 11 complete). QA sweep: lint/tsc clean, dev.log clean, browser E2E public portal + console zero errors. Verified round-11 candidate "PPDB accept → auto-create User+Student" was ALREADY implemented in round 1 (registrations PUT DITERIMA path) → picked 3 new features.
- [12-a subagent] Target Hafalan planning: schema Student.hafalanTarget String? (+db:push); students API POST/PUT accept it; portal/parent maps it; types.ts Student + parent child-summary typed; students-admin "Target Hafalan" Select (38 Juz-30 names, 'none' sentinel, edit prefill, h-11, helper caption) + amber "Target: X" chip in table rows + CSV "Target Hafalan" column; HafalanProgress({hafalans, target?}) — amber target strip (Target icon, "reached/position surah", amber Progress %, emerald "Tercapai" chip when target itself has setoran), ring-2 ring-amber-400 + icon overlay on target tile, new legend swatch, NORM_ALIASES (alkausar→alkautsar etc.) bridging spelling variants; child-panel passes child.hafalanTarget. Transient dev-restart (Prisma client reload) documented; historical "Fast Refresh" warnings at dev.log 562/564 are from that, not a current issue.
- [12-b subagent] NEW src/components/parent/attendance-recap.tsx (standalone): exports AttendanceRecap({attendances}) + AttendancePoint + aggregateMonthly (tested: empty/unsorted/8-month cap/invalid dates). Card "Rekap Kehadiran Bulanan" — per-month stacked bars (segment hues verbatim from child-panel status colors: HADIR emerald-500/IZIN amber-500/SAKIT orange-500/ALPA red-500), flexGrow∝count min-w-2px, tooltips "HADIR 12 · IZIN 1…", rate chip (≥85 emerald/≥70 amber/else red) + "n/total", legend only present statuses, 6-month cap, null when empty, hydration-deterministic YYYY-MM keys.
- [ain wiring] child-panel.tsx: import + render <AttendanceRecap attendances={child.attendances} /> between Riwayat Kehadiran and Progres Hafalan cards; types.ts ParentPortalData attendances status narrowed string → 'HADIR'|'IZIN'|'SAKIT'|'ALPA'.
- [12-c subagent] NEW src/app/api/registrations/check/route.ts: GET ?regNumber&phone → safe fields only {regNumber, childName, parentName, status, reviewNote, createdAt, updatedAt}; 400 missing, 404 generic (never reveals which field wrong); regNumber trim+uppercase; phone matched by last-5 digits (formatted phones accepted). ppdb-section.tsx: "Sudah mendaftar? Cek Status" toggle + inline check card (2 fields, mono regNumber input, Loader2 submit, role=alert inline error, aria-live result); success panel = status badge + An. {childName} + Wali + NOMOR/DIAJUKAN/DIPERBARUI + status-specific guidance + DITOLAK red reviewNote panel + 3-step timeline for PENDING/VERIFIKASI + "Periksa Lagi" reset. SECURITY FIX: removed the old right-column "Cek Status" widget that fetched the FULL /api/registrations list client-side (leaked all parents' phone/email/address to any visitor).
- [ain demo data] Set realistic targets via API: Rizky DJ-2024-001 → "An-Naba" (1/2, 50%), Alika DJ-2023-021 → "Abasa" (3/4, 75% + Tercapai) — kept as demo data.
- E2E verified in browser: PPDB check UI (valid last-5 + formatted phone → Diterima panel; wrong phone → role=alert error; Periksa Lagi resets); parent portal Muhammad (recap card Sep 2026 stacked bar 33% red chip + legend; target strip "An-Naba 1/2 50%" + amber-ringed tile + legend "Target hafalan") and Alika ("Abasa" strip 3/4 75% + Tercapai chip + ✓ tiles); admin Santri table shows "Target: Abasa"/"Target: An-Naba" chips, edit dialog prefills Target Hafalan select w/ helper caption; zero console/page errors; lint + tsc clean after integration.

Stage Summary:
- Round 12 shipped: Student.hafalanTarget end-to-end (schema→API→admin form/chip/CSV→parent target strip), parent Rekap Kehadiran Bulanan (stacked monthly bars), public PPDB Cek Status (guarded lookup + result panel), and a privacy fix (removed public widget that leaked registrations data).
- New API: GET /api/registrations/check?regNumber&phone → {regNumber, childName, parentName, status, reviewNote, createdAt, updatedAt} | 400 | 404-generic.
- Contracts: HafalanProgress takes optional target?: string|null; AttendanceRecap({attendances: AttendancePoint[]}) null-safe; PUT/POST /api/students accept hafalanTarget ('none'→null at UI).
- Demo data: Rizky target An-Naba, Alika target Abasa (kept); registrations 4 rows incl. 1 DITERIMA (Salman) for check demos (regNumber PPDB-2025-0004 + last5 00099).
- Next-step candidates (priority order): (1) hafalan-admin form shows student's current target inline ("Target santri: An-Naba") + warn when setoran passes the target, (2) guru-overview "santri mendekati target" nudge card, (3) admin students detail drawer (per-student hafalan/absensi/tagihan tabs), (4) dark-mode audit, (5) WhatsApp log filters (search by phone/status/date + export CSV).

---
Task ID: 13-b
Agent: wa-log-filters (frontend-styling-expert)
Task: WhatsApp log — search, status filter chips with counts, combined result info, filtered CSV export, empty-filter state

Work Log:
- Read worklog.md (head 120 + tail 150, re-read tail before append — unchanged at 465 lines), whatsapp-log.tsx fully (239 lines), verified shared helpers in overview.tsx (csvCell quotes+escapes ; / quotes / newlines, csvDate dd/mm/yyyy, csvFileStamp YYYY-MM-DD, downloadCsv = BOM \uFEFF + ';' separator + a.click()); read sibling idioms (payments/students toolbar search: relative wrapper + Search left-3 + rounded-xl border-stone-200 bg-white pl-9 + outline Ekspor CSV with Loader2/Download swap + disabled loading||exporting||filtered.length===0; registrations-admin Tabs filter with per-status text-[10px] counts); confirmed /api/notifications GET returns NotificationLog[] (userId, phone, message, status, createdAt — NO recipient name) and GET /api/users returns AppUser[] (id+name) for the Penerima mapping; probed lucide 0.525: Search/X/Download/Loader2/SearchX/RotateCcw all exported.
- Edited src/components/admin/whatsapp-log.tsx ONLY (239→~360 lines): (1) toolbar search Input h-11 rounded-xl border-stone-200 bg-white pl-9 pr-11 aria-label "Cari pesan" placeholder "Cari penerima, pesan, atau nomor…", wrapper w-full sm:min-w-52 sm:flex-1 (mobile full-width then filters wrap), clear = ghost size="icon" Button absolute right-0.5 size-11 (=44px target, twMerge overrides size-9) rounded-full X icon, rendered only when search non-empty, aria-label "Hapus pencarian"; (2) segmented status chips (module-level STATUS_CHIPS: SEMUA 'Semua'/SENT/PENDING/FAILED) as raw buttons mirroring buttonVariants base (h-9 rounded-full border px-3.5 text-sm font-medium transition-all outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]), active = emerald solid (border-emerald-700 bg-emerald-700 text-white hover:bg-emerald-800), inactive = border-stone-200 bg-white text-stone-600 hover:border-emerald-300 hover:bg-emerald-50/60, aria-pressed, count badge rounded-full px-1.5 py-0.5 text-[10px] font-semibold (inactive tinted per status: stone/emerald-100/amber-100/red-100 — matches statusBadgeClass hues; active bg-white/20 text-white), role="group" aria-label "Filter status pesan"; (3) combined info row below toolbar: "Menampilkan X dari Y pesan" text-xs text-stone-500 + "Bersihkan filter" ghost sm Button (RotateCcw) when hasFilter (search.trim() || statusFilter!=='SEMUA') → resetFilters(); right side ml-auto keeps ALL prior actions (Kirim Pesan Manual emerald, refresh icon "Muat ulang") plus new outline "Ekspor CSV"; (4) exportCsv mirrors round-9 exactly (setExporting guard, 200ms delay, downloadCsv, finally reset): filename `log-whatsapp-${csvFileStamp()}.csv`, headers [Tanggal, Penerima, No. HP, Pesan, Status], rows from `filtered` (same state the cards render), Tanggal=csvDate(createdAt), Penerima=usersById.get(userId) ?? '', Pesan/phone sanitized by shared csvCell inside downloadCsv; toast {title:'Ekspor CSV berhasil', description: `${filtered.length} data pesan tersimpan di ${filename}.`}; (5) NEW empty-filter state (filtered.length===0 && messages.length>0): Card border-2 border-dashed border-stone-300 bg-stone-50/50 + SearchX + "Tidak ada pesan yang cocok dengan filter." + emerald-outline "Bersihkan Filter" reset — existing Inbox "Belum ada pesan" card untouched for truly-zero messages; list maps `filtered` instead of `messages`; card header now also shows recipient name "· {name}" (text-xs stone-500) next to the phone when userId resolves; (6) users fetched once on mount (isAdmin branch, apiGet /api/users .then(setUsers).catch(() => setUsers([])) — silent fallback so log still renders/searches phone+message if users fetch fails); usersById useMemo Map; counts useMemo; filtered useMemo (status gate + q across recipient name / phone / message, case-insensitive, trim). All new hooks (usersById/counts/filtered) placed BEFORE the !isAdmin early return (rules-of-hooks safe).
- Note: this section never had a "Tandai semua dibaca" action (that lives in the parent portal inbox) — existing actions here = Kirim Pesan Manual dialog + Muat ulang refresh, both preserved verbatim.
- QA: `bun run lint` → clean (eslint . exit 0); `bunx tsc --noEmit 2>&1 | grep whatsapp` → empty; full tsc output = only the 4 pre-existing examples/+skills/ errors. Dev server NOT run; overview.tsx and all other files untouched; no new deps.

Stage Summary:
- File changed (only): src/components/admin/whatsapp-log.tsx. WhatsAppLog now has: h-11 clearable search (aria-label "Cari pesan") across recipient name+phone+message, SEMUA/SENT/PENDING/FAILED chip filter with live counts (emerald-solid active), "Menampilkan X dari Y pesan" + conditional "Bersihkan filter" ghost, "Ekspor CSV" outline (log-whatsapp-<stamp>.csv, BOM+';', filtered rows only, disabled when loading/exporting/empty, Loader2 swap, round-9 toast wording), dashed-border empty-filter card with reset (distinct from the zero-data Inbox card), recipient name shown on cards when resolvable via /api/users map.
- Decisions: recipient name sourced client-side from GET /api/users (notifications API returns userId only; section is ADMIN-guarded; silent catch → search degrades to phone+message) — no API changes allowed in scope; chips implemented as custom buttons with the exact buttonVariants focus-visible/ring idiom instead of Tabs because the task mandates "active chip = emerald solid" + badge counts (registrations' TabsList can't do emerald-solid without overriding its internals anyway); chip count badges reuse the status hue map from statusBadgeClass (stone/emerald/amber/red) so chips visually key into the card badges; search input h-11 (task spec) vs siblings' default h-9 — deliberate to seat the 44px clear target flush inside the field; export reads `filtered` (exports exactly what the user sees, round-9 contract).
- QA results: lint clean; tsc clean in scope (4 pre-existing unrelated errors remain); no dev server, no other files modified, no data/API changes.

---
Task ID: 13-a
Agent: target-workflow (frontend-styling-expert)
Task: Target-aware hafalan workflow for guru/admin — shared hafalan-utils lib, inline target strip + live surah hint + riwayat target markers in hafalan-admin, "Progres Target Santri" nudge card in guru-overview.

Work Log:
- Read worklog.md (head 120 + tail 180; round 12 shipped Student.hafalanTarget + parent target strip) and read hafalan-chart.tsx, hafalan-admin.tsx, guru-overview.tsx fully; confirmed /api/students rows carry hafalanTarget (Student type) and hafalan-admin loads /api/hafalan rows incl. h.student + h.studentId; verified lucide 0.525 exports Crosshair/Target/Sparkles/CheckCircle2; dev server healthy on :3000 (no restarts).
- NEW src/lib/hafalan-utils.ts: extracted verbatim from hafalan-chart.tsx — JUZ30_SURAHS (38 entries), norm(), NORM_ALIASES (alkausar→alkautsar, allahab→almasad, aththariq→attariq), resolveNorm(), JUZ30_NORM — plus NEW pure targetProgress(hafalans, target): null when target falsy/blank or outside Juz-30 (resolveNorm lookup); position = 1-based list index; reached = distinct list surahs at index ≤ position with ≥1 setoran (Set of resolveNorm(surahName)); percent = round(reached/position*100); targetReached = target surah itself has setoran; targetName = canonical list name. All exports: JUZ30_SURAHS, norm, NORM_ALIASES, resolveNorm, JUZ30_NORM, targetProgress (+ TargetProgress interface). No React, no fetch, hydration-safe.
- REFACTOR src/components/parent/hafalan-chart.tsx (minimal): deleted the local 38-entry list + norm/NORM_ALIASES/resolveNorm/JUZ30_NORM, replaced with `import { JUZ30_NORM, JUZ30_SURAHS, norm, resolveNorm } from '@/lib/hafalan-utils'`. All computation logic (setoranCount, targetInfo, targetReached, targetPercent, targetDone) untouched → parent strip behavior identical.
- bun -e verification of targetProgress (post-refactor):
  Rizky  target An-Naba : {"valid":true,"position":2,"reached":1,"percent":50,"targetReached":false,"targetName":"An-Naba"}
  Alika  target Abasa   : {"valid":true,"position":4,"reached":3,"percent":75,"targetReached":true,"targetName":"Abasa"}
  PASS: both fixtures match expected values (also spot-checked: null target → null, non-Juz-30 target "Al-Baqarah" → null).
- EDIT src/components/admin/hafalan-admin.tsx: (1) inline amber strip (rounded-xl border-amber-200 bg-amber-50 p-3, role="status", aria-label) rendered right after the Santri field when the selected student has hafalanTarget: amber-600 Target icon + "Target santri: **{name}**" (bold) + "{reached}/{position} surah ({percent}%)" + Progress h-1.5 bg-amber-200 [&>div]:bg-amber-500 + emerald CheckCircle2 chip "Target sudah tercapai — pertahankan!" when targetReached; computed via targetProgress over hafalans filtered to studentId; degraded copy "Target di luar peta Juz 30 — progres tidak dihitung." if target unresolvable (parity with parent strip hiding). (2) Live surah hint under the "Nama Surah" datalist input in a persistent aria-live="polite" container id="surah-target-hint": typed surah resolveNorm-matches target → emerald Sparkles line "Surat ini adalah target santri — setoran ini akan menyelesaikan target!" (or amber "Surat target sudah tercapai sebelumnya." when targetReached); silent when no target/no match. (3) Riwayat rows: tiny amber Target icon (title/aria "Target tercapai") appended next to surah text when resolveNorm(h.surahName) === resolveNorm(target of h.studentId) via a precomputed targetHitIds Set (per-row owner's target, not the selected one). No-target students → nothing renders (backward compatible).
- EDIT src/components/admin/guru-overview.tsx: added full-width "Progres Target Santri" Card AFTER WeeklySchedule — CardHeader with Crosshair in size-9 rounded-lg bg-amber-100 text-amber-700 tile (matches WeeklySchedule header pattern), description "Ketekunan setoran dibanding target hafalan tiap santri."; body rows = ownStudents with a target that resolves in Juz-30 via targetProgress over ownHafalan grouped by studentId (no new fetches — reuses loaded students+hafalans state; invalid/no targets skipped); each li: emerald initials avatar (new initialsOf helper, max 2 letters) + name + class · Target name, amber mini progress track (h-1.5 rounded-full bg-stone-100, amber-500 fill width=percent, role="progressbar" aria-valuenow/min/max), "{reached}/{position}" font-mono tabular-nums, status chip: targetReached||percent>=100 → emerald CheckCircle2 "Target tercapai"/"Tercapai" (targetReached wins), percent>=60 → emerald "Mendekati", else amber "Progres {percent}%"; sorted targetReached-first then percent desc; rows non-interactive with title tooltip "{name} — target {targetName}, {reached}/{position} surah"; role="list"/"listitem", flex-wrap mobile-safe, max-h-64 thin-scrollbar (existing SCROLL_AREA) when >4 rows; empty state = dashed note "Belum ada santri dengan target hafalan. Atur melalui menu Santri." with no CTA.
- QA: `bun run lint` → clean (exit 0); `bunx tsc --noEmit 2>&1 | grep -vE "examples/|skills/"` → empty (exit 1 = zero matches); both bun -e fixtures pass post-refactor; GET /api/hafalan | head -c 300 still returns student.className-shaped rows (contract unchanged); git scope check confirms only the 4 allowed files touched (students-admin/whatsapp-log/student-detail-drawer changes visible in git belong to parallel agents, untouched by me).
- Note: worklog tail re-read immediately before append (still 465 lines, round-12 end) — no concurrent changes to merge.

Stage Summary:
- New lib contract: src/lib/hafalan-utils.ts exports JUZ30_SURAHS (38), norm, NORM_ALIASES, resolveNorm, JUZ30_NORM, targetProgress(hafalans: {surahName}[], target: string|null|undefined) → TargetProgress|null {valid, position, reached, percent, targetReached, targetName}. Single source of truth for target math — parent chart and guru/admin UIs can never drift.
- hafalan-chart.tsx is now a thin consumer (imports only); its strip/coverage-map behavior is byte-equivalent in logic. hafalan-admin gains: target strip (only when selected student has target), aria-live surah-target hint, per-row amber Target markers. guru-overview gains the "Progres Target Santri" card (no new API calls; rows only for own-class students with resolvable targets; targetReached-first sorting).
- Styling kept emerald/amber/stone, rounded-xl/2xl, aria labels/roles/live regions throughout; no Date.now/random in render paths (all target math pure from fetched props/state).
- QA results: lint clean; tsc clean (minus examples/skills); targetProgress fixtures 2/2 exact (Rizky 2/1/50%/false, Alika 4/3/75%/true); GET /api/hafalan contract verified unchanged; dev server untouched on :3000.
- Files changed (4, within scope): NEW src/lib/hafalan-utils.ts; EDIT src/components/parent/hafalan-chart.tsx (imports only), src/components/admin/hafalan-admin.tsx, src/components/admin/guru-overview.tsx.

---
Task ID: 13-c
Agent: student-drawer (frontend-styling-expert)
Task: Per-student detail drawer for admin — right-side Sheet from each Santri row's Aksi dropdown with Hafalan / Absensi / Tagihan tabs.

Work Log:
- Read worklog.md (head 120 + tail, re-read tail before append — 13-a/whatsapp-log landed in parallel), students-admin.tsx fully (555 lines), types.ts, api-client.ts, ui/sheet.tsx + ui/tabs.tsx, statusBadgeClass in overview.tsx, type/grade/status color maps in hafalan-admin + child-panel + payments-admin, prisma schema (Attendance has NO own date; session holds date+topic but API strips it).
- curl shape verification (dev server untouched, SID = Rizky cmtufisd1000sq12wyuomvtbq from GET /api/students):
  * /api/hafalan?studentId= → [{id, studentId, surahName, ayatRange, type, grade (int|null), teacherNote (string|null), createdAt, student:{id, fullName, nis, className}}] — 5 rows for Rizky.
  * /api/attendance?studentId= → [{id, sessionId, studentId, status, note (string|null), createdAt, student:{...}, className}] — confirmed NO date/topic keys (session: undefined is dropped by JSON; className mapped) → coded date from createdAt, secondary line = className + note. 3 rows.
  * /api/payments?studentId= → [{id, invoiceNo, studentId, title, amount (float), method (string|null), status, paidAt (string|null), createdAt, student:{id, fullName, nis, parent:{id, name, phone}}}] — 3 rows (1 PENDING 75000, 2 SUCCESS 75000).
- NEW src/components/admin/student-detail-drawer.tsx: export StudentDetailDrawer({student, open, onOpenChange}) — shadcn Sheet side="right" w-full sm:max-w-md p-0 flex-col. Header (stone-50/60, border-b): emerald-700 initials avatar size-12 + SheetTitle fullName + sr-only SheetDescription + chips (NIS font-mono stone, class emerald-50 outline, status badge via copied studentStatusBadge mapping (AKTIF emerald/LULUS violet/NONAKTIF stone — students-admin's local fn is not exported), amber "Target: X" chip when hafalanTarget); identity dl divide-y stone-100 rows with lucide icons (User jenis kelamin, Cake tanggal lahir formatShortDate, MapPin alamat, Phone wali+phone); WhatsApp-style demo note (emerald-50 bubble rounded-2xl rounded-bl-sm, MessageCircle, NOT a link) when parent.phone exists. Tabs (Radix, keyboard-navigable): TabsList grid-cols-3 with h-11 triggers (≥44px targets), per-tab content min-h-0 flex-1 overflow-y-auto + [scrollbar-width:thin] emerald thumb. Fetch: single useEffect on [open, studentId] — fires only when open && studentId; resets tab to 'hafalan' + clears data/errors and bumps per-tab genRef (stale-response cancel guard) each open/student change; fetches all three endpoints in parallel via apiGet; per-tab loading skeleton (3×h-16), per-tab destructive Alert + h-11 "Coba Lagi" retry, Inbox empty states ("Belum ada setoran hafalan." / "Belum ada data absensi." / "Belum ada tagihan."). Hafalan tab: summary "X setoran · rata-rata Y" (avg of non-null grades), rows = grade circle (>=85 emerald-600 / >=70 amber-500 / else red-500, null stone-300 "—" — child-panel thresholds), "QS {surah} ayat {range}", type badge matching hafalan-admin exactly (TAHFIDZ emerald / TAHSHIN amber / MURAJAAH teal — task said "stone" but its own tie-breaker "match hafalan-admin" wins; hafalan-admin uses teal), italic truncated teacherNote w/ title, formatShortDate createdAt; role=list/listitem, rounded-xl border-stone-100 hover:bg-stone-50 transitions. Absensi tab: summary chips H/I/S/A with status dots (emerald/amber/orange/red) + rate chip (>=85 emerald / >=70 amber / else red; rate = hadir/total), rows = date + className + note + status badge via imported statusBadgeClass (HADIR emerald/IZIN amber/SAKIT orange/ALPA red — matches parent portal). Tagihan tab: two stat tiles "Total dibayar" (emerald, SUCCESS sum) / "Tunggakan" (amber, PENDING sum — same rule as portal/parent billing.outstanding), rows = mono invoiceNo + status badge (statusBadgeClass: SUCCESS emerald/PENDING amber/FAILED red — payments-admin uses the same import), title + formatRupiah(amount), "Dibayar {paidAt}" else "Dibuat {createdAt}".
- EDIT src/components/admin/students-admin.tsx ONLY (5 spots): +IdCard lucide import; +import StudentDetailDrawer; +detailStudent/detailOpen state; "Detail" DropdownMenuItem (IdCard emerald icon) as FIRST item of the per-row Aksi menu (before Edit); drawer mounted before the Hapus AlertDialog. Nothing else touched (git diff verified).
- Browser E2E (agent-browser, admin session): Aksi menu shows Detail first; drawer opens for Alika (chips NIS/kelas/AKTIF/"Target: Abasa", WhatsApp note, "6 setoran · rata-rata 85", type labels Tahshin/Murajaah/Tahfidz, grade circles) and Rizky (Hafalan "5 setoran · rata-rata 86"; Absensi "H 1 I 1 S 1 A 0" + "33% kehadiran" + rows w/ class+note+badges; Tagihan "Total dibayar Rp 150.000 · Tunggakan Rp 75.000" + 3 invoice rows with correct badges); network log shows exactly 3 API GETs per open (hafalan/attendance/payments?studentId=, all 200) only while open; Escape closes (sheet-content removed); zero page errors.

Stage Summary:
- Contract: StudentDetailDrawer({student: Student | null, open: boolean, onOpenChange: (o: boolean) => void}) in src/components/admin/student-detail-drawer.tsx; internally keys fetches by student.id + open (fetch only when open && student; full state reset per open → always-fresh data), per-tab genRef stale-guard, no new deps, hydration-safe (no Date/random in render; all dates via formatShortDate).
- Response shapes coded against (curl-verified): hafalan rows {surahName, ayatRange, type, grade|null, teacherNote|null, createdAt, student.className}; attendance rows {status, note|null, createdAt, className} — NO date/topic (session stripped by API; createdAt is the session-day date, className replaces topic); payment rows {invoiceNo, title, amount, status, paidAt|null, createdAt, student.parent}.
- Wiring notes for main agent: nothing further needed — students-admin already mounts the drawer; drawer is ADMIN/GURU-agnostic (renders whatever student row it is given); Color maps: status via shared statusBadgeClass('./overview'), student status copied (not exported in students-admin), hafalan type matches hafalan-admin (MURAJAAH=teal, documented deviation from the brief's "stone" hint), grade/attendance-rate thresholds match child-panel/attendance-recap.
- Files changed: NEW src/components/admin/student-detail-drawer.tsx (~430 lines); EDIT src/components/admin/students-admin.tsx (+13 lines, 5 spots). No API/schema/other files touched; no new packages; dev server not started/stopped.
- QA results: bun run lint → clean (exit 0); bunx tsc --noEmit | grep -E "student-detail|students-admin" → empty; curl evidence head -c 300 captured for all three endpoints (see Work Log); browser E2E all 3 tabs + a11y (dialog name = student, sr-only description, tablist keyboard focusable, Escape close, role=list lists, 44px tab/retry targets) pass with zero console/page errors.

---
Task ID: 13 (cron review round)
Agent: main (Z.ai Code) + 3 subagents (13-a/13-b/13-c)
Task: Round-13 — QA sweep, target-aware hafalan workflow (shared lib + admin form + guru nudge card), WhatsApp log filters/export, per-student detail drawer

Work Log:
- Reviewed worklog (round 12 complete). QA sweep: lint/tsc clean, dev.log clean, browser E2E + console zero errors. STABLE → features. Verified /api/hafalan & /api/attendance & /api/payments already accept ?studentId (no API changes needed this round).
- [13-a subagent] (1) NEW src/lib/hafalan-utils.ts: JUZ30_SURAHS/norm/NORM_ALIASES/resolveNorm/JUZ30_NORM + targetProgress() extracted from hafalan-chart.tsx (parent) — hafalan-chart refactored to import (behavior identical; fixtures re-proven: Rizky An-Naba → 1/2 50% notReached, Alika Abasa → 3/4 75% reached). (2) hafalan-admin.tsx: amber "Target santri" strip in Catat Setoran form when selected student has target (name, reached/position, %, amber Progress, emerald "Target sudah tercapai" chip when reached; degraded note for non-Juz-30 targets), aria-live surah hint under Nama Surah ("Surat ini adalah target santri — setoran ini akan menyelesaikan target!" emerald Sparkles / "sudah tercapai sebelumnya" amber), amber Target markers in Riwayat rows whose surah hits that row's student target (targetHitIds set). (3) guru-overview.tsx: "Progres Target Santri" card after WeeklySchedule — rows for own-class students w/ resolvable targets (initials avatar, name + "Kelas · Target: X", amber mini progress bar, mono "r/p", chip: targetReached wins emerald "Target tercapai" / ≥100 "Tercapai" / ≥60 "Mendekati" / amber "Progres n%"), sorted reached-first-then-percent, role=list, max-h-64 thin scrollbar, dashed empty note.
- [13-b subagent] whatsapp-log.tsx (+164 net): toolbar search (Search icon wrapper, clearable 44px X, aria-label "Cari pesan") across name/phone/message; segmented status chips SEMUA/SENT/PENDING/FAILED with live count badges (active emerald solid, aria-pressed); "Menampilkan X dari Y pesan" + "Bersihkan filter" reset; "Ekspor CSV" mirroring round-9 (filtered rows, BOM+';', log-whatsapp-<stamp>.csv, Loader2 swap, toast); dashed SearchX empty-filter state distinct from zero-data Inbox state; message cards show "· {recipient}" via /api/users id→name map (silent fallback); all existing actions preserved.
- [13-c subagent] NEW src/components/admin/student-detail-drawer.tsx (~430 lines): StudentDetailDrawer({student, open, onOpenChange}) — Sheet side=right sm:max-w-md; header initials + NIS/class/status chips + amber target chip; identity rows (gender/birth/address/wali+phone) + WhatsApp info note; 3 lazy tabs (fetch-all-on-open, per-tab genRef stale guards, skeletons, error+retry, empty states): Hafalan (surah+ayat, type badge TAHFIDZ emerald/TAHSHIN amber/MURAJAAH teal, grade circle, "X setoran · rata-rata Y"), Absensi (date/className/note rows + status badges + H/I/S/A chips + rate), Tagihan (invoiceNo mono, formatRupiah, status badges, "Total dibayar · Tunggakan"); students-admin.tsx: "Detail" as FIRST Aksi menuitem (IdCard icon) + drawer mount + state (5 edit spots only).
- [ain demo data] Salman DJ-2025-009 → target Al-Fatihah (new registrant, first surah), Hamzah DJ-2024-005 (Iqra 4, Fatimah's student) → target Al-Fatihah (0/1 Progres 0% — has no setoran yet). Kept as demo.
- E2E verified in browser: hafalan form — Rizky selected → strip "Target santri: An-Naba 1/2 surah (50%)", typing "An-Naba" → emerald Sparkles hint; guru Ringkasan — nudge card empty state (before Hamzah had target) then populated row (Hamzah · Iqra 4 · Target: Al-Fatihah · 0/1 · Progres 0%) after reload; WA log — chips Semua 13/SENT 13/PENDING 0/FAILED 0, search "Zahra" → "Menampilkan 2 dari 13 pesan"; student drawer (Salman) — identity + Hafalan empty state + Tagihan "Total dibayar Rp 0 · Tunggakan Rp 75.000" + INV-2025-1010 PENDING row; parent portal re-verified post-refactor (target strip An-Naba 50% + amber-ringed tile intact); zero console/page errors; lint + tsc clean after integration.

Stage Summary:
- Round 13 shipped: shared hafalan-utils lib (single source of Juz-30 target math across admin+guru+parent), target-aware setoran form (strip + live hint + riwayat markers), guru "Progres Target Santri" nudge card, WA log search/status-filters/CSV export, per-student detail drawer (Hafalan/Absensi/Tagihan) wired into Santri Aksi menu.
- Contracts: targetProgress() in '@/lib/hafalan-utils'; StudentDetailDrawer({student, open, onOpenChange}) from '@/components/admin/student-detail-drawer'; no API changes.
- Demo data: Hamzah + Salman targets Al-Fatihah (both 0/1, Progres 0%) alongside Rizky An-Naba 50% / Alika Abasa 75% Tercapai.
- Known notes: hafalan GET take:100 cap still applies (fine for demo scale); WA recipient names resolved client-side via /api/users (silent fallback keeps search working if it fails).
- Next-step candidates (priority order): (1) hafalan POST → auto "target reached" WhatsApp celebration to parent when setoran completes the student's target, (2) admin dashboard announcement composer preview (WA-style preview before broadcast), (3) PPDB stats mini-chart on admin overview (registrations per week), (4) dark-mode audit, (5) session QR history archive view.

---
Task ID: 14-a
Agent: target-celebration (full-stack-developer)
Task: "Target tercapai" celebration — WA ucapan ke wali + UI dialog perayaan saat setoran pertama kali menyempurnakan target hafalan santri

Work Log:
- Read worklog.md (head 120 + tail 200), src/lib/hafalan-utils.ts (targetProgress/resolveNorm contract), src/app/api/hafalan/route.ts, hafalan-admin.tsx (round-13 form + target strip + live hint), lib/api.ts (sendWhatsApp → Notification row WHATSAPP/SENT), prisma schema (Student.hafalanTarget, Hafalan, Notification).
- [API] EDIT src/app/api/hafalan/route.ts POST only: after create + standard setoran WA, added guarded celebration block (own try/catch → failure logs + resets to targetJustReached:false, NEVER breaks record creation): loads beforeList = db.hafalan.findMany({where:{studentId, id:{not:newId}}, select:{surahName:true}}); gate = hafalan.student.hafalanTarget && resolveNorm(surahName)===resolveNorm(target) (alias-safe); before=targetProgress(beforeList,target), after=targetProgress([...beforeList,{surahName}],target); justReached = before?.valid && after?.valid && !before.targetReached && after.targetReached. When justReached: sendWhatsApp to student.parent (skip silently when no parent) with "🎉 MasyaAllah, tabarakallah! {fullName} telah MENYEMPURNAKAN target hafalan *{targetName}* ({reached}/{position} surah Juz 30). Jazakumullahu khairan. — TPQ Darul Jinan" (after values; *bold* consistent w/ codebase). Response now { ...hafalan, targetJustReached: boolean, targetName: string|null }. Added doc-comment header contract (GET/POST/DELETE). No other handlers touched.
- [UI] EDIT src/components/admin/hafalan-admin.tsx: +PartyPopper lucide, +shadcn Dialog imports, +HafalanResponse type, +Celebration interface, +celebration state. Submit handler mirrors server math BEFORE POST (afterProg = targetProgress([...studentHafalans, {surahName}], selectedStudent.hafalanTarget)) → on created.targetJustReached: setCelebration({studentName, targetName: created.targetName, reached, position}) + toast title "🎉 Target tercapai!" desc "MasyaAllah! {studentName} menyempurnakan target {targetName}. WhatsApp ucapan terkirim ke wali." (replaces normal toast); else normal toast. Form reset + load() unchanged. One-shot celebratory Dialog: rounded-2xl text-center max-w-sm, PartyPopper size-10 in size-20 emerald-100 circle, "MasyaAllah, {studentName}!", "Target hafalan {targetName} berhasil disempurnakan ({reached}/{position} surah)." (target name bold emerald), amber "Orang tua telah menerima ucapan via WhatsApp.", single emerald "Alhamdulillah" button (h-11 min-h-11 = 44px, rounded-2xl); onOpenChange close → setCelebration(null) (X/Esc/overlay all reset); role="status" aria-live="polite" on DialogContent; palette emerald/amber/stone only. Round-13 live hint ("setoran ini akan menyelesaikan target!") untouched.
- curl E2E (dev server untouched, running): baseline Hamzah DJ-2024-005 (id cmtufisd70016q12wwgsqzies, target Al-Fatihah) = 0 hafalan rows, 13 notifications. POSITIVE: POST /api/hafalan {studentId, surahName:"Al-Fatihah", ayatRange:"1-7", type:"TAHFIDZ", grade:88} → {"id":"cmtuly1d40001q15lyizkjp78","targetJustReached":true,"targetName":"Al-Fatihah"}; GET /api/notifications 13→15 with top row "🎉 MasyaAllah, tabarakallah! Hamzah Abdurrahman telah MENYEMPURNAKAN target hafalan *Al-Fatihah* (1/1 surah Juz 30). Jazakumullahu khairan. — TPQ Darul Jinan" (phone 081300000003) + standard setoran WA. NEGATIVE: repeat Al-Fatihah (MURAJAAH 90) → targetJustReached:false (before.targetReached already true); An-Nas 1-6 (bukan target) → targetJustReached:false, targetName:null.
- Cleanup: DELETE /api/hafalan?id= x3 → {"success":true} each; bun+Prisma script deleted the 3 test Notification rows (2 standard Al-Fatihah WAs + 1 celebration) and 1 leftover An-Nas test WA; verified baseline restored: Hamzah hafalan rows 0, notifications 13, celebration rows 0. dev.log: no errors (only Fast Refresh full-reload notes from HMR + 200s).
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit | grep -vE "examples/|skills/" → empty.

Stage Summary:
- API contract: POST /api/hafalan returns { ...createdHafalan, targetJustReached: boolean, targetName: string | null }; celebration fires ONLY on first completion (before/after targetProgress diff + resolveNorm alias guard), WA message uses after-progress values, parent-less students skip WA silently, all celebration/WA failures degrade to targetJustReached:false without breaking the create. DELETE ?id= unchanged.
- UI contract: HafalanAdmin consumes response fields; celebration = 1) toast "🎉 Target tercapai!" 2) one-shot Dialog (PartyPopper in emerald-100 circle, heading "MasyaAllah, {studentName}!", line with bold targetName + {reached}/{position}, amber WA line, 44px emerald "Alhamdulillah" close); state resets on dialog close; no new deps; hydration-safe (no Date/random in render); emerald/amber/stone only.
- QA results: lint clean; tsc filtered clean; curl evidence above (positive true + 2 negatives false + WA row + full cleanup to baseline 13 notifications / 0 Hamzah hafalan rows).
- Files changed (exactly 2): src/app/api/hafalan/route.ts (+52/-1), src/components/admin/hafalan-admin.tsx (+69/-5). No schema/other files; no new packages; dev server not started/stopped.

---
Task ID: 14-c
Agent: ppdb-chart (frontend-styling-expert)
Task: "PPDB — Pendaftar 6 Minggu" insights card on admin overview — stacked weekly registrations BarChart (status split) + momentum trend note.

Work Log:
- Read worklog.md (head 120 + tail 150; re-read tail before append — 14-a celebration entry landed mid-flight at lines 550-568; its hafalan route/admin changes are that agent's scope, untouched by me). Read src/components/admin/overview.tsx fully (statusBadgeClass hues, KpiCard tile idiom, LineChart conventions: CartesianGrid #e7e5e4 "3 3" vertical=false, tick 11px #78716c, stroke #d6d3d1, margin {top:8,right:12,bottom:0,left:-8}, Tooltip contentStyle borderRadius 12 / border #e7e5e4 / fontSize 12), src/app/api/registrations/route.ts GET (findMany orderBy createdAt desc, no cap → Registration[] with createdAt+status), src/app/api/stats/route.ts, src/lib/types.ts; verified lucide-react TrendingUp/TrendingDown and recharts Bar/BarChart exports exist.
- EDIT src/components/admin/overview.tsx ONLY: (1) imports +useMemo, +TrendingDown/TrendingUp, +Bar/BarChart, +Registration type; (2) module-level PPDB_SERIES config (PENDING #d97706 "Menunggu", VERIFIKASI #0d9488 "Verifikasi", DITERIMA #059669 "Diterima", DITOLAK #dc2626 "Ditolak" — hexes matched to the statusBadgeClass hue families amber/teal/emerald/red) + WeekBucket interface + pure helpers pad2/dayKey/mondayOf/buildWeekBuckets; (3) load() extended to Promise.all([apiGet /api/stats, apiGet /api/registrations]) with Array.isArray guard, SAME single try/catch → destructive Alert + "Coba Lagi" (registrations failure fails the section like stats failure — file's existing error/retry pattern); (4) useMemo weekBuckets + derived totals + ppdbAriaLabel (all pure functions of fetched data, no Date.now/wall-clock in render → hydration-safe); (5) one extra <Skeleton h-64> added to the loading skeleton for the new card slot; (6) new full-width Card inserted directly after "Tren Kehadiran 7 Hari" (trend cards grouped, all existing cards intact): header = ClipboardList in emerald pastel tile (size-9 rounded-xl bg-emerald-100 text-emerald-700) + title "PPDB — Pendaftar 6 Minggu" + description "Tren pendaftaran santri baru per minggu."; stacked recharts BarChart (4× Bar stackId="a", maxBarSize 36, NO radius — per-bar radius on 4 stacked series renders broken caps) in h-40 sm:h-48 container; XAxis Senin-start dd/MM labels interval={0} 11px #78716c, YAxis allowDecimals={false}, stone-tinted hover cursor, Tooltip labelFormatter → "Minggu ke-N · dd/MM–dd/MM" + Indonesian series labels, contentStyle matching the LineChart; inline legend of 4 status dots (size-2.5, STATUS_DOTS idiom); stat row "Total: N pendaftar · Menunggu: X · Diterima: Y" with tiny amber/emerald dots; momentum chip: weekRising (latest>0 && latest>=prev) → emerald TrendingUp "Momentum naik (N minggu terakhir)" else stone TrendingDown "Menurun (...)"; zero total → dashed empty state "Belum ada pendaftar dalam 6 minggu terakhir." (same pattern as sibling empty states); chart container role="img" aria-label summarizing total + per-status counts.
- Aggregation logic: window anchored at Monday (Senin-start, local midnight, dayKey = y*10000+m*100+d → DST/tz-safe comparisons) of the LATEST registration createdAt — not Date.now — giving 6 buckets oldest→newest labeled Minggu ke-1..ke-6; each registration bucketed by dayKey(mondayOf(createdAt)); registrations older than the window are excluded; unexpected statuses still count toward total.
- Week-bucket sanity (bun -e, TZ=Asia/Jakarta, exact copy of component logic): Minggu ke-1 29/09–05/10 {PENDING:2} (incl. Sabtu 4 Okt row), ke-2 06/10 {VERIFIKASI:1} ← 2025-10-05T20:00Z (Sunday UTC) correctly rolls to SENIN 6 Okt WIB, ke-3 13/10 {0}, ke-4 20/10 {DITERIMA:2}, ke-5 27/10 {0}, ke-6 03/11 {DITOLAK:1} — Senin-start aligned, oldest→newest; + older-than-window row (1 Agu) excluded (sum stays 6); [] → []; single old registration slides the whole window to its own week. PASS.
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit 2>&1 | grep overview → empty (exit 1, zero matches); full tsc = only the 4 pre-existing examples/+skills errors; git scope: src diff = overview.tsx (mine) + hafalan route/admin (14-a, parallel agent); dev server NOT run; no new deps (recharts/lucide already installed).

Stage Summary:
- File changed (only): src/components/admin/overview.tsx (~+160 lines). New full-width "PPDB — Pendaftar 6 Minggu" card sits between the Tren Kehadiran chart and the recent-lists grid; every existing card and the skeleton layout preserved (one skeleton added for the new slot).
- Contract (in-file, exported nothing new): buildWeekBuckets(Registration[]) → WeekBucket[] of exactly 6 Senin-start weeks anchored on the latest data week, each {label "dd/MM", tooltip "Minggu ke-N · dd/MM–dd/MM", PENDING/VERIFIKASI/DITERIMA/DITOLAK/total}; fetch = Promise.all(stats, registrations) inside the existing load(); no API/schema changes.
- Decisions: bucket anchor = latest createdAt instead of Date.now (task's hydration rule) → the newest bucket is the most recent week WITH activity, so the chip says "minggu terakhir" rather than "minggu ini"; rising requires latest.total > 0 (avoids "Momentum naik" on a 0-vs-0 tie); stacked bars skip radius (task's "only if simple" guard); full-width placement groups it with the attendance trend card; icon = ClipboardList (already the file's PPDB icon, imported); status hexes taken from statusBadgeClass hue families for cross-card consistency.
- QA results: lint clean; tsc clean in scope; bun -e week-bucket fixtures 4/4 pass (Senin-start, UTC-Sunday→Senin-WIB rollover, oldest→newest ordering, window exclusion, empty + single-reg edges); role="img" a11y label verified in code; only overview.tsx touched.

---
Task ID: 14-b
Agent: wa-preview (frontend-styling-expert)
Task: WhatsApp-style live preview inside the announcement composer — admin sees the exact broadcast message on a mock WA phone before sending.

Work Log:
- Read worklog.md (head 120 + tail 150; re-read tail before append — 14-a/14-c landed mid-flight, now 585 lines, no overlap with my file), src/app/api/announcements/route.ts (broadcast gate = `b.priority === 'PENTING' && b.broadcast`; message = `*${b.title}*\n${b.content}\n— TPQ Darul Jinan`), src/components/admin/content-admin.tsx fully (composer state annTitle/annContent/annPriority/annBroadcast, submit sends broadcast: annPriority==='PENTING' && annBroadcast, checkbox only rendered for PENTING; dialog is single-column sm:max-w-lg).
- EDIT src/components/admin/content-admin.tsx ONLY: (1) imports +MoonStar/+CheckCheck/+useSyncExternalStore/+Separator; (2) module-level WA_SIGNATURE const + composeBroadcastMessage(title, content) pure helper replicating route.ts composition character-for-character, + hydration-safe client clock: getWaClock() with per-minute cached waClockStore consumed via useSyncExternalStore (getServerSnapshot → '' so SSR renders empty; 15s subscribe interval keeps "HH.MM" fresh) — avoids the new react-hooks/set-state-in-effect lint error that a plain mount-effect setState triggered; (3) NEW file-local WaBroadcastPreview({title, content, willBroadcast}): <section aria-label="Pratinjau pesan WhatsApp" aria-live="polite"> containing (a) status line — willBroadcast ? amber strip (Megaphone + "Pesan ini akan disiarkan ke semua wali via WhatsApp." + amber PENTING badge) : stone strip (MessageCircle + "Disimpan sebagai pengumuman portal (tanpa broadcast WhatsApp).") — (b) phone mock: w-full max-w-sm mx-auto rounded-2xl border-stone-200 overflow-hidden; WA contact header row (size-9 emerald-700 avatar circle + MoonStar size-4, "TPQ Darul Jinan" text-sm font-semibold, "notifikasi simulasi" text-[11px] text-stone-400); chat backdrop bg-stone-200 (= #e7e5e4) with subtle radial-dot pattern via Tailwind arbitrary properties [background-image:radial-gradient(rgba(87,83,78,0.14)_1px,transparent_1px)] [background-size:14px_14px]; received bubble = max-w-[85%] rounded-2xl rounded-tl-sm bg-white p-2.5 shadow-sm; body <p whitespace-pre-line text-sm leading-relaxed text-stone-800> rendering the composed message with asterisks parsed bold via split('*') (codebase idiom from whatsapp-log/parent-portal: segments[1] → <strong> title, segments[2] → content body, signature re-appended after signature-stripped tail) — empty fields swap in muted text-stone-400 placeholders "Judul pengumuman…"/"Isi pengumuman…"; time row bottom-right = clock text-[11px] text-stone-400 + CheckCheck size-3.5 text-emerald-500; all icons aria-hidden; (4) composer wiring: derived const annWillBroadcast = annPriority === 'PENTING' && annBroadcast declared once and reused by submit payload, success-toast ternary AND preview prop (single source of truth, zero drift vs API gate); preview inserted below the fields after a Separator + small label "Pratinjau WhatsApp"; announcement DialogContent gains max-h-[88vh] overflow-y-auto (posts dialog idiom) so the taller dialog scrolls on short/mobile screens.
- Fidelity proof: temp bun script extracted both template literals from route.ts and content-admin.tsx, evaluated with identical sample title/content (multiline) → IDENTICAL: true (incl. em-dash signature); script deleted (scripts-tmp removed).
- QA: bun run lint → clean (fixed 1 self-introduced react-hooks/set-state-in-effect error, final exit 0); bunx tsc --noEmit 2>&1 | grep content-admin → empty; remaining tsc errors only the 4 pre-existing examples/+skills ones; dev server NOT run; no other files touched; no new deps.

Stage Summary:
- File changed (only): src/components/admin/content-admin.tsx (+~105 lines net). The Buat Pengumuman dialog now shows a live WhatsApp preview under the form fields: amber "akan disiarkan" strip + PENTING badge only when (PENTING && broadcast), stone "tanpa broadcast" strip otherwise, phone mock (contact header TPQ Darul Jinan / notifikasi simulasi, stone-200 dotted chat backdrop, white rounded-2xl rounded-tl-sm received bubble with shadow-sm) whose bubble text is the EXACT broadcast composition `*{title}*\n{content}\n— TPQ Darul Jinan` with *…* rendered bold, newlines preserved, muted placeholders when empty, client-only "HH.MM" timestamp + emerald CheckCheck ticks.
- Preview logic: derived from the same annTitle/annContent/annPriority/annBroadcast state the form uses (trim applied to mirror submit); broadcast coupling via shared annWillBroadcast const = exact API gate; aria-live="polite" region announces typing changes (no debounce needed at demo scale); hydration-safe clock via useSyncExternalStore (SSR '', per-minute cached snapshot).
- Decisions: single-column dialog → preview below fields separated by Separator + "Pratinjau WhatsApp" label (matches dialog idiom); bold parsing done on the composed message string with split('*') so the preview literally consumes the same string shape the API sends (title pair → strong, tail → content + signature) rather than hand-built JSX; max-h-[88vh] overflow-y-auto added to the announcement dialog (parity with the posts dialog) because the preview makes it taller than most viewports; stone/emerald palette only, no transitions needed (live text).
- QA results: lint clean; tsc clean in scope (grep content-admin empty); template-literal equivalence API vs preview → IDENTICAL:true; only content-admin.tsx modified.

---
Task ID: 14 (cron review round)
Agent: main (Z.ai Code) + 3 subagents (14-a/14-b/14-c)
Task: Round-14 — QA sweep, target-reached WA celebration, WhatsApp broadcast preview, PPDB 6-week trend chart

Work Log:
- Reviewed worklog (round 13 complete). QA sweep: lint/tsc clean, dev.log 400s all historical (low line numbers, from rounds 11-12 QA), browser loads + console zero errors. STABLE → features.
- [14-a subagent] Target-reached celebration: POST /api/hafalan now computes BEFORE/AFTER targetProgress (student list excl./incl. new row) with resolveNorm gate; justReached = valid && !before.targetReached && after.targetReached; isolated try/catch so celebration can never break creation; response +{targetJustReached, targetName}; on justReached sends WA "🎉 MasyaAllah, tabarakallah! {fullName} telah MENYEMPURNAKAN target hafalan *{targetName}* ({reached}/{position} surah Juz 30)…" to parent. hafalan-admin.tsx: success toast upgraded + one-shot celebration Dialog (PartyPopper in emerald-100 circle, "MasyaAllah, {name}!", target line, amber WA note, 44px "Alhamdulillah" button, role=status aria-live, resets on all close paths). curl E2E: Hamzah (target Al-Fatihah, 0 rows) → POST → targetJustReached:true + WA row created; repeat → false; An-Nas → false/null; test rows+notifications cleaned to baseline (0 hafalan / 13 notifications).
- [14-b subagent] WA broadcast preview in content-admin announcement dialog: WaBroadcastPreview({title, content, willBroadcast}) — phone-bubble mock (emerald avatar + "TPQ Darul Jinan"/"notifikasi simulasi" header, stone backdrop w/ dot pattern, white bubble rounded-tl-sm shadow-sm), live body = composeBroadcastMessage() replicating route.ts VERBATIM (fidelity proven by extracting both template literals into a temp bun script → IDENTICAL:true), *bold* parsed via split('*'), whitespace-pre-line, client-only HH.MM + CheckCheck via useSyncExternalStore (hydration-safe), muted placeholders when empty; willBroadcast = PENTING && broadcast (shared const → submit payload, toast, preview — zero drift): broadcast strip amber (Megaphone + "Pesan ini akan disiarkan ke semua wali via WhatsApp." + PENTING badge) else stone "tanpa broadcast" line; dialog got max-h-[88vh] scroll; aria-live region + aria-hidden icons.
- [14-c subagent] overview.tsx: NEW full-width "PPDB — Pendaftar 6 Minggu" card after Tren Kehadiran (all existing cards intact + skeleton slot): load() extended with /api/registrations (same error/retry path); buildWeekBuckets() useMemo — Monday-anchored 6 ISO weeks ending at latest registration's week (hydration-safe, no Date.now in render; tz/DST-safe dayKey math; bun -e test incl. UTC-Sunday→WIB-Monday boundary case); recharts stacked BarChart (PENDING #d97706 / VERIFIKASI #0d9488 / DITERIMA #059669 / DITOLAK #dc2626, maxBarSize 36, h-40 sm:h-48, grid/tooltip matching existing LineChart, labelFormatter "Minggu ke-N · dd/MM–dd/MM"); inline 4-dot legend + "Total/Menunggu/Diterima" stat row + momentum chip (TrendingUp emerald "Momentum naik" requires latest.total>0, else TrendingDown stone); dashed empty state at zero; role="img" aria-label summary.
- E2E verified in browser (admin): overview shows PPDB card w/ stacked bar (07/09 bucket amber+teal+emerald), tooltip breakdown, "Total: 4 pendaftar · Menunggu: 1 · Diterima: 2", emerald "Momentum naik (4 minggu terakhir)" chip; Konten → Pengumuman → Buat: preview bubble renders live bold title + content + signature + HH.MM ticks; NORMAL → stone "tanpa broadcast" line; PENTING → amber broadcast toggle block appears; broadcast ON → amber "Pesan ini akan disiarkan…" strip + PENTING badge in preview; dialog cancelled without saving; zero console/page errors; lint + tsc clean after integration.

Stage Summary:
- Round 14 shipped: POST /api/hafalan target-just-reached detection + WA celebration + UI celebration Dialog; WA-style live broadcast preview in announcement composer (API-verbatim composition); PPDB 6-week stacked trend card on admin overview with momentum chip.
- Contracts: POST /api/hafalan response +{targetJustReached: boolean, targetName: string|null}; WaBroadcastPreview internal to content-admin; overview load() now also fetches /api/registrations.
- Demo data: unchanged (14-a curl tests fully reverted; no new rows).
- Known notes: celebration math runs per POST (2 extra small queries — fine at demo scale); preview clock is client-only via useSyncExternalStore (SSR renders empty time — intended).
- Next-step candidates (priority order): (1) guru "quick setoran" inline sheet from guru-overview nudge card (pre-fills selected student), (2) PPDB accept dialog shows generated credentials (email + temp password) after DITERIMA for admin copy, (3) parent portal announcement read-state ("baru" badges per announcement), (4) dark-mode audit, (5) materials public section search/filter chips.

---
Task ID: 15-b
Agent: ppdb-credentials (full-stack-developer)
Task: "Credentials hasil penerimaan" panel — PUT /api/registrations DITERIMA now returns generated wali credentials and the Terima dialog switches to a copy-friendly success state.

Work Log:
- Read worklog.md (head 120 + tail 200), src/app/api/registrations/route.ts fully, registrations-admin.tsx fully; verified lucide-react exports Copy/Check/KeyRound/Info (runtime check) and that users/students APIs support DELETE ?id= for QA cleanup.
- [API] EDIT src/app/api/registrations/route.ts PUT only (GET/POST/DELETE + try/catch bad('Gagal memperbarui pendaftaran') contract untouched): DITERIMA + !existingStudent branch response extended to { registration, student, credentials: { nis, email: parent.email, tempPassword: 'ortu123' } } (parent may have pre-existed via findUnique — actual parent.email returned either way; inline comment documents it). DITERIMA + existingStudent (duplicate childName) falls through to the shared return which is now ok({ registration: reg, credentials: null }) — explicit null documented via comment: credentials null = no new account created (duplicate student OR non-DITERIMA status); UI keys the amber panel off credentials === null.
- [UI] EDIT src/components/admin/registrations-admin.tsx only: +AcceptCredentials/AcceptResult interfaces (no any), +file-local CredentialRow (label + font-mono text-sm value + outline copy Button min-h-11 with Copy icon → Check icon "Tersalin" for 2s, aria-label "Salin {label}", emerald outline hover emerald-100), +acceptResult/copiedKey state, +copyCredential() using navigator.clipboard.writeText with textarea+document.execCommand('copy') fallback (both try/catch; silent-fail → toast "Gagal menyalin…"), +resetAcceptState()/finishAccept() (reset acceptTarget/acceptClassId/acceptResult/copiedKey then await load()).
  - submitAccept(): PUT typed apiSend<AcceptResult>; on success toast kept concise ("{childName} resmi diterima. Notifikasi WhatsApp terkirim ke wali." — credential sentence removed since panel shows it) and instead of closing, setAcceptResult({registration, credentials ?? null}) → SAME dialog switches to success state: emerald CheckCircle2 header "Pendaftaran Diterima" + "{childName} resmi menjadi santri TPQ Darul Jinan."; credentials card rounded-xl border-emerald-200 bg-emerald-50/60 p-4 with 3 rows (NIS Santri / Email Wali / Kata Sandi Awal 'ortu123', all mono); KeyRound note line text-xs stone-500 "Kredensial juga telah dikirim ke wali via WhatsApp."; credentials === null → amber panel (Info icon, border-amber-200 bg-amber-50/60): "Santri dengan nama serupa sudah terdaftar — tidak ada akun baru dibuat." + status pendaftaran badge via shared statusBadgeClass; single emerald "Selesai" button min-h-11 (w-full on mobile) closing + reset + load(). Dialog onOpenChange close path also resets state and void load() if a success result was showing (X/Esc/overlay never leave a stale table). Reject/delete flows untouched; table untouched (no bonus additions per scope).
  - Palette: emerald/amber/stone only; rounded-xl/2xl; 44px targets on Salin + Selesai; a11y: aria-labels on copy buttons, aria-hidden icons, role=group + aria-label "Kredensial akun santri".
- curl E2E: baseline registrations 4 / students 9 / users 12 / notifications 13 → POST /api/registrations (QA-Cred-Anak, phone 081999887766) → PPDB-2025-0005 → PUT {status:'DITERIMA'} (classId omitted) → response credentials {nis:"DJ-2025-010", email:"081999887766@wali.daruljinan.id", tempPassword:"ortu123"} (exact-match assertion PASS, NIS regex PASS) + student/parentId ids returned; GET /api/notifications top row = "Selamat! Pendaftaran *QA-Cred-Anak* DITERIMA. NIS: *DJ-2025-010*. Akun Portal Wali: 081999887766@wali.daruljinan.id / ortu123. — TPQ Darul Jinan" (PASS). Duplicate-path (credentials:null) verified by code reading only per task scope.
- Cleanup: DELETE /api/registrations?id / /api/students?id / /api/users?id (users DELETE supported, ORANG_TUA not admin-guarded) → {"success":true} x3; scripts-tmp/cleanup-qa-notifs.ts (Prisma deleteMany by phone 081999887766) removed the 2 QA WA rows, then scripts-tmp dir deleted; re-verified baseline registrations 4 / students 9 / users 12 / notifications 13 with 0 QA rows anywhere; tmp files removed.

Stage Summary:
- API contract: PUT /api/registrations DITERIMA → { registration, student, credentials: { nis, email, tempPassword } } when a new santri+account are created; { registration, credentials: null } when no new account (duplicate childName or non-DITERIMA status). GET/POST/DELETE + error contract unchanged.
- UI contract: submitAccept no longer closes the dialog on success — the "Terima Pendaftaran" dialog switches to a success state driven by acceptResult (emerald credentials card w/ 3 copyable rows, or amber duplicate notice when credentials === null); "Selesai"/X/Esc/overlay all reset state and refresh the table via load(); copy fallback execCommand with toast on total failure.
- Files changed (exactly 2): src/app/api/registrations/route.ts (+3 lines/comments), src/components/admin/registrations-admin.tsx (+~120/-20). No other files; no new packages; dev server not started/stopped.
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit 2>&1 | grep -vE "examples/|skills/" → empty; curl E2E assertions PASS (credentials shape + WA log) with full baseline restoration (4/9/12/13, zero QA residue); dev.log clean, GET / → 200.

---
Task ID: 15-c
Agent: ann-readstate (frontend-styling-expert)
Task: Parent portal hero announcement read-state — localStorage-backed "Baru" badges, per-card mark-read buttons, "n pengumuman baru" line + "Tandai semua dibaca".

Work Log:
- Read worklog.md (head 120 + tail 200; re-read tail immediately before append — 641 lines now, PPDB-credentials entry landed in parallel, no file overlap), src/components/parent/parent-portal.tsx fully (422 lines pre-edit), types.ts (Announcement: id/title/content/priority/createdAt — cuid id safe as storage value), page.tsx deferred localStorage pattern (setTimeout in effect — the established workaround for the react-hooks/set-state-in-effect rule that a plain mount-effect setState triggers), probed lucide 0.525 (Sparkles/BellDot both exported) and confirmed dev server healthy on :3000.
- Edited src/components/parent/parent-portal.tsx ONLY (no API/backend changes — portal contract untouched): (1) module consts READ_ANNOUNCEMENTS_KEY='simadji.readAnnouncements' + READ_ANNOUNCEMENTS_CAP=100; pure helpers readStoredReadAnnouncements() (getItem + JSON.parse in try/catch → corrupt/missing/unavailable storage = [] ; Array.isArray + typeof string filter, no any) and persistReadAnnouncements(ids) (read-merge-dedupe via Set, slice(-100) drops OLDEST when exceeding cap, setItem in try/catch best-effort; both documented as never-call-during-render); (2) readAnnouncementIds state init [] → SSR + first client render identical (hydration-safe), hydrated in a dedicated useEffect via setTimeout(10ms)+clearTimeout cleanup (page.tsx pattern); (3) hero announcement cards converted div → <button type="button"> keeping ALL existing classes verbatim (flex gap-2.5 rounded-xl border p-3 + amber-50/white-10 variants) and appending w-full cursor-pointer text-left transition-colors focus-visible:ring-2 ring-white/80 + subtle hovers (hover:bg-white/15 / hover:border-amber-400 hover:bg-amber-100/80); single behavior: click = mark read (persist + state) — no toggle, no dimming; aria-label = "{title} (pengumuman baru, klik untuk menandai sudah dibaca)" ONLY while unread, else title only; (4) amber "Baru" mini-badge (inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-bold + Sparkles size-3 aria-hidden) with per-context variants: bg-amber-400 text-amber-950 on gradient cards, bg-emerald-600 text-white on PENTING amber-50 cards; badge disappears immediately on read (state update), card stays full opacity; (5) above the list when ≥1 unread among the 2 shown: "{n} pengumuman baru" text-[11px] font-medium text-amber-300 + BellDot size-3 aria-hidden, and a supplementary "Tandai semua dibaca" text button (text-[11px] px-1 py-1, underline-offset-2 hover:underline hover:text-white, focus-visible ring) that marks ALL shown announcements read; count line + button unmount entirely when count hits 0.
- Browser E2E (agent-browser, parent login via Masuk → "Wali — Budi Santoso" quick-fill → Masuk): fresh visit (localStorage pre-cleared) → "2 pengumuman baru" + both cards show "Baru" (DOM probe: badgeTexts ["Baru","Baru"], btnCount 3 in hero section); computed-style probe: normal-card badge bg lab(80.2 16.6 99.2)≈amber-400 / text amber-950, PENTING-card badge bg lab(55.0 -49.9 15.9)≈emerald-600 / text rgb(255,255,255) ✓; click "Pembayaran Iuran Bulan Ini" → its badge + aria-label suffix gone, count line → "1 pengumuman baru", localStorage simadji.readAnnouncements = ["cmtufise6003zq12wiqmc7b1i"]; "Tandai semua dibaca" → all badges gone, line/button unmounted, storage = both ids ["cmtufise6003zq12wiqmc7b1i","cmtufise5003xq12w9ikoqh03"]; reload → badges stay gone (persisted); eval localStorage.clear() + reload (session also cleared → re-login) → "2 pengumuman baru" + both "Baru" badges return (new-visitor state verified). Screenshot /tmp/ann-baru-badges.png. agent-browser errors → EMPTY; console → only React-DevTools info + HMR logs, zero errors/warnings (no hydration mismatch). No data modified (login POST + reads only).
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit 2>&1 | grep -vE "examples/|skills/" → empty (only the 4 pre-existing scaffold errors filtered). Deterministic: ids are stable cuids, no Date.now/Math.random in render, no localStorage access during render (mount timer + handlers only).

Stage Summary:
- File changed (only): src/components/parent/parent-portal.tsx (+~90/-12). Public portal untouched; no API/schema/data changes; no new packages.
- Behavior contract: read-state is client-only under localStorage key `simadji.readAnnouncements` (JSON array of announcement ids, dedup, cap 100 dropping oldest); hydration-safe (SSR/first render = empty, hydrated via deferred timer); unread hero announcement = "Baru" badge (amber-400/amber-950 on gradient, emerald-600/white on PENTING) + "(pengumuman baru, klik untuk menandai sudah dibaca)" aria-label suffix; clicking a card or "Tandai semua dibaca" removes badges instantly without dimming cards and persists across reloads; "{n} pengumuman baru" amber-300 line with BellDot appears only while ≥1 shown announcement is unread; corrupt storage values degrade to empty (try/catch).
- QA results: lint clean; tsc filtered clean; full browser E2E pass (fresh badges → per-card read → mark-all → reload persistence → storage-clear restore) with zero console/page errors.

---
Task ID: 15-a
Agent: quick-setoran (frontend-styling-expert)
Task: "Quick Setoran" Sheet dari kartu Progres Target di guru-overview — catat setoran tanpa navigasi, konsumsi kontrak targetJustReached (Task 14-a).

Work Log:
- Read worklog.md (head 120 + tail 200), src/components/admin/guru-overview.tsx FULLY, student-detail-drawer.tsx (Sheet idiom: side=right, sm:max-w-md, header stone-50/60, sr-only description), hafalan-admin.tsx (datalist surah + toast wording idiom), src/lib/hafalan-utils.ts (JUZ30_SURAHS/TargetProgress), src/lib/api-client.ts (apiSend), src/app/api/hafalan/route.ts (POST contract: surah+ayatRange required server-side), ui primitives (sheet/select/input/textarea/label/badge/button).
- EDIT src/components/admin/guru-overview.tsx ONLY: (1) header comment updated; imports +Loader2/+PencilLine/+Target, +apiSend, +JUZ30_SURAHS/+type TargetProgress, +Input/Label/Textarea/Sheet*/Select*; (2) NEW file-local QuickSetoranSheet (mounted once inside main return, before closing div): Sheet side=right flex w-full flex-col gap-0 p-0 sm:max-w-md (exact drawer idiom), SheetTitle "Catat Setoran Cepat" + sr-only SheetDescription, student header (size-12 emerald-700 initials avatar, fullName, emerald kelas chip, amber Target chip "Target: {targetName} · {reached}/{position}" — tp diturunkan dari data TERKINI parent, bukan snapshot); form: Nama Surah* Input list="quick-surah-list" (38 option dari JUZ30_SURAHS, key=s.no, idiom datalist hafalan-admin), Rentang Ayat* Input placeholder "1-7", Jenis Select TAHFIDZ/TAHSHIN/MURAJAAH default TAHFIDZ, Nilai (opsional) number 0-100 (clamp+round+NaN-safe), Catatan ustadz (opsional) Textarea rows=2, submit full-width emerald-700 min-h-11 "Simpan Setoran" dengan Loader2 pending; (3) reset-on-open effect ala drawer (guard !open||!student, deps [open, student?.id]) → tiap buka selalu form bersih, state tetap tampil selama animasi tutup; (4) validasi: surah+ayatRange required → submit disabled saat kosong, helper amber inline "wajib diisi" (aria-live polite) setelah field diisi lalu dikosongkan; (5) submit → apiSend POST /api/hafalan {studentId, surahName, ayatRange, type, grade: number|undefined, teacherNote: string|undefined}; sukses → tutup sheet + toast "Setoran tercatat" (nama santri + QS surah ayat) atau saat response.targetJustReached===true toast perayaan "🎉 Target tercapai!" / "MasyaAllah! {nama} menyempurnakan target {targetName}." (wording idiom hafalan-admin), lalu onSaved → load(true); gagal → toast "Gagal" + pesan error, sheet TETAP terbuka; (6) load() diberi param quiet=false (default) — quiet=true melewati skeleton/error penuh & hanya setData saat sukses (refresh senyap, idiom parent-portal) sehingga bar progres nudge card update instan tanpa flash halaman; (7) per-row trigger: tombol ghost icon PencilLine size-9 shrink-0 text-stone-400 hover:bg-emerald-100 hover:text-emerald-700 aria-label "Catat setoran {nama}" di ujung tiap li target row; (8) state quickStudentId + derive quickStudent (data.students.find)/quickTp (targetProgress dari hafalanByStudent) → data sheet selalu segar setelah reload (poin 5 task).
- Keputusan: ukuran tombol baris size-9 (36px) sesuai spesifikasi eksplisit task ("size-8 or size-9 icon button") & kepadatan baris — bukan min-h-11 umum; icon submit BookMarked (idiom tombol hafalan-admin), trigger baris PencilLine.

Stage Summary:
- Files changed (exactly 1): src/components/admin/guru-overview.tsx (+~291 lines: QuickSetoranSheet + wiring). Tidak ada file lain/API/schema/package baru.
- UI contract: QuickSetoranSheet file-local ({student, tp, open, onOpenChange, onSaved}); open diturunkan dari quickStudentId!==null; onSaved=() => void load(true); sheet mengonsumsi kontrak POST /api/hafalan Task 14-a ({targetJustReached: boolean, targetName: string|null}); datalist id="quick-surah-list"; semua kontrol ≥min-h-11 kecuali icon-button baris (size-9 per spec task); palette emerald/amber/stone; aria-label pada icon button; role=dialog otomatis dari Radix; hydration-safe (tanpa Date.now/Math.random di render).
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit | grep -vE "examples/|skills/" → kosong (exit 1, zero matches); GET /api/hafalan → 200, baseline Hamzah (cmtufisd70016q12wwgsqzies) = 0 rows; POST bukti jalur sukses 1 row {Al-Fatihah, 1-7, TAHFIDZ} → {"id":"cmtumx2co000mq15lllgc5tty","targetJustReached":true,"targetName":"Al-Fatihah"} (kontrak perayaan terbukti); DELETE ?id= → {"success":true}; baseline pulih (Hamzah 0 rows, total 19); GET / → 200, dev.log bersih; git status src/: hanya guru-overview.tsx milik saya — registrations route/admin + parent-portal adalah pekerjaan agen paralel in-flight, tidak saya sentuh.

---
Task ID: 15 (cron review round)
Agent: main (Z.ai Code) + 3 subagents (15-a/15-b/15-c)
Task: Round-15 — QA sweep, guru quick-setoran sheet, PPDB accept credentials panel, parent announcement read-state badges

Work Log:
- Reviewed worklog (round 14 complete, 621 lines). QA sweep: lint clean, tsc clean (only pre-existing examples/skills errors), dev.log clean, GET / + /api/stats 200. Browser E2E baseline: admin overview (PPDB 6-week chart intact), guru portal (nudge card), parent portal (Rekap Kehadiran + Target strip) — ALL zero console errors. STABLE → features.
- [15-a subagent] Quick Setoran Sheet in guru-overview.tsx (+~291): every "Progres Target Santri" row gains ghost PencilLine icon-button (aria-label "Catat setoran {nama}") opening Sheet (side=right sm:max-w-md, student-detail-drawer idiom) with student header (initials, class chip, amber "Target: X · r/p" chip) + compact form (surah Input + 38-name datalist, ayat, Jenis Select, nilai 0-100 clamped, catatan) + min-h-11 emerald submit (disabled until surah+ayat). POST /api/hafalan via apiSend; success closes sheet + toast "Setoran tercatat", targetJustReached:true → celebration toast "🎉 Target tercapai!" ("MasyaAllah! {nama} menyempurnakan target {target}."); load(true) quiet-refresh updates the nudge progress bar in place. Form resets per open; inline amber helpers aria-live.
- [15-b subagent] PPDB accept credentials: PUT /api/registrations DITERIMA-new-student response now {registration, student, credentials:{nis, email: parent.email, tempPassword:'ortu123'}}; duplicate-childName path returns credentials:null (documented). registrations-admin.tsx submitAccept keeps dialog open and swaps to success state: emerald CheckCircle2 "Pendaftaran Diterima" + credentials card (border-emerald-200 bg-emerald-50/60) with mono NIS/Email/Sandi rows each with min-h-11 copy button (Copy→Check "Tersalin" 2s, clipboard API + execCommand fallback), KeyRound note "Kredensial juga telah dikirim ke wali via WhatsApp.", credentials:null → amber "Santri dengan nama serupa sudah terdaftar" panel; single emerald "Selesai" resets all state + load(). Reject/delete untouched.
- [15-c subagent] Parent announcement read-state (parent-portal.tsx only, +~90/−12): localStorage key simadji.readAnnouncements (cuid array, dedup, cap 100, hydration-safe read-after-mount in setTimeout(10ms) w/ cleanup + try/catch). Hero announcement divs → real <button type=button> (text-left, focus-visible ring, hover) = click to mark read; unread cards show "Baru" mini-badge (gradient cards bg-amber-400/amber-950, PENTING amber-50 cards bg-emerald-600/white, Sparkles size-3) + aria-label "(pengumuman baru, klik untuk menandai sudah dibaca)"; "{n} pengumuman baru" count line (BellDot, amber-300 on gradient) + "Tandai semua dibaca" text button when ≥1 unread; badges vanish instantly, cards never dim, persists across reloads.
- [ain integration E2E] Guru: logged in as Fatimah → nudge row button "Catat setoran Hamzah Abdurrahman" → sheet opens w/ Target chip + disabled submit until filled → Al-Fatihah 1-7 TAHFIDZ → submit → celebration toast "🎉 Target tercapai! … menyempurnakan target Al-Fatihah." + progressbar 0%→100% + "Target tercapai" chip in row; zero console errors. PPDB (admin): temp reg QA-Integ-Anak (PPDB-2025-0005) created via curl → Terima dialog → Terima Santri → success panel renders "Kredensial akun santri" group w/ Salin NIS/Email/Sandi + WA note → Selesai closes + table shows DITERIMA; zero console errors. Parent: fresh storage → "2 pengumuman baru" + both Baru badges → click marks read (count 2→1) → reload persists (read one stays read) → zero errors.
- [ain cleanup] Deleted browser-E2E setoran row (Hamzah back to 0) + its 2 WA rows; deleted temp PPDB reg/student/user via API DELETE + 2 QA WA rows via temp bun script (scripts-tmp removed). Baseline re-verified: registrations 4 / students 9 / users 12 / notifications 13 / Hamzah hafalan 0. Final lint + tsc clean after all 3 agents; dev.log clean.

Stage Summary:
- Round 15 shipped: guru "Catat Setoran Cepat" Sheet from target nudge rows (with live target-progress refresh + celebration toast), PPDB accept credentials result panel (copyable NIS/email/temp-password, duplicate-safe), parent announcement read-state ("Baru" badges + count line + mark-read, localStorage-persisted, hydration-safe).
- Contracts: POST /api/hafalan response unchanged (targetJustReached consumed by guru sheet too); PUT /api/registrations DITERIMA now returns credentials:{nis,email,tempPassword}|null; localStorage key simadji.readAnnouncements = string[] of announcement ids (cap 100).
- Files changed (3 + 1 API): src/components/admin/guru-overview.tsx, src/app/api/registrations/route.ts (PUT only), src/components/admin/registrations-admin.tsx, src/components/parent/parent-portal.tsx. No schema changes, no new deps.
- QA: lint clean; tsc clean (only pre-existing examples/skills errors); full browser E2E of all 3 features with zero console errors; DB fully restored to pre-round baseline after QA (verified counts).
- Next-step candidates (priority order): (1) guru attendance quick-mark from sessions card (mirroring quick setoran), (2) parent portal export child report (print-friendly recap), (3) admin Santri drawer "add setoran" shortcut, (4) dark-mode audit, (5) public materials search/filter chips.
---
Task ID: 16-a
Agent: quick-absen-sheet (general-purpose sub agent)
Task: Tambah Sheet "Absen Cepat" (quick attendance) di overview GURU, dibuka dari tiap baris kartu "Sesi Aktif Kelas Saya", mengikuti idiom QuickSetoranSheet (Task 15-a) di file yang sama.

Work Log:
- Read worklog.md tail 150 (pola Task 15-a, 14-a, 14-c) + src/components/admin/guru-overview.tsx penuh (810 lama) + kontrak: src/lib/types.ts (SessionItem/AttendanceRecord/Student), src/lib/api-client.ts (apiGet/apiSend throw on error), src/app/api/attendance/route.ts (GET ?sessionId= → AttendanceRecord[] {id, sessionId, studentId, status, note, createdAt, student{id,fullName,nis}, className}; POST {sessionId, records:[{studentId,status,note?}]} → {success,count}, upsert + WA ke wali per santri), attendance-admin.tsx (STATUSES warna on/off persis), student-detail-drawer.tsx (idiom fetch-per-open + cancelled flag), ui/button.tsx (ring focus-visible bawaan shadcn).
- EDIT 1 file saja: src/components/admin/guru-overview.tsx (810 → 1092 baris, +282). Tidak ada perubahan API/schema/file lain.
- Komponen file-local QuickAbsenSheet({session, students, open, onOpenChange, onSaved}): Sheet side="right" sm:max-w-md flex-col p-0; header stone-50/60 dengan tile emerald pastel size-9 rounded-xl bg-emerald-100 text-emerald-700 + ClipboardCheck, judul "Absen Cepat — {className}", deskripsi "{topic||'Tanpa topik'} · formatShortDate(date)" + chip mono kode sesi (border-stone-200 bg-stone-50 font-mono text-xs tracking-widest).
- Prefill: useEffect per-open/ganti-sesi (deps [open, sessionId], guard `open && sessionId`, cancelled flag) → GET /api/attendance?sessionId= → rows: Record<studentId, {status, note}> (note null → ''); gagal → loadFailed=true, tetap bisa isi dari awal + hint amber aria-live "Gagal memuat absensi tersimpan — isi dari awal"; loading → 4 Skeleton h-14 rounded-xl.
- Baris santri (list divide-y divide-stone-100 dalam container flex-1 min-h-0 overflow-y-auto + SCROLL_AREA): avatar inisial size-9 (emerald-100 bila status terisi, stone-100 bila kosong) + nama truncate + selector 4 segmen grid grid-cols-4 gap-1, tombol h-9 rounded-lg border text-xs font-semibold transition-colors, kelas on/off PERSIS dari STATUSES attendance-admin (HADIR emerald-700, IZIN amber-500, SAKIT orange-500, ALPA red-600; off border-stone-200 bg-white text-stone-600 + hover:border sesuai status), aria-pressed + aria-label `Tandai {nama} {label}` + ring focus-visible ala shadcn (border-ring ring-ring/50 ring-[3px]); klik status aktif = batal pilih. Input catatan h-9 text-xs "Catatan untuk orang tua (opsional)" muncul hanya bila status terisi ≠ HADIR. Empty state santri kosong + CTA tetap bisa tutup.
- Footer sticky (border-t border-stone-100 p-4 pt-3 space-y-2): ringkasan live aria-live="polite" ("2 Hadir · 1 Izin · 1 belum diisi", dihitung murni dari state — aman hidrasi) + Button min-h-11 w-full bg-emerald-700 hover:bg-emerald-800 disabled bila 0 status terisi / saving, Loader2 spin saat saving.
- Submit: apiSend POST /api/attendance {sessionId, records: hanya baris berstatus → {studentId, status, note: trim()||undefined}}; sukses → toast "Absensi tersimpan" desc "{n} santri · {className}[ · {m} belum diisi]", tutup sheet, onSaved()=load(true) quiet (badge {hadir}/{total} kartu sesi ikut segar); gagal → toast "Gagal" + pesan, sheet TETAP terbuka.
- Wiring kartu Sesi Aktif: tombol salin kode dibungkus `<div className="flex shrink-0 items-center gap-1">` + tombol kedua outline size-11 ikon UserCheck text-emerald-700 hover:bg-emerald-50 hover:border-emerald-300, aria-label `Absen cepat ${s.className}`; state quickSessionId + derivasi quickSession (ownSessions.find) & quickAbsenStudents (ownStudents filter classId sesi && status AKTIF) — dari data terkini, bukan snapshot.
- QA: bun run lint = bersih; bunx tsc --noEmit (filter examples/|skills/) = kosong; curl GET /api/stats → 2 sesi aktif; GET /api/attendance?sessionId=cmtufisdm002eq12wfv1xqlut → 2 baris (HADIR/ALPA, note null) — mapping prefill sesuai payload; POST test ke sesi cmtuh2cxd0003q1wittjjtt57 dengan status+note IDENTIK (upsert no-op: IZIN "Acara keluarga" + HADIR) → {success:true,count:2}, data absensi tidak berubah; dev.log 30 baris terakhir tanpa error (0 "Error").

Stage Summary:
- Kontrak dipakai (tanpa perubahan API): GET /api/attendance?sessionId= → AttendanceRecord[] (unordered relatif, take 200); POST /api/attendance {sessionId, records:[{studentId, status:'HADIR'|'IZIN'|'SAKIT'|'ALPA', note?}]} → {success:true, count}, upsert per (sessionId, studentId) + 1 row Notification WHATSAPP/SENT per santri terkirim (HADIR dan non-HADIR).
- Artefak: QuickAbsenSheet file-local di guru-overview.tsx; ABSEN_STATUSES (kelas warna persis attendance-admin); tipe AttStatus/QuickAbsenRow file-local.
- Catatan integrator (opsional dirapikan): POST QA menghasilkan 2 log WA demo — Notification id cmtunmy6y001bq15laumygvvq (Zahra Nur Aini HADIR, 2026-09-09T22:13:05.051Z) dan cmtunmy6s0017q15lygpgw15g (Muhammad Rizky Ramadhan IZIN, ...044Z). Baris Attendance TIDAK berubah (upsert status identik). Tidak ada file/schema/API lain yang disentuh.
- Next-step candidates: (1) pilihan "tandai semua HADIR" massal di footer sheet, (2) badge keterlambatan/tepat waktu di Absen Cepat, (3) histori absensi per sesi (sheet riwayat), (4) dark-mode audit komponen sheet baru.
---
Task ID: 16-c
Agent: general-purpose (sub-agent, Round 16 — 16-c)
Task: Upgrade PUBLIC materials catalog (Materi section) with text search, sort Select, and live result count — detail-polish for the public portal. Exactly 1 file modified: src/components/public/materials-section.tsx; no API changes.

Work Log:
- Read worklog tail (120+ lines) for idioms; read materials-section.tsx fully; verified Material fields from src/lib/types.ts + live payload GET /api/materials (title, type, url, description, category, classId, teacherId, createdAt, teacher{fullName}, class{name} | null) — search/sort keys exist in payload; API already returns createdAt DESC (default sort "Terbaru" consistent).
- Matched repo search-input idiom (whatsapp-log.tsx/students-admin.tsx): relative wrapper + absolute left-3 Search icon size-4 text-stone-400 pointer-events-none + Input rounded-xl border-stone-200 bg-white pl-9 (shadcn Input ref-as-prop works under React 19 / ComponentProps<"input">).
- EDIT materials-section.tsx ONLY (+124/−21): (1) imports +useRef, +Search, +X, +Input; (2) state query + sort ('TERBARU' default) + searchRef; (3) useMemo extended: trim+toLocaleLowerCase('id-ID') haystack over title+description+category (q==='' passthrough), then [...list].sort switch — TERBARU createdAt desc / TERLAMA asc (ISO strings → localeCompare), JUDUL_ASC/DESC localeCompare 'id' — deps [materials, category, classId, query, sort], pure (no wall-clock → hydration-safe); (4) filter row restructured: chips group unchanged, right control group flex w-full flex-wrap justify-center gap-2 lg:w-auto lg:justify-end holding search (relative w-full sm:w-[240px], min-h-11 pl-9 pr-10, X clear Button ghost size-8 absolute right-0.5 aria-label "Bersihkan pencarian" visible when query!=='' → setQuery('')+refocus input), sort Select (aria-label "Urutkan materi", w-[170px] min-h-11 rounded-xl, items Terbaru/Terlama/Judul A-Z/Judul Z-A), kelas Select kept (trigger +min-h-11 rounded-xl for row coherence); outer container mb-8→mb-6 + lg:flex-wrap; (5) result count <p aria-live="polite" text-right text-xs text-stone-500 mb-6> "{n} materi ditemukan"/"1 materi ditemukan", rendered only when !loading && !error; (6) empty state: query non-empty → `Tidak ada materi yang cocok dengan pencarian "{query}"` + Button outline min-h-11 "Hapus pencarian" (resetFilters: query='' + category/classId='SEMUA' + refocus input); generic state untouched otherwise; footer "Menampilkan X dari Y" intact.

Stage Summary:
- QA all green: bun run lint clean; bunx tsc --noEmit (filtered) silent; curl /api/materials 200 sample fields {id,title,type,url,description,category,classId,teacherId,createdAt,teacher,class}; dev.log tail clean (✓ Compiled, GETs 200).
- Live browser QA (agent-browser on :3000, public portal): search "tajwid" → 3 cards + "3 materi ditemukan" + X visible; sort Judul A-Z → first card "Panduan Tajwid Dasar (Iqra 1-2)"; "zzzqqq" → "0 materi ditemukan" + custom empty state `Tidak ada materi yang cocok dengan pencarian "zzzqqq"` + Hapus pencarian → restores 5 cards + focus back to input; X clear → query cleared, 5 cards, focus on input, button unmounts. a11y labels verified in AX tree: textbox "Cari materi", combobox "Urutkan materi", button "Bersihkan pencarian". No page errors. (One transient "covered by span" agent-browser ref artifact — disproven via elementFromPoint hit-test: button itself is topmost after scroll; not a UI bug.)
- Contracts: sort values 'TERBARU'|'TERLAMA'|'JUDUL_ASC'|'JUDUL_DESC' (string state); searchActive = query.trim()!=='' gates empty-state copy; no new deps; Indonesian UI; stone/emerald palette only.

---
Task ID: 16-b
Agent: attendance-rekap (general-purpose subagent)
Task: Kartu "Rekap Absensi Bulanan" — agregasi per santri + ekspor CSV di view Absensi ADMIN/GURU (attendance-admin.tsx saja, tanpa perubahan API/schema).

Work Log:
- Baca worklog.md (idiom round 11-15), attendance-admin.tsx penuh (494 baris), overview.tsx (csvCell/csvDate/csvFileStamp/downloadCsv + STATUS_DOTS + statusBadgeClass), hafalan-admin.tsx (idiom CSV: filename + rows + toast), student-detail-drawer.tsx (kompromi bucketing createdAt + initials + gradeCircleClass), ui/progress.tsx & ui/table.tsx & ui/card.tsx (CardAction), api attendance/students routes (kontrak terverifikasi).
- EDIT src/components/admin/attendance-admin.tsx SAJA (+323/−3, kini 814 baris): (1) import +useMemo, +BarChart3/ClipboardList/Inbox/Download, +Progress, +CardAction, overview import ditambah downloadCsv; (2) helper modul: STATUS_COUNT_KEY (map status→kolom), RECAP_CHIPS (dot bg-emerald-500/amber-500/orange-500/red-500 = STATUS_DOTS overview), recapPctBadgeClass (>=85 emerald, >=70 amber, else red — ambang grade-badge), formatPct (id-ID desimal koma 1dp → "83,3"), initials(), interface RecapRow; (3) state rekap terpisah dari form Buka Sesi: recapClassId/recapMonth ('none')/recapData{records,roster}/recapLoading/recapError/recapRetry; (4) monthOptions useMemo([]) — 6 bulan terakhir dihitung SEKALI per mount, label toLocaleDateString('id-ID',{month:'long',year:'numeric'}) ("September 2026"), value "2026-09", tanpa pembacaan wall-clock saat render hasil (hydration-safe, kompromi sama dgn useState(todayISO()) yang sudah ada); (5) SATU effect [recapClassId,recapMonth,recapRetry] guard keduanya !=='none' → Promise.all(apiGet /api/attendance?classId=, apiGet /api/students?classId=) + flag cancelled, Skeleton 5×h-12 saat loading, Alert destructive+Coba Lagi (recapRetry++) saat gagal — idiom persis file ini; (6) agregasi useMemo: filter records createdAt.slice(0,7)===bulan (session dilepas API — kompromi student-detail-drawer), totals per status, pertemuan=Set(sessionId).size, rows = SEMUA santri kelas (AKTIF dulu, lalu nama A-Z; tanpa catatan = nol), pct per santri = hadir/pertemuan (1dp), rate = hadir/totalRecords; (7) UI: Card full-width rounded-2xl border-stone-200 shadow-sm SETELAH kartu Riwayat; CardHeader = tile BarChart3 size-9 rounded-xl bg-emerald-100 text-emerald-700 + judul/deskripsi + CardAction tombol outline "Unduh CSV" (min-h-11, Download, aria-label "Unduh rekap CSV", disabled saat tabel kosong); CardContent: controls flex-col sm:flex-row gap-3 (Select kelas placeholder "Pilih kelas" + Select bulan placeholder "Pilih bulan", dua-duanya min-h-11 w-full sm:w-60, aria-label "Kelas/Bulan rekap absensi"); empty state tanpa pilihan → dashed box ClipboardList text-stone-300 "Pilih kelas dan bulan untuk melihat rekap"; zero catatan bulan → dashed box Inbox "Belum ada catatan kehadiran pada bulan ini"; strip aria-live="polite" 4 chip (dot warna + count text-lg font-bold + label text-xs text-stone-500, border-stone-100 bg-stone-50/60 px-3 py-2) + chip ke-5 "Tingkat Kehadiran" dgn Progress emerald (className="[&>div]:bg-emerald-600", track bg-stone-200); tabel Table min-w-[640px]: Santri (avatar inisial size-8 emerald-100 + nama + NIS mono), Hadir/Izin/Sakit/Alpa (center), % Hadir (Badge recapPctBadgeClass, format "83,3%"), row hover:bg-stone-50/60; thead+th sticky top-0 z-10 bg-white; area scroll = kontainer internal Table via arbitrary-variant wrapper "[&_[data-slot=table-container]]:max-h-80 overflow-y-auto pr-1 [scrollbar-width:thin] [&::-webkit-scrollbar]:w-1.5 thumb:rounded-full bg-stone-300" (WAJIB: div bawaan shadcn Table ber-data-slot itu scrollportnya — sticky di wrapper luar tidak bekerja karena table-container punya overflow-x-auto→jadi scroll container tersendiri); (8) exportRecapCsv: slug kelas (lowercase, non-alnum→'-'), filename rekap-absensi-{slug}-{YYYY-MM}.csv (YYYY-MM = bulan terpilih, idiom csvFileStamp: nama bertanggal via helper), kolom NIS;Nama;Kelas;Hadir;Izin;Sakit;Alpa;Total Pertemuan;Persentase Hadir (%), persentase formatPct → "83,3"; downloadCsv(filename, rows) + toast "Ekspor CSV berhasil".
- Catatan implementasi: escape per sel DILAKUKAN downloadCsv (rows.map(csvCell) internal, ';'+BOM+quote-doubling) — persis idiom hafalan-admin round 11; TIDAK membungkus sel dgn csvCell manual utk menghindari double-escape (nilai berisi ';'/'"' akan rusak jika lolos csvCell dua kali). csvDate tidak dipakai (rekap bulanan tanpa kolom tanggal).

Stage Summary:
- File changed (exactly 1): src/components/admin/attendance-admin.tsx (+323/−3 → 814 baris). Tidak ada perubahan API/schema/dep; guru-overview.tsx & materials-section.tsx & db/custom.db adalah pekerjaan paralel agen lain, tidak disentuh.
- UI contract: kartu Rekap Absensi Bulanan muncul di bawah Riwayat Kehadiran Terbaru (view Absensi ADMIN & GURU); fetch hanya saat kelas+bulan dipilih (1 effect, Promise.all); konsumsi GET /api/attendance?classId= → AttendanceRecord[] (take 200, orderBy createdAt desc, tanpa session date) + GET /api/students?classId= → Student[] (semua santri kelas tampil, nol bila tanpa catatan); angka rekap = fungsi murni useMemo (records,roster,bulan); CSV: rekap-absensi-{kelas-slug}-{YYYY-MM}.csv via downloadCsv (csvCell per sel, ';', BOM); tanpa Date.now/Math.random di render; bulan = useMemo sekali per mount.
- QA: bun run lint → clean; bunx tsc --noEmit | grep -vE "examples/|skills/" → kosong; curl (GET only, tanpa tulis DB): /api/classes → 4 kelas; /api/attendance?classId= & /api/students?classId= utk 4 kelas → array (Tahfidz A: 4 record/2 sesi/2 santri bulan 2026-09; Iqra 4: 2/1/2; Al-Qur'an A: 2/1/2; Iqra 1: 0-1 — ada agen paralel yang menulis row); dev.log 30 baris terakhir = 200 + prisma:query, tanpa error. Browser E2E (agent-browser, login admin quick-fill): pilih Tahfidz A + September 2026 → strip Hadir 3/Izin 0/Sakit 0/Alpa 1/Tingkat Kehadiran 75% (progressbar emerald, transform -25% — cocok hitung manual 3/4); tabel 2 santri (Fathan 1-0-0-1 → 50% badge merah; Alfatih 2-0-0-0 → 100% badge emerald), urut nama, AKTIF semua; computed style: table-container max-height 320px + overflow-y auto + scrollbar-width thin, thead/th sticky top-0 z-10 bg-white, sticky terverifikasi mekanis (inject 8 baris + scrollTop 200 → thead top == container top); Agustus 2026 → Inbox "Belum ada catatan kehadiran pada bulan ini" + tombol ekspor disabled; klik Unduh CSV → toast "Ekspor CSV berhasil · Rekap 2 santri tersimpan di rekap-absensi-tahfidz-a-2026-09.csv"; opsi bulan September→April 2026 (id-ID); zero console/page errors; manipulasi DOM uji sticky dipulihkan, tanpa perubahan data.

---
Task ID: 16 (cron review round, integration)
Agent: main (Z.ai Code) + 3 subagents (16-a/16-b/16-c)
Task: Round-16 — QA sweep, guru quick-absen sheet, monthly attendance recap + CSV, public materials search/sort

Work Log:
- Reviewed worklog (round 15 complete, 692 lines). QA sweep: lint clean, tsc clean (only pre-existing examples/skills errors), dev.log clean, GET / + /api/stats 200, baseline APIs verified (9 students / 19 hafalan / 4 registrations / 13 notifications / 2 active sessions). Browser E2E baseline: public landing, admin (Ringkasan/PPDB/Hafalan/Keuangan), guru (Fatimah), parent (Budi, read-state badges + Rapor Santri print dialog intact) — ALL zero console errors. NOTE: parent "Rapor Santri" print feature and WA-log filter/CSV + admin student drawer already existed from earlier rounds (candidates 2/3/5 of round-15 list were done) → chose remaining gaps.
- [16-a subagent] QuickAbsenSheet in guru-overview.tsx (+281): per active-session row second icon button UserCheck "Absen cepat {className}" opens Sheet (right, sm:max-w-md) — prefill from GET /api/attendance?sessionId= (skeleton rows; amber aria-live hint on prefill failure), student rows with 4-segment status selector (exact attendance-admin on/off color families: HADIR emerald-700 / IZIN amber-500 / SAKIT orange-500 / ALPA red-600, aria-pressed + aria-label, h-9), note Input (h-9) revealed only when status≠HADIR, sticky footer with aria-live summary ("n Hadir · m belum diisi") + min-h-11 emerald submit; POST /api/attendance {sessionId, records[]} → toast "Absensi tersimpan · n santri · kelas" → load(true) quiet refresh (hadir/total badge updates in place).
- [16-b subagent] "Rekap Absensi Bulanan" card in attendance-admin.tsx (+323/−3): kelas + bulan (last 6 months, useMemo([]) — hydration-safe) Selects → Promise.all(GET attendance?classId + students?classId) with file's skeleton/alert idioms → aria-live summary strip (4 status-dot chips + Tingkat Kehadiran emerald Progress), per-santri sticky-thead table (all class students, zeros included; % Hadir badge ≥85 emerald / ≥70 amber / else red), CSV export rekap-absensi-{kelas}-{YYYY-MM}.csv (NIS;Nama;Kelas;Hadir;Izin;Sakit;Alpa;Total Pertemuan;Persentase Hadir % — id-ID comma decimals via shared downloadCsv). Month bucket = createdAt.slice(0,7) (API strips session date — documented).
- [16-c subagent] materials-section.tsx (+124/−21): text search Input (pl-9 Search icon, clear X refocuses, aria-label "Cari materi"), id-ID case-insensitive match over title+description+category, sort Select (Terbaru/Terlama/Judul A-Z/Z-A via localeCompare 'id'), aria-live "{n} materi ditemukan" count, search-specific empty state ("Tidak ada materi yang cocok…" + "Hapus pencarian" reset button), responsive wrap.
- [ain integration E2E] Guru quick-absen as Ustadz Yusuf (Tahfidz A owner): sheet prefill correct (Alfatih HADIR, Fathan ALPA) → Fathan→Hadir → submit → toast "Absensi tersimpan 2 santri · Tahfidz A" + card badge 1/2→2/2 via quiet refresh → restored Fathan→Alpa → API verified exact baseline (HADIR/ALPA, notes null). Recap card as guru: Tahfidz A + September 2026 → chips 3/0/0/1 + Tingkat Kehadiran 75% + per-santri rows (Fathan 50% red, Alfatih 100% emerald) — matches manual math. Materials as public: "tajwid" → 3 cards + "3 materi ditemukan" + X visible. Zero console errors throughout.
- [ain cleanup] Deleted 6 QA-created WA notification rows (4 from integration E2E + 2 left by 16-a QA) via temp bun script (scripts-tmp removed); notifications back to 13. Attendance/status baselines verified unchanged. Final lint + tsc clean across all 3 agents; dev.log clean; git status shows exactly the 3 intended files.

Stage Summary:
- Round 16 shipped: guru "Absen Cepat" Sheet from active-session card (prefill + upsert + live badge refresh), admin/guru "Rekap Absensi Bulanan" (per-santri monthly aggregation + % badges + CSV export), public materials search/sort/count polish.
- Contracts unchanged: GET/POST /api/attendance, GET /api/students?classId, GET /api/materials — frontend-only round, no API/schema/deps changes.
- Files changed (3): src/components/admin/guru-overview.tsx, src/components/admin/attendance-admin.tsx, src/components/public/materials-section.tsx.
- QA: lint clean; tsc clean (only pre-existing examples/skills errors); full browser E2E of all 3 features zero console errors; DB fully restored to baseline (13 notifications, attendance rows identical).
- Next-step candidates (priority order): (1) dark-mode audit across all portals, (2) guru classes view polish (per-class detail summary), (3) parent-side monthly recap link into attendance CSV/print, (4) admin hafalan target bulk-edit (set target for whole class), (5) public PPDB brochure download section.
