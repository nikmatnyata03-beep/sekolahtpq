   640→- Files changed (exactly 2): src/app/api/registrations/route.ts (+3 lines/comments), src/components/admin/registrations-admin.tsx (+~120/-20). No other files; no new packages; dev server not started/stopped.
   641→- QA: bun run lint → clean (exit 0); bunx tsc --noEmit 2>&1 | grep -vE "examples/|skills/" → empty; curl E2E assertions PASS (credentials shape + WA log) with full baseline restoration (4/9/12/13, zero QA residue); dev.log clean, GET / → 200.
   642→
   643→---
   644→Task ID: 15-c
   645→Agent: ann-readstate (frontend-styling-expert)
   646→Task: Parent portal hero announcement read-state — localStorage-backed "Baru" badges, per-card mark-read buttons, "n pengumuman baru" line + "Tandai semua dibaca".
   647→
   648→Work Log:
   649→- Read worklog.md (head 120 + tail 200; re-read tail immediately before append — 641 lines now, PPDB-credentials entry landed in parallel, no file overlap), src/components/parent/parent-portal.tsx fully (422 lines pre-edit), types.ts (Announcement: id/title/content/priority/createdAt — cuid id safe as storage value), page.tsx deferred localStorage pattern (setTimeout in effect — the established workaround for the react-hooks/set-state-in-effect rule that a plain mount-effect setState triggers), probed lucide 0.525 (Sparkles/BellDot both exported) and confirmed dev server healthy on :3000.
   650→- Edited src/components/parent/parent-portal.tsx ONLY (no API/backend changes — portal contract untouched): (1) module consts READ_ANNOUNCEMENTS_KEY='simadji.readAnnouncements' + READ_ANNOUNCEMENTS_CAP=100; pure helpers readStoredReadAnnouncements() (getItem + JSON.parse in try/catch → corrupt/missing/unavailable storage = [] ; Array.isArray + typeof string filter, no any) and persistReadAnnouncements(ids) (read-merge-dedupe via Set, slice(-100) drops OLDEST when exceeding cap, setItem in try/catch best-effort; both documented as never-call-during-render); (2) readAnnouncementIds state init [] → SSR + first client render identical (hydration-safe), hydrated in a dedicated useEffect via setTimeout(10ms)+clearTimeout cleanup (page.tsx pattern); (3) hero announcement cards converted div → <button type="button"> keeping ALL existing classes verbatim (flex gap-2.5 rounded-xl border p-3 + amber-50/white-10 variants) and appending w-full cursor-pointer text-left transition-colors focus-visible:ring-2 ring-white/80 + subtle hovers (hover:bg-white/15 / hover:border-amber-400 hover:bg-amber-100/80); single behavior: click = mark read (persist + state) — no toggle, no dimming; aria-label = "{title} (pengumuman baru, klik untuk menandai sudah dibaca)" ONLY while unread, else title only; (4) amber "Baru" mini-badge (inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-bold + Sparkles size-3 aria-hidden) with per-context variants: bg-amber-400 text-amber-950 on gradient cards, bg-emerald-600 text-white on PENTING amber-50 cards; badge disappears immediately on read (state update), card stays full opacity; (5) above the list when ≥1 unread among the 2 shown: "{n} pengumuman baru" text-[11px] font-medium text-amber-300 + BellDot size-3 aria-hidden, and a supplementary "Tandai semua dibaca" text button (text-[11px] px-1 py-1, underline-offset-2 hover:underline hover:text-white, focus-visible ring) that marks ALL shown announcements read; count line + button unmount entirely when count hits 0.
   651→- Browser E2E (agent-browser, parent login via Masuk → "Wali — Budi Santoso" quick-fill → Masuk): fresh visit (localStorage pre-cleared) → "2 pengumuman baru" + both cards show "Baru" (DOM probe: badgeTexts ["Baru","Baru"], btnCount 3 in hero section); computed-style probe: normal-card badge bg lab(80.2 16.6 99.2)≈amber-400 / text amber-950, PENTING-card badge bg lab(55.0 -49.9 15.9)≈emerald-600 / text rgb(255,255,255) ✓; click "Pembayaran Iuran Bulan Ini" → its badge + aria-label suffix gone, count line → "1 pengumuman baru", localStorage simadji.readAnnouncements = ["cmtufise6003zq12wiqmc7b1i"]; "Tandai semua dibaca" → all badges gone, line/button unmounted, storage = both ids ["cmtufise6003zq12wiqmc7b1i","cmtufise5003xq12w9ikoqh03"]; reload → badges stay gone (persisted); eval localStorage.clear() + reload (session also cleared → re-login) → "2 pengumuman baru" + both "Baru" badges return (new-visitor state verified). Screenshot /tmp/ann-baru-badges.png. agent-browser errors → EMPTY; console → only React-DevTools info + HMR logs, zero errors/warnings (no hydration mismatch). No data modified (login POST + reads only).
   652→- QA: bun run lint → clean (exit 0); bunx tsc --noEmit 2>&1 | grep -vE "examples/|skills/" → empty (only the 4 pre-existing scaffold errors filtered). Deterministic: ids are stable cuids, no Date.now/Math.random in render, no localStorage access during render (mount timer + handlers only).
   653→
   654→Stage Summary:
   655→- File changed (only): src/components/parent/parent-portal.tsx (+~90/-12). Public portal untouched; no API/schema/data changes; no new packages.
   656→- Behavior contract: read-state is client-only under localStorage key `simadji.readAnnouncements` (JSON array of announcement ids, dedup, cap 100 dropping oldest); hydration-safe (SSR/first render = empty, hydrated via deferred timer); unread hero announcement = "Baru" badge (amber-400/amber-950 on gradient, emerald-600/white on PENTING) + "(pengumuman baru, klik untuk menandai sudah dibaca)" aria-label suffix; clicking a card or "Tandai semua dibaca" removes badges instantly without dimming cards and persists across reloads; "{n} pengumuman baru" amber-300 line with BellDot appears only while ≥1 shown announcement is unread; corrupt storage values degrade to empty (try/catch).
   657→- QA results: lint clean; tsc filtered clean; full browser E2E pass (fresh badges → per-card read → mark-all → reload persistence → storage-clear restore) with zero console/page errors.
   658→
   659→---
   660→Task ID: 15-a
   661→Agent: quick-setoran (frontend-styling-expert)
   662→Task: "Quick Setoran" Sheet dari kartu Progres Target di guru-overview — catat setoran tanpa navigasi, konsumsi kontrak targetJustReached (Task 14-a).
   663→
   664→Work Log:
   665→- Read worklog.md (head 120 + tail 200), src/components/admin/guru-overview.tsx FULLY, student-detail-drawer.tsx (Sheet idiom: side=right, sm:max-w-md, header stone-50/60, sr-only description), hafalan-admin.tsx (datalist surah + toast wording idiom), src/lib/hafalan-utils.ts (JUZ30_SURAHS/TargetProgress), src/lib/api-client.ts (apiSend), src/app/api/hafalan/route.ts (POST contract: surah+ayatRange required server-side), ui primitives (sheet/select/input/textarea/label/badge/button).
   666→- EDIT src/components/admin/guru-overview.tsx ONLY: (1) header comment updated; imports +Loader2/+PencilLine/+Target, +apiSend, +JUZ30_SURAHS/+type TargetProgress, +Input/Label/Textarea/Sheet*/Select*; (2) NEW file-local QuickSetoranSheet (mounted once inside main return, before closing div): Sheet side=right flex w-full flex-col gap-0 p-0 sm:max-w-md (exact drawer idiom), SheetTitle "Catat Setoran Cepat" + sr-only SheetDescription, student header (size-12 emerald-700 initials avatar, fullName, emerald kelas chip, amber Target chip "Target: {targetName} · {reached}/{position}" — tp diturunkan dari data TERKINI parent, bukan snapshot); form: Nama Surah* Input list="quick-surah-list" (38 option dari JUZ30_SURAHS, key=s.no, idiom datalist hafalan-admin), Rentang Ayat* Input placeholder "1-7", Jenis Select TAHFIDZ/TAHSHIN/MURAJAAH default TAHFIDZ, Nilai (opsional) number 0-100 (clamp+round+NaN-safe), Catatan ustadz (opsional) Textarea rows=2, submit full-width emerald-700 min-h-11 "Simpan Setoran" dengan Loader2 pending; (3) reset-on-open effect ala drawer (guard !open||!student, deps [open, student?.id]) → tiap buka selalu form bersih, state tetap tampil selama animasi tutup; (4) validasi: surah+ayatRange required → submit disabled saat kosong, helper amber inline "wajib diisi" (aria-live polite) setelah field diisi lalu dikosongkan; (5) submit → apiSend POST /api/hafalan {studentId, surahName, ayatRange, type, grade: number|undefined, teacherNote: string|undefined}; sukses → tutup sheet + toast "Setoran tercatat" (nama santri + QS surah ayat) atau saat response.targetJustReached===true toast perayaan "🎉 Target tercapai!" / "MasyaAllah! {nama} menyempurnakan target {targetName}." (wording idiom hafalan-admin), lalu onSaved → load(true); gagal → toast "Gagal" + pesan error, sheet TETAP terbuka; (6) load() diberi param quiet=false (default) — quiet=true melewati skeleton/error penuh & hanya setData saat sukses (refresh senyap, idiom parent-portal) sehingga bar progres nudge card update instan tanpa flash halaman; (7) per-row trigger: tombol ghost icon PencilLine size-9 shrink-0 text-stone-400 hover:bg-emerald-100 hover:text-emerald-700 aria-label "Catat setoran {nama}" di ujung tiap li target row; (8) state quickStudentId + derive quickStudent (data.students.find)/quickTp (targetProgress dari hafalanByStudent) → data sheet selalu segar setelah reload (poin 5 task).
   667→- Keputusan: ukuran tombol baris size-9 (36px) sesuai spesifikasi eksplisit task ("size-8 or size-9 icon button") & kepadatan baris — bukan min-h-11 umum; icon submit BookMarked (idiom tombol hafalan-admin), trigger baris PencilLine.
   668→
   669→Stage Summary:
   670→- Files changed (exactly 1): src/components/admin/guru-overview.tsx (+~291 lines: QuickSetoranSheet + wiring). Tidak ada file lain/API/schema/package baru.
   671→- UI contract: QuickSetoranSheet file-local ({student, tp, open, onOpenChange, onSaved}); open diturunkan dari quickStudentId!==null; onSaved=() => void load(true); sheet mengonsumsi kontrak POST /api/hafalan Task 14-a ({targetJustReached: boolean, targetName: string|null}); datalist id="quick-surah-list"; semua kontrol ≥min-h-11 kecuali icon-button baris (size-9 per spec task); palette emerald/amber/stone; aria-label pada icon button; role=dialog otomatis dari Radix; hydration-safe (tanpa Date.now/Math.random di render).
   672→- QA: bun run lint → clean (exit 0); bunx tsc --noEmit | grep -vE "examples/|skills/" → kosong (exit 1, zero matches); GET /api/hafalan → 200, baseline Hamzah (cmtufisd70016q12wwgsqzies) = 0 rows; POST bukti jalur sukses 1 row {Al-Fatihah, 1-7, TAHFIDZ} → {"id":"cmtumx2co000mq15lllgc5tty","targetJustReached":true,"targetName":"Al-Fatihah"} (kontrak perayaan terbukti); DELETE ?id= → {"success":true}; baseline pulih (Hamzah 0 rows, total 19); GET / → 200, dev.log bersih; git status src/: hanya guru-overview.tsx milik saya — registrations route/admin + parent-portal adalah pekerjaan agen paralel in-flight, tidak saya sentuh.
   673→
   674→---
   675→Task ID: 15 (cron review round)
   676→Agent: main (Z.ai Code) + 3 subagents (15-a/15-b/15-c)
   677→Task: Round-15 — QA sweep, guru quick-setoran sheet, PPDB accept credentials panel, parent announcement read-state badges
   678→
   679→Work Log:
   680→- Reviewed worklog (round 14 complete, 621 lines). QA sweep: lint clean, tsc clean (only pre-existing examples/skills errors), dev.log clean, GET / + /api/stats 200. Browser E2E baseline: admin overview (PPDB 6-week chart intact), guru portal (nudge card), parent portal (Rekap Kehadiran + Target strip) — ALL zero console errors. STABLE → features.
   681→- [15-a subagent] Quick Setoran Sheet in guru-overview.tsx (+~291): every "Progres Target Santri" row gains ghost PencilLine icon-button (aria-label "Catat setoran {nama}") opening Sheet (side=right sm:max-w-md, student-detail-drawer idiom) with student header (initials, class chip, amber "Target: X · r/p" chip) + compact form (surah Input + 38-name datalist, ayat, Jenis Select, nilai 0-100 clamped, catatan) + min-h-11 emerald submit (disabled until surah+ayat). POST /api/hafalan via apiSend; success closes sheet + toast "Setoran tercatat", targetJustReached:true → celebration toast "🎉 Target tercapai!" ("MasyaAllah! {nama} menyempurnakan target {target}."); load(true) quiet-refresh updates the nudge progress bar in place. Form resets per open; inline amber helpers aria-live.
   682→- [15-b subagent] PPDB accept credentials: PUT /api/registrations DITERIMA-new-student response now {registration, student, credentials:{nis, email: parent.email, tempPassword:'ortu123'}}; duplicate-childName path returns credentials:null (documented). registrations-admin.tsx submitAccept keeps dialog open and swaps to success state: emerald CheckCircle2 "Pendaftaran Diterima" + credentials card (border-emerald-200 bg-emerald-50/60) with mono NIS/Email/Sandi rows each with min-h-11 copy button (Copy→Check "Tersalin" 2s, clipboard API + execCommand fallback), KeyRound note "Kredensial juga telah dikirim ke wali via WhatsApp.", credentials:null → amber "Santri dengan nama serupa sudah terdaftar" panel; single emerald "Selesai" resets all state + load(). Reject/delete untouched.
   683→- [15-c subagent] Parent announcement read-state (parent-portal.tsx only, +~90/−12): localStorage key simadji.readAnnouncements (cuid array, dedup, cap 100, hydration-safe read-after-mount in setTimeout(10ms) w/ cleanup + try/catch). Hero announcement divs → real <button type=button> (text-left, focus-visible ring, hover) = click to mark read; unread cards show "Baru" mini-badge (gradient cards bg-amber-400/amber-950, PENTING amber-50 cards bg-emerald-600/white, Sparkles size-3) + aria-label "(pengumuman baru, klik untuk menandai sudah dibaca)"; "{n} pengumuman baru" count line (BellDot, amber-300 on gradient) + "Tandai semua dibaca" text button when ≥1 unread; badges vanish instantly, cards never dim, persists across reloads.
   684→- [ain integration E2E] Guru: logged in as Fatimah → nudge row button "Catat setoran Hamzah Abdurrahman" → sheet opens w/ Target chip + disabled submit until filled → Al-Fatihah 1-7 TAHFIDZ → submit → celebration toast "🎉 Target tercapai! … menyempurnakan target Al-Fatihah." + progressbar 0%→100% + "Target tercapai" chip in row; zero console errors. PPDB (admin): temp reg QA-Integ-Anak (PPDB-2025-0005) created via curl → Terima dialog → Terima Santri → success panel renders "Kredensial akun santri" group w/ Salin NIS/Email/Sandi + WA note → Selesai closes + table shows DITERIMA; zero console errors. Parent: fresh storage → "2 pengumuman baru" + both Baru badges → click marks read (count 2→1) → reload persists (read one stays read) → zero errors.
   685→- [ain cleanup] Deleted browser-E2E setoran row (Hamzah back to 0) + its 2 WA rows; deleted temp PPDB reg/student/user via API DELETE + 2 QA WA rows via temp bun script (scripts-tmp removed). Baseline re-verified: registrations 4 / students 9 / users 12 / notifications 13 / Hamzah hafalan 0. Final lint + tsc clean after all 3 agents; dev.log clean.
   686→
   687→Stage Summary:
   688→- Round 15 shipped: guru "Catat Setoran Cepat" Sheet from target nudge rows (with live target-progress refresh + celebration toast), PPDB accept credentials result panel (copyable NIS/email/temp-password, duplicate-safe), parent announcement read-state ("Baru" badges + count line + mark-read, localStorage-persisted, hydration-safe).
   689→- Contracts: POST /api/hafalan response unchanged (targetJustReached consumed by guru sheet too); PUT /api/registrations DITERIMA now returns credentials:{nis,email,tempPassword}|null; localStorage key simadji.readAnnouncements = string[] of announcement ids (cap 100).
   690→- Files changed (3 + 1 API): src/components/admin/guru-overview.tsx, src/app/api/registrations/route.ts (PUT only), src/components/admin/registrations-admin.tsx, src/components/parent/parent-portal.tsx. No schema changes, no new deps.
   691→- QA: lint clean; tsc clean (only pre-existing examples/skills errors); full browser E2E of all 3 features with zero console errors; DB fully restored to pre-round baseline after QA (verified counts).
   692→- Next-step candidates (priority order): (1) guru attendance quick-mark from sessions card (mirroring quick setoran), (2) parent portal export child report (print-friendly recap), (3) admin Santri drawer "add setoran" shortcut, (4) dark-mode audit, (5) public materials search/filter chips.
   693→---
   694→Task ID: 16-a
   695→Agent: quick-absen-sheet (general-purpose sub agent)
   696→Task: Tambah Sheet "Absen Cepat" (quick attendance) di overview GURU, dibuka dari tiap baris kartu "Sesi Aktif Kelas Saya", mengikuti idiom QuickSetoranSheet (Task 15-a) di file yang sama.
   697→
   698→Work Log:
   699→- Read worklog.md tail 150 (pola Task 15-a, 14-a, 14-c) + src/components/admin/guru-overview.tsx penuh (810 lama) + kontrak: src/lib/types.ts (SessionItem/AttendanceRecord/Student), src/lib/api-client.ts (apiGet/apiSend throw on error), src/app/api/attendance/route.ts (GET ?sessionId= → AttendanceRecord[] {id, sessionId, studentId, status, note, createdAt, student{id,fullName,nis}, className}; POST {sessionId, records:[{studentId,status,note?}]} → {success,count}, upsert + WA ke wali per santri), attendance-admin.tsx (STATUSES warna on/off persis), student-detail-drawer.tsx (idiom fetch-per-open + cancelled flag), ui/button.tsx (ring focus-visible bawaan shadcn).
   700→- EDIT 1 file saja: src/components/admin/guru-overview.tsx (810 → 1092 baris, +282). Tidak ada perubahan API/schema/file lain.
   701→- Komponen file-local QuickAbsenSheet({session, students, open, onOpenChange, onSaved}): Sheet side="right" sm:max-w-md flex-col p-0; header stone-50/60 dengan tile emerald pastel size-9 rounded-xl bg-emerald-100 text-emerald-700 + ClipboardCheck, judul "Absen Cepat — {className}", deskripsi "{topic||'Tanpa topik'} · formatShortDate(date)" + chip mono kode sesi (border-stone-200 bg-stone-50 font-mono text-xs tracking-widest).
   702→- Prefill: useEffect per-open/ganti-sesi (deps [open, sessionId], guard `open && sessionId`, cancelled flag) → GET /api/attendance?sessionId= → rows: Record<studentId, {status, note}> (note null → ''); gagal → loadFailed=true, tetap bisa isi dari awal + hint amber aria-live "Gagal memuat absensi tersimpan — isi dari awal"; loading → 4 Skeleton h-14 rounded-xl.
   703→- Baris santri (list divide-y divide-stone-100 dalam container flex-1 min-h-0 overflow-y-auto + SCROLL_AREA): avatar inisial size-9 (emerald-100 bila status terisi, stone-100 bila kosong) + nama truncate + selector 4 segmen grid grid-cols-4 gap-1, tombol h-9 rounded-lg border text-xs font-semibold transition-colors, kelas on/off PERSIS dari STATUSES attendance-admin (HADIR emerald-700, IZIN amber-500, SAKIT orange-500, ALPA red-600; off border-stone-200 bg-white text-stone-600 + hover:border sesuai status), aria-pressed + aria-label `Tandai {nama} {label}` + ring focus-visible ala shadcn (border-ring ring-ring/50 ring-[3px]); klik status aktif = batal pilih. Input catatan h-9 text-xs "Catatan untuk orang tua (opsional)" muncul hanya bila status terisi ≠ HADIR. Empty state santri kosong + CTA tetap bisa tutup.
   704→- Footer sticky (border-t border-stone-100 p-4 pt-3 space-y-2): ringkasan live aria-live="polite" ("2 Hadir · 1 Izin · 1 belum diisi", dihitung murni dari state — aman hidrasi) + Button min-h-11 w-full bg-emerald-700 hover:bg-emerald-800 disabled bila 0 status terisi / saving, Loader2 spin saat saving.
   705→- Submit: apiSend POST /api/attendance {sessionId, records: hanya baris berstatus → {studentId, status, note: trim()||undefined}}; sukses → toast "Absensi tersimpan" desc "{n} santri · {className}[ · {m} belum diisi]", tutup sheet, onSaved()=load(true) quiet (badge {hadir}/{total} kartu sesi ikut segar); gagal → toast "Gagal" + pesan, sheet TETAP terbuka.
   706→- Wiring kartu Sesi Aktif: tombol salin kode dibungkus `<div className="flex shrink-0 items-center gap-1">` + tombol kedua outline size-11 ikon UserCheck text-emerald-700 hover:bg-emerald-50 hover:border-emerald-300, aria-label `Absen cepat ${s.className}`; state quickSessionId + derivasi quickSession (ownSessions.find) & quickAbsenStudents (ownStudents filter classId sesi && status AKTIF) — dari data terkini, bukan snapshot.
   707→- QA: bun run lint = bersih; bunx tsc --noEmit (filter examples/|skills/) = kosong; curl GET /api/stats → 2 sesi aktif; GET /api/attendance?sessionId=cmtufisdm002eq12wfv1xqlut → 2 baris (HADIR/ALPA, note null) — mapping prefill sesuai payload; POST test ke sesi cmtuh2cxd0003q1wittjjtt57 dengan status+note IDENTIK (upsert no-op: IZIN "Acara keluarga" + HADIR) → {success:true,count:2}, data absensi tidak berubah; dev.log 30 baris terakhir tanpa error (0 "Error").
   708→
   709→Stage Summary:
   710→- Kontrak dipakai (tanpa perubahan API): GET /api/attendance?sessionId= → AttendanceRecord[] (unordered relatif, take 200); POST /api/attendance {sessionId, records:[{studentId, status:'HADIR'|'IZIN'|'SAKIT'|'ALPA', note?}]} → {success:true, count}, upsert per (sessionId, studentId) + 1 row Notification WHATSAPP/SENT per santri terkirim (HADIR dan non-HADIR).
   711→- Artefak: QuickAbsenSheet file-local di guru-overview.tsx; ABSEN_STATUSES (kelas warna persis attendance-admin); tipe AttStatus/QuickAbsenRow file-local.
   712→- Catatan integrator (opsional dirapikan): POST QA menghasilkan 2 log WA demo — Notification id cmtunmy6y001bq15laumygvvq (Zahra Nur Aini HADIR, 2026-09-09T22:13:05.051Z) dan cmtunmy6s0017q15lygpgw15g (Muhammad Rizky Ramadhan IZIN, ...044Z). Baris Attendance TIDAK berubah (upsert status identik). Tidak ada file/schema/API lain yang disentuh.
   713→- Next-step candidates: (1) pilihan "tandai semua HADIR" massal di footer sheet, (2) badge keterlambatan/tepat waktu di Absen Cepat, (3) histori absensi per sesi (sheet riwayat), (4) dark-mode audit komponen sheet baru.
   714→---
   715→Task ID: 16-c
   716→Agent: general-purpose (sub-agent, Round 16 — 16-c)
   717→Task: Upgrade PUBLIC materials catalog (Materi section) with text search, sort Select, and live result count — detail-polish for the public portal. Exactly 1 file modified: src/components/public/materials-section.tsx; no API changes.
   718→
   719→Work Log:
   720→- Read worklog tail (120+ lines) for idioms; read materials-section.tsx fully; verified Material fields from src/lib/types.ts + live payload GET /api/materials (title, type, url, description, category, classId, teacherId, createdAt, teacher{fullName}, class{name} | null) — search/sort keys exist in payload; API already returns createdAt DESC (default sort "Terbaru" consistent).
   721→- Matched repo search-input idiom (whatsapp-log.tsx/students-admin.tsx): relative wrapper + absolute left-3 Search icon size-4 text-stone-400 pointer-events-none + Input rounded-xl border-stone-200 bg-white pl-9 (shadcn Input ref-as-prop works under React 19 / ComponentProps<"input">).
   722→- EDIT materials-section.tsx ONLY (+124/−21): (1) imports +useRef, +Search, +X, +Input; (2) state query + sort ('TERBARU' default) + searchRef; (3) useMemo extended: trim+toLocaleLowerCase('id-ID') haystack over title+description+category (q==='' passthrough), then [...list].sort switch — TERBARU createdAt desc / TERLAMA asc (ISO strings → localeCompare), JUDUL_ASC/DESC localeCompare 'id' — deps [materials, category, classId, query, sort], pure (no wall-clock → hydration-safe); (4) filter row restructured: chips group unchanged, right control group flex w-full flex-wrap justify-center gap-2 lg:w-auto lg:justify-end holding search (relative w-full sm:w-[240px], min-h-11 pl-9 pr-10, X clear Button ghost size-8 absolute right-0.5 aria-label "Bersihkan pencarian" visible when query!=='' → setQuery('')+refocus input), sort Select (aria-label "Urutkan materi", w-[170px] min-h-11 rounded-xl, items Terbaru/Terlama/Judul A-Z/Judul Z-A), kelas Select kept (trigger +min-h-11 rounded-xl for row coherence); outer container mb-8→mb-6 + lg:flex-wrap; (5) result count <p aria-live="polite" text-right text-xs text-stone-500 mb-6> "{n} materi ditemukan"/"1 materi ditemukan", rendered only when !loading && !error; (6) empty state: query non-empty → `Tidak ada materi yang cocok dengan pencarian "{query}"` + Button outline min-h-11 "Hapus pencarian" (resetFilters: query='' + category/classId='SEMUA' + refocus input); generic state untouched otherwise; footer "Menampilkan X dari Y" intact.
   723→
   724→Stage Summary:
   725→- QA all green: bun run lint clean; bunx tsc --noEmit (filtered) silent; curl /api/materials 200 sample fields {id,title,type,url,description,category,classId,teacherId,createdAt,teacher,class}; dev.log tail clean (✓ Compiled, GETs 200).
   726→- Live browser QA (agent-browser on :3000, public portal): search "tajwid" → 3 cards + "3 materi ditemukan" + X visible; sort Judul A-Z → first card "Panduan Tajwid Dasar (Iqra 1-2)"; "zzzqqq" → "0 materi ditemukan" + custom empty state `Tidak ada materi yang cocok dengan pencarian "zzzqqq"` + Hapus pencarian → restores 5 cards + focus back to input; X clear → query cleared, 5 cards, focus on input, button unmounts. a11y labels verified in AX tree: textbox "Cari materi", combobox "Urutkan materi", button "Bersihkan pencarian". No page errors. (One transient "covered by span" agent-browser ref artifact — disproven via elementFromPoint hit-test: button itself is topmost after scroll; not a UI bug.)
   727→- Contracts: sort values 'TERBARU'|'TERLAMA'|'JUDUL_ASC'|'JUDUL_DESC' (string state); searchActive = query.trim()!=='' gates empty-state copy; no new deps; Indonesian UI; stone/emerald palette only.
   728→
   729→---
   730→Task ID: 16-b
   731→Agent: attendance-rekap (general-purpose subagent)
   732→Task: Kartu "Rekap Absensi Bulanan" — agregasi per santri + ekspor CSV di view Absensi ADMIN/GURU (attendance-admin.tsx saja, tanpa perubahan API/schema).
   733→
   734→Work Log:
   735→- Baca worklog.md (idiom round 11-15), attendance-admin.tsx penuh (494 baris), overview.tsx (csvCell/csvDate/csvFileStamp/downloadCsv + STATUS_DOTS + statusBadgeClass), hafalan-admin.tsx (idiom CSV: filename + rows + toast), student-detail-drawer.tsx (kompromi bucketing createdAt + initials + gradeCircleClass), ui/progress.tsx & ui/table.tsx & ui/card.tsx (CardAction), api attendance/students routes (kontrak terverifikasi).
   736→- EDIT src/components/admin/attendance-admin.tsx SAJA (+323/−3, kini 814 baris): (1) import +useMemo, +BarChart3/ClipboardList/Inbox/Download, +Progress, +CardAction, overview import ditambah downloadCsv; (2) helper modul: STATUS_COUNT_KEY (map status→kolom), RECAP_CHIPS (dot bg-emerald-500/amber-500/orange-500/red-500 = STATUS_DOTS overview), recapPctBadgeClass (>=85 emerald, >=70 amber, else red — ambang grade-badge), formatPct (id-ID desimal koma 1dp → "83,3"), initials(), interface RecapRow; (3) state rekap terpisah dari form Buka Sesi: recapClassId/recapMonth ('none')/recapData{records,roster}/recapLoading/recapError/recapRetry; (4) monthOptions useMemo([]) — 6 bulan terakhir dihitung SEKALI per mount, label toLocaleDateString('id-ID',{month:'long',year:'numeric'}) ("September 2026"), value "2026-09", tanpa pembacaan wall-clock saat render hasil (hydration-safe, kompromi sama dgn useState(todayISO()) yang sudah ada); (5) SATU effect [recapClassId,recapMonth,recapRetry] guard keduanya !=='none' → Promise.all(apiGet /api/attendance?classId=, apiGet /api/students?classId=) + flag cancelled, Skeleton 5×h-12 saat loading, Alert destructive+Coba Lagi (recapRetry++) saat gagal — idiom persis file ini; (6) agregasi useMemo: filter records createdAt.slice(0,7)===bulan (session dilepas API — kompromi student-detail-drawer), totals per status, pertemuan=Set(sessionId).size, rows = SEMUA santri kelas (AKTIF dulu, lalu nama A-Z; tanpa catatan = nol), pct per santri = hadir/pertemuan (1dp), rate = hadir/totalRecords; (7) UI: Card full-width rounded-2xl border-stone-200 shadow-sm SETELAH kartu Riwayat; CardHeader = tile BarChart3 size-9 rounded-xl bg-emerald-100 text-emerald-700 + judul/deskripsi + CardAction tombol outline "Unduh CSV" (min-h-11, Download, aria-label "Unduh rekap CSV", disabled saat tabel kosong); CardContent: controls flex-col sm:flex-row gap-3 (Select kelas placeholder "Pilih kelas" + Select bulan placeholder "Pilih bulan", dua-duanya min-h-11 w-full sm:w-60, aria-label "Kelas/Bulan rekap absensi"); empty state tanpa pilihan → dashed box ClipboardList text-stone-300 "Pilih kelas dan bulan untuk melihat rekap"; zero catatan bulan → dashed box Inbox "Belum ada catatan kehadiran pada bulan ini"; strip aria-live="polite" 4 chip (dot warna + count text-lg font-bold + label text-xs text-stone-500, border-stone-100 bg-stone-50/60 px-3 py-2) + chip ke-5 "Tingkat Kehadiran" dgn Progress emerald (className="[&>div]:bg-emerald-600", track bg-stone-200); tabel Table min-w-[640px]: Santri (avatar inisial size-8 emerald-100 + nama + NIS mono), Hadir/Izin/Sakit/Alpa (center), % Hadir (Badge recapPctBadgeClass, format "83,3%"), row hover:bg-stone-50/60; thead+th sticky top-0 z-10 bg-white; area scroll = kontainer internal Table via arbitrary-variant wrapper "[&_[data-slot=table-container]]:max-h-80 overflow-y-auto pr-1 [scrollbar-width:thin] [&::-webkit-scrollbar]:w-1.5 thumb:rounded-full bg-stone-300" (WAJIB: div bawaan shadcn Table ber-data-slot itu scrollportnya — sticky di wrapper luar tidak bekerja karena table-container punya overflow-x-auto→jadi scroll container tersendiri); (8) exportRecapCsv: slug kelas (lowercase, non-alnum→'-'), filename rekap-absensi-{slug}-{YYYY-MM}.csv (YYYY-MM = bulan terpilih, idiom csvFileStamp: nama bertanggal via helper), kolom NIS;Nama;Kelas;Hadir;Izin;Sakit;Alpa;Total Pertemuan;Persentase Hadir (%), persentase formatPct → "83,3"; downloadCsv(filename, rows) + toast "Ekspor CSV berhasil".
   737→- Catatan implementasi: escape per sel DILAKUKAN downloadCsv (rows.map(csvCell) internal, ';'+BOM+quote-doubling) — persis idiom hafalan-admin round 11; TIDAK membungkus sel dgn csvCell manual utk menghindari double-escape (nilai berisi ';'/'"' akan rusak jika lolos csvCell dua kali). csvDate tidak dipakai (rekap bulanan tanpa kolom tanggal).
   738→
   739→Stage Summary:
   740→- File changed (exactly 1): src/components/admin/attendance-admin.tsx (+323/−3 → 814 baris). Tidak ada perubahan API/schema/dep; guru-overview.tsx & materials-section.tsx & db/custom.db adalah pekerjaan paralel agen lain, tidak disentuh.
   741→- UI contract: kartu Rekap Absensi Bulanan muncul di bawah Riwayat Kehadiran Terbaru (view Absensi ADMIN & GURU); fetch hanya saat kelas+bulan dipilih (1 effect, Promise.all); konsumsi GET /api/attendance?classId= → AttendanceRecord[] (take 200, orderBy createdAt desc, tanpa session date) + GET /api/students?classId= → Student[] (semua santri kelas tampil, nol bila tanpa catatan); angka rekap = fungsi murni useMemo (records,roster,bulan); CSV: rekap-absensi-{kelas-slug}-{YYYY-MM}.csv via downloadCsv (csvCell per sel, ';', BOM); tanpa Date.now/Math.random di render; bulan = useMemo sekali per mount.
   742→- QA: bun run lint → clean; bunx tsc --noEmit | grep -vE "examples/|skills/" → kosong; curl (GET only, tanpa tulis DB): /api/classes → 4 kelas; /api/attendance?classId= & /api/students?classId= utk 4 kelas → array (Tahfidz A: 4 record/2 sesi/2 santri bulan 2026-09; Iqra 4: 2/1/2; Al-Qur'an A: 2/1/2; Iqra 1: 0-1 — ada agen paralel yang menulis row); dev.log 30 baris terakhir = 200 + prisma:query, tanpa error. Browser E2E (agent-browser, login admin quick-fill): pilih Tahfidz A + September 2026 → strip Hadir 3/Izin 0/Sakit 0/Alpa 1/Tingkat Kehadiran 75% (progressbar emerald, transform -25% — cocok hitung manual 3/4); tabel 2 santri (Fathan 1-0-0-1 → 50% badge merah; Alfatih 2-0-0-0 → 100% badge emerald), urut nama, AKTIF semua; computed style: table-container max-height 320px + overflow-y auto + scrollbar-width thin, thead/th sticky top-0 z-10 bg-white, sticky terverifikasi mekanis (inject 8 baris + scrollTop 200 → thead top == container top); Agustus 2026 → Inbox "Belum ada catatan kehadiran pada bulan ini" + tombol ekspor disabled; klik Unduh CSV → toast "Ekspor CSV berhasil · Rekap 2 santri tersimpan di rekap-absensi-tahfidz-a-2026-09.csv"; opsi bulan September→April 2026 (id-ID); zero console/page errors; manipulasi DOM uji sticky dipulihkan, tanpa perubahan data.
   743→
   744→---
   745→Task ID: 16 (cron review round, integration)
   746→Agent: main (Z.ai Code) + 3 subagents (16-a/16-b/16-c)
   747→Task: Round-16 — QA sweep, guru quick-absen sheet, monthly attendance recap + CSV, public materials search/sort
   748→
   749→Work Log:
   750→- Reviewed worklog (round 15 complete, 692 lines). QA sweep: lint clean, tsc clean (only pre-existing examples/skills errors), dev.log clean, GET / + /api/stats 200, baseline APIs verified (9 students / 19 hafalan / 4 registrations / 13 notifications / 2 active sessions). Browser E2E baseline: public landing, admin (Ringkasan/PPDB/Hafalan/Keuangan), guru (Fatimah), parent (Budi, read-state badges + Rapor Santri print dialog intact) — ALL zero console errors. NOTE: parent "Rapor Santri" print feature and WA-log filter/CSV + admin student drawer already existed from earlier rounds (candidates 2/3/5 of round-15 list were done) → chose remaining gaps.
   751→- [16-a subagent] QuickAbsenSheet in guru-overview.tsx (+281): per active-session row second icon button UserCheck "Absen cepat {className}" opens Sheet (right, sm:max-w-md) — prefill from GET /api/attendance?sessionId= (skeleton rows; amber aria-live hint on prefill failure), student rows with 4-segment status selector (exact attendance-admin on/off color families: HADIR emerald-700 / IZIN amber-500 / SAKIT orange-500 / ALPA red-600, aria-pressed + aria-label, h-9), note Input (h-9) revealed only when status≠HADIR, sticky footer with aria-live summary ("n Hadir · m belum diisi") + min-h-11 emerald submit; POST /api/attendance {sessionId, records[]} → toast "Absensi tersimpan · n santri · kelas" → load(true) quiet refresh (hadir/total badge updates in place).
   752→- [16-b subagent] "Rekap Absensi Bulanan" card in attendance-admin.tsx (+323/−3): kelas + bulan (last 6 months, useMemo([]) — hydration-safe) Selects → Promise.all(GET attendance?classId + students?classId) with file's skeleton/alert idioms → aria-live summary strip (4 status-dot chips + Tingkat Kehadiran emerald Progress), per-santri sticky-thead table (all class students, zeros included; % Hadir badge ≥85 emerald / ≥70 amber / else red), CSV export rekap-absensi-{kelas}-{YYYY-MM}.csv (NIS;Nama;Kelas;Hadir;Izin;Sakit;Alpa;Total Pertemuan;Persentase Hadir % — id-ID comma decimals via shared downloadCsv). Month bucket = createdAt.slice(0,7) (API strips session date — documented).
   753→- [16-c subagent] materials-section.tsx (+124/−21): text search Input (pl-9 Search icon, clear X refocuses, aria-label "Cari materi"), id-ID case-insensitive match over title+description+category, sort Select (Terbaru/Terlama/Judul A-Z/Z-A via localeCompare 'id'), aria-live "{n} materi ditemukan" count, search-specific empty state ("Tidak ada materi yang cocok…" + "Hapus pencarian" reset button), responsive wrap.
   754→- [ain integration E2E] Guru quick-absen as Ustadz Yusuf (Tahfidz A owner): sheet prefill correct (Alfatih HADIR, Fathan ALPA) → Fathan→Hadir → submit → toast "Absensi tersimpan 2 santri · Tahfidz A" + card badge 1/2→2/2 via quiet refresh → restored Fathan→Alpa → API verified exact baseline (HADIR/ALPA, notes null). Recap card as guru: Tahfidz A + September 2026 → chips 3/0/0/1 + Tingkat Kehadiran 75% + per-santri rows (Fathan 50% red, Alfatih 100% emerald) — matches manual math. Materials as public: "tajwid" → 3 cards + "3 materi ditemukan" + X visible. Zero console errors throughout.
   755→- [ain cleanup] Deleted 6 QA-created WA notification rows (4 from integration E2E + 2 left by 16-a QA) via temp bun script (scripts-tmp removed); notifications back to 13. Attendance/status baselines verified unchanged. Final lint + tsc clean across all 3 agents; dev.log clean; git status shows exactly the 3 intended files.
   756→
   757→Stage Summary:
   758→- Round 16 shipped: guru "Absen Cepat" Sheet from active-session card (prefill + upsert + live badge refresh), admin/guru "Rekap Absensi Bulanan" (per-santri monthly aggregation + % badges + CSV export), public materials search/sort/count polish.
   759→- Contracts unchanged: GET/POST /api/attendance, GET /api/students?classId, GET /api/materials — frontend-only round, no API/schema/deps changes.
   760→- Files changed (3): src/components/admin/guru-overview.tsx, src/components/admin/attendance-admin.tsx, src/components/public/materials-section.tsx.
   761→- QA: lint clean; tsc clean (only pre-existing examples/skills errors); full browser E2E of all 3 features zero console errors; DB fully restored to baseline (13 notifications, attendance rows identical).
   762→- Next-step candidates (priority order): (1) dark-mode audit across all portals, (2) guru classes view polish (per-class detail summary), (3) parent-side monthly recap link into attendance CSV/print, (4) admin hafalan target bulk-edit (set target for whole class), (5) public PPDB brochure download section.
   763→