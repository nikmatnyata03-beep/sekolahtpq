   348→Task: Guru weekly teaching timetable ("Jadwal Mengajar Mingguan") parsed from class.schedule strings.
   349→
   350→Work Log:
   351→- Read worklog.md (round-10 "next-step candidates" listed this feature), guru-overview.tsx (card/KPI tile patterns, ownClasses filtering, two-column grid placement point), types.ts (ClassRoom: level/schedule/room/teacherId/studentCount), eslint.config.mjs + page.tsx (set-state-in-effect precedent → timer-deferred setState), classes-admin.tsx level list; verified live DB schedules via bun+PrismaClient: exactly "Senin, Rabu — 15.30-17.00" (Iqra 1/IQRA/Ruang Serambi), "Selasa, Kamis — 15.30-17.00" (Iqra 4), "Senin-Kamis — 16.00-18.00" (Tahfidz A/TAHFIDZ/Musholla Utama), "Senin, Rabu, Jumat — 15.30-17.30" (Al-Qur'an A/AL_QURAN/Ruang Utama); verified lucide 0.525 exports CalendarDays/MapPin/AlertTriangle.
   352→- Created src/components/admin/weekly-schedule.tsx ('use client', named exports WeeklySchedule + parseWeeklySchedule + type ParsedSchedule). Parser: normalize em/en/minus dashes → "-", locate the unique "HH.MM-HH.MM" (dots or colons) time range first, dayPart = prefix with leading/trailing dash runs stripped (fixes "Senin-Kamis - " residue from the em-dash separator); tokens split on [,;] — each either single day (normalizeDayName: lowercase, strip apostrophes incl. U+2019 + non-letters, alias map for senen/slasa/rebo/kemis/jum'at→jumat/jumad/jumuat/sabt/ahad/minggu) or inclusive range "Senin-Kamis" expanded with %7 wrap (Sabtu-Senin → Sabtu,Ahad,Senin); any unrecognized token → null (caller surfaces it, never drops). Component: Card rounded-2xl border-stone-200 shadow-sm, CardHeader with CalendarDays in size-9 rounded-lg bg-emerald-100 text-emerald-700 tile, title "Jadwal Mengajar Mingguan", description "Keterampilan jadwal kelas yang Anda ampu"; day grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 (lg:grid-cols-7 when Ahad column appears) role="list" + role="listitem" aria-label "Senin — 2 kelas"; day header text-xs font-bold uppercase tracking-wide + emerald underline bar (h-0.5 w-7 bg-emerald-500); chips rounded-xl border-stone-200 border-l-4 (IQRA border-l-emerald-600 / TAHFIDZ border-l-amber-500 / AL_QURAN border-l-teal-600, unknown → stone-400) bg-stone-50 hover:bg-emerald-50 p-2.5 with name text-xs font-semibold, time font-mono tabular-nums text-[11px] "15.30-17.00", room MapPin size-3 text-[11px], title tooltip "Iqra 1 · 15.30-17.00 · Ruang Serambi"; chips sorted by start minutes then name; empty day dashed border-stone-200 h-16 with centered "—" text-stone-300; today highlight effect-gated (useState<DayKey|null>(null) + useEffect setTimeout(0) setState — SSR/first paint fully neutral, mirrors page.tsx hydration pattern) → column bg-emerald-50/60 ring-1 ring-emerald-200 + amber-100/amber-800 "Hari ini" pill; unparsed classes render in amber-50/amber-200 AlertTriangle "Format jadwal tidak dikenali" list (name + raw mono schedule); legend row border-t stone-100 with level dots (only levels present, canonical order) + emerald-100 "N sesi/minggu" chip (N = total class-day sessions); returns null when classes empty.
   353→- Edited src/components/admin/guru-overview.tsx minimally (2 edits, +4 lines): added `import { WeeklySchedule } from './weekly-schedule'`; rendered `<WeeklySchedule classes={ownClasses} />` after the lg:grid-cols-2 grid (Sesi Aktif + Setoran Hafalan) as full-width block with comment. No other changes.
   354→- QA: parser sanity via `bun -e` importing the real module — all 4 DB schedules parse correctly ("Senin, Rabu — 15.30-17.00" → Senin,Rabu 15.30-17.00; "Selasa, Kamis — 15.30-17.00" → Selasa,Kamis; "Senin-Kamis — 16.00-18.00" → Senin,Selasa,Rabu,Kamis 16.00-18.00; "Senin, Rabu, Jumat — 15.30-17.30" → 3 days) plus robustness cases (en-dash, Jum'at, colon times, no-space "Senin-Kamis-16.00-18.00", Minggu→Ahad, wrap Sabtu-Senin; "garbled text"/"Senin, Bulan"/""/null → null). One parser bug found & fixed during QA (trailing em-dash residue broke range form → dayPart dash-trim). `bun run lint` → clean (eslint . zero output); `bunx tsc --noEmit | grep weekly-schedule|guru-overview` → empty; only the 4 pre-existing errors in examples/+skills/ remain. Dev server NOT run; no other files touched; no new packages.
   355→
   356→Stage Summary:
   357→- Files: NEW src/components/admin/weekly-schedule.tsx (exports WeeklySchedule({ classes }: { classes: ClassRoom[] }), plus parseWeeklySchedule(raw): ParsedSchedule|null and type ParsedSchedule for testability); EDIT src/components/admin/guru-overview.tsx (+1 import, +3 lines render — WeeklySchedule mounts only in the has-assignment branch, after the Sesi Aktif/Setoran Hafalan grid).
   358→- Decisions: pure client component fed with already-loaded ownClasses (no fetches); DayKey 0=Senin…6=Ahad with getDay()+6)%7 conversion; Ahad column auto-appears only when used (grid switches to lg:grid-cols-7); today highlight effect-gated via timer callback to satisfy react-hooks 7 set-state-in-effect and hydration safety (SSR HTML = neutral columns); TAHFIDZ chip color = amber-500 per task spec (classes-admin badge uses violet — chip accent follows the timetable spec, badge untouched); level border classes resolved through a LEVEL_STYLES map with stone-400 fallback for future levels; sessions/minggu counts class-day pairs (e.g. Tahfidz A Senin-Kamis = 4).
   359→- QA results: bun run lint clean; tsc clean for both files; parser verified live against all 4 seeded schedules + 8 robustness cases; no dev server, no other files edited.
   360→
   361→---
   362→Task ID: 11 (cron review round)
   363→Agent: main (Z.ai Code) + 4 subagents (11-a/11-b/11-c/11-e)
   364→Task: Round-11 review — QA sweep, public announcements section, guru weekly timetable, hafalan CSV export, mass-invoice generator, sidebar role-label fix
   365→
   366→Work Log:
   367→- Reviewed worklog (round 10 complete, candidates listed). QA sweep: lint clean, tsc clean (src only), dev.log clean, browser E2E (public portal, guru login, parent portal Juz-30 map + trend + notification badge, logout; zero console errors). App STABLE → proceeded to features.
   368→- [ain BUGFIX] dashboard.tsx sidebar brand hardcoded "TPQ Darul Jinan · Admin" for every role → now role-aware: "TPQ Darul Jinan · {Admin|Guru|Wali Santri}" via existing roleLabel().
   369→- [11-a subagent] NEW src/components/public/announcements-section.tsx: public "Pengumuman" section (id=pengumuman, nav item added after Berita, wired between News + Testimonials). Section header w/ amber eyebrow + count badge; 3 most-recent as hover-lift feature cards (PENTING = amber left border + AlertTriangle badge, NORMAL = emerald outline + BadgeCheck; CalendarDays id-ID date; line-clamp-3 preview); rest in max-h-80 thin-scrollbar "Riwayat" list; detail Dialog (priority badge, date, scrollable content, WA-style footer); skeleton/empty/error-retry states; a11y (role=button cards w/ keyboard handlers, focus-visible rings, ≥44px targets).
   370→- [11-b subagent] NEW src/components/admin/weekly-schedule.tsx: "Jadwal Mengajar Mingguan" timetable in GuruOverview (full-width, after Sesi Aktif/Setoran grid). Robust schedule-string parser (comma days, Senin-Kamis inclusive ranges w/ %7 wrap, em/en-dash + colon tolerance, Jum'at/Minggu aliases, unknown → amber fallback list; parseWeeklySchedule exported + 12-case tested); 6/7-day grid (Senin-first, Ahad auto-appears), level-accented chips (IQRA emerald/TAHFIDZ amber/AL_QURAN teal left borders, mono time, MapPin room, tooltips), effect-gated today highlight ("Hari ini" amber pill + emerald ring — hydration-safe), legend + "N sesi/minggu" chip, role=list/listitem semantics. guru-overview.tsx +4 lines wiring.
   371→- [11-c subagent] hafalan-admin.tsx: "Ekspor CSV" toolbar button reusing round-9 shared helpers (overview.tsx csvDate/csvFileStamp/downloadCsv — BOM + ';' + sanitizer). Exports Riwayat rows: Tanggal/Santri/NIS/Kelas/Surat/Ayat/Jenis(label)/Nilai/Catatan; filename data-hafalan-<stamp>.csv; toast "Ekspor CSV berhasil — 19 data hafalan tersimpan…"; disabled-when-empty/loading, Loader2 swap.
   372→- [11-e subagent] NEW src/app/api/payments/batch/route.ts: POST {classId,title,amount,month?} → {created,skipped,invoices[]}; validation chain (400 missing/non-positive, 404 class, 400 no-AKTIF-students), JS-side duplicate guard (studentId::trim+lowercase title vs PENDING/SUCCESS — SQLite has no insensitive filter), sequential invoiceNo INV-2025-{1000+count+i}, WA notify per created invoice. payments-admin.tsx: "Tagihan Massal" emerald-outline toolbar button + Dialog (Kelas select w/ studentCount, Keterangan prefilled "Iuran SPP {Bulan} {Tahun}", Nominal 75000 + Rp hint, live preview "X santri AKTIF · est. total Rp Y", amber duplicate-skip Alert, h-11 controls, Loader2 submit) → success toast "X tagihan dibuat, Y dilewati (sudah ada)" + list refetch. curl suite 6/6 pass (400×2/404/200/200 dup-skip/200 case-insensitive dup-skip); test rows cleaned; WA rows verified.
   373→- E2E verified in browser: sidebar shows "· Guru"/"· Admin" per role; Pengumuman section + nav + detail dialog (PENTING badge, content); guru timetable (Iqra 4 chips on Selasa/Kamis, Rabu "Hari ini" highlight, legend "2 sesi/minggu", time text correct); admin Hafalan Ekspor CSV toast "19 data hafalan tersimpan"; Keuangan Tagihan Massal full flow (Iqra 1 → preview "3 santri AKTIF · est. total Rp 225.000" → submit → toast "3 tagihan dibuat, 0 dilewati" + INV-1009/1010/1011 PENDING rows appear + Tunggakan KPI updates); mobile 390px Pengumuman stacks cleanly w/ amber PENTING border; zero console/page errors; lint + tsc clean after integration.
   374→- Note: 3 browser-test SPP invoices (INV-2025-1009..1011, Iqra 1, "Iuran SPP September 2026", PENDING) intentionally KEPT as realistic demo data (exercises reminder flow + PENDING KPIs).
   375→
   376→Stage Summary:
   377→- Round 11 shipped: public Pengumuman section (+nav), guru Jadwal Mengajar Mingguan timetable, hafalan CSV export, POST /api/payments/batch + Tagihan Massal dialog, sidebar role-label fix. All features browser-verified; lint+tsc clean; zero console errors.
   378→- New API: POST /api/payments/batch {classId, title, amount, month?} → {created, skipped, invoices[]} | {error} (400/404).
   379→- Demo data state: 8→11 payments (3 new PENDING SPP Sept for Iqra 1), announcements 3 (1 PENTING), hafalan 19.
   380→- Known notes: batch duplicate-guard is per-title (case/trim-insensitive), not per-month — intended; dev.log may contain one 404 probe from subagent QA (expected).
   381→- Next-step candidates (priority order): (1) parent portal "Konfirmasi Terima" for announcements read-state or light per-user read tracking, (2) admin PPDB → auto-create User+Student on accept (currently registration accept creates student only — verify), (3) session attendance recap per student in parent portal (percentage chip already exists; add monthly breakdown), (4) dark-mode audit of new sections, (5) hafalan target planning (set target surah per student, show % in guru timetable card or santri detail).
   382→
   383→---
   384→Task ID: 12-b
   385→Agent: attendance-recap (frontend-styling-expert)
   386→Task: Parent portal "Rekap Kehadiran Bulanan" — monthly stacked-bar attendance recap component (standalone, unwired).
   387→
   388→Work Log:
   389→- Read worklog.md in full (round 9/11 styling language + round-11 next-step candidate #3 = this feature); inspected child-panel.tsx (ATTENDANCE_CHIP/ATTENDANCE_BADGE/RAPOR_ATT_DOT status maps, "Riwayat Kehadiran — 30 pertemuan terakhir" card, ~line 766), hafalan-chart.tsx card language, ui/card.tsx primitives, weekly-schedule.tsx size-9 emerald icon-tile header pattern; verified lucide 0.525 exports CalendarRange + BarChart3 via bun probe; confirmed @types/react ^19 → return type uses `import type { JSX } from 'react'` (global JSX namespace removed in React 19 types).
   390→- Created src/components/parent/attendance-recap.tsx ('use client') ONLY — zero existing files touched. Exports: AttendanceRecap({attendances}) + AttendancePoint/MonthAgg types + aggregateMonthly(attendances) helper.
   391→- Rendering: Card rounded-2xl border-stone-200 bg-white shadow-sm; CardHeader with CalendarRange in size-9 rounded-lg bg-emerald-100 text-emerald-700 tile + title "Rekap Kehadiran Bulanan" + description "Ringkasan per bulan dari 30 pertemuan terakhir."; per-month rows (w-20 text-xs font-semibold label, flex h-3.5 rounded-full overflow-hidden bg-stone-100 stacked bar w/ flexGrow∝count + min-w-[2px] segments, right column rate chip + "hadir/total" text-[11px] text-stone-500); row hover bg-stone-50 rounded-lg transition + title tooltip (long month + total + rate); bar title tooltip "HADIR n · IZIN n · SAKIT n · ALPA n"; a11y role="img" + aria-label "September 2026: 12 hadir, 1 izin, 1 sakit, 0 alpa — 86% kehadiran"; legend (size-2 dots, Hadir/Izin/Sakit/Alpa labels, only statuses present); footer note "Persentase = hadir ÷ total pertemuan tercatat bulan tersebut."; returns null when attendances empty (also when all dates unparseable).
   392→- Aggregation via useMemo → aggregateMonthly: monthKeyOf prefers the ISO string "YYYY-MM" prefix (deterministic + timezone-safe, full-ISO timestamps and date-only both handled), invalid dates skipped; labels via new Date(y, m-1, 1, 12).toLocaleDateString('id-ID', {month:'short'|'long', year:'numeric'}); sorted newest-first, capped at 6 most recent months with data; rate = Math.round(hadir/total*100); chip thresholds ≥85 emerald-100/emerald-800, ≥70 amber-100/amber-800, else red-100/red-800.
   393→- STATUS COLOR MAPPING (reused EXACTLY from child-panel.tsx RAPOR_ATT_DOT, overriding the brief's bg-amber-400/stone-400/teal suggestions per the brief's own "reuse EXACTLY those hues" rule): HADIR=bg-emerald-500, IZIN=bg-amber-500, SAKIT=bg-orange-500, ALPA=bg-red-500 (teal rejected — SAKIT is orange in the parent portal; orange already established there, no indigo/blue introduced).
   394→- aggregateMonthly sanity via `bun -e` importing the real module: T0 empty→[]; T1 3 synthetic months (unsorted) → newest-first [{Okt 2026, hadir 1, total 1, rate 100 (single-record edge)}, {Sep 2026, hadir 12 izin 1 sakit 1 alpa 1, total 15, rate 80 (mixed)}, {Agu 2026, hadir 14, total 14, rate 100 (all-HADIR, full-ISO timestamps)}]; T2 8 months → capped to 6 newest ["2026-08".."2026-03"] w/ correct id-ID labels (Agu/Jul/Jun/Mei/Apr/Mar); T3 invalid-date records skipped gracefully. All assertions passed.
   395→- QA: `bun run lint` → clean (eslint . zero output, exit 0); `bunx tsc --noEmit 2>&1 | grep attendance-recap` → empty (exit 1, zero errors in new file). Dev server NOT run; no existing file edited; no new deps.
   396→
   397→Stage Summary:
   398→- File created (sole file touched): src/components/parent/attendance-recap.tsx. Export contract for wiring: `AttendanceRecap({ attendances }: { attendances: AttendancePoint[] }): JSX.Element | null` + `interface AttendancePoint { id: string; status: 'HADIR'|'IZIN'|'SAKIT'|'ALPA'; date: string; topic: string|null; className: string }` + `aggregateMonthly(attendances): MonthAgg[]` (+ MonthAgg/AttendanceStatus types). Pure presentational client component — no fetch, no toast, no effects.
   399→- Suggested wiring (main agent): in child-panel.tsx Riwayat Kehadiran card (item b), render `{child.attendances.length > 0 && <AttendanceRecap attendances={child.attendances} />}` — either inside that CardContent above the scroll list or as a sibling card in the lg:grid-cols-2 grid; child.attendances (ParentStudent.attendances) matches AttendancePoint shape (status/date/topic via session, className via class name) — topic/className fields are accepted but only status+date are consumed; empty data renders null so no guard is strictly required.
   400→- Decisions: segment hues copied verbatim from child-panel RAPOR_ATT_DOT (emerald-500/amber-500/orange-500/red-500) for cross-card consistency; legend labels "Hadir/Izin/Sakit/Alpa" per child-panel's status-label convention; month key from date-string prefix to avoid UTC-vs-local drift and hydration mismatch (server/client ICU identical since input depends only on data); single-record months render full-width segment via flexBasis 0 + flexGrow 1 + min-w-[2px]; unknown/invalid statuses count toward total but leave a stone-100 gap (honest representation of "tercatat").
   401→- QA results: aggregateMonthly 4/4 bun-e scenarios pass (empty / all-HADIR / mixed 12-1-1-1 / single-record / cap-6 / invalid-date skip); lint clean; tsc clean for attendance-recap; zero files besides the new component modified.
   402→
   403→---
   404→Task ID: 12-a
   405→Agent: hafalan-target (full-stack-developer)
   406→Task: Target Hafalan planning — ustadz sets a Juz-30 target surah per santri; parents see progress toward it in the child panel.
   407→
   408→Work Log:
   409→- Read worklog.md (head 120 + tail 150), prisma/schema.prisma, students + portal/parent routes, types.ts, students-admin.tsx (fully), hafalan-chart.tsx, child-panel.tsx wiring point; verified lucide 0.525 exports Target/CheckCircle2 and dev server live on :3000.
   410→- Schema: Student.hafalanTarget String? added after photoUrl (comment: target surah Juz 30). `bun run db:push` OK (14ms, generate ran). Temp verify script scripts-tmp/verify-hafalan-target.ts read 3 students with hafalanTarget:null then DELETED (dir removed).
   411→- API: students/route.ts POST data + `hafalanTarget: b.hafalanTarget || null`; PUT `...(b.hafalanTarget !== undefined && { hafalanTarget: b.hafalanTarget || null })` (explicit null clears; '' also maps to null). portal/parent/route.ts students.map now returns `hafalanTarget: s.hafalanTarget,`. GET /api/students returns it automatically (scalar).
   412→- Types: Student interface + ParentPortalData.students[] (child-summary w/ attendances/hafalans/payments/billing) both gained `hafalanTarget: string | null` (non-optional scalars, matches API reality after include/map).
   413→- students-admin.tsx: JUZ30_TARGET_OPTIONS const (exact 38 names per task: Al-Fatihah + 37, incl. An-Nazi'at/Al-Kausar/Al-Lahab spelling); StudentFormState.hafalanTarget ('none' sentinel); Edit dialog prefills s.hafalanTarget ?? 'none'; payload sends `form.hafalanTarget === 'none' ? null : form.hafalanTarget` (POST & PUT share payload); dialog field "Target Hafalan" = shadcn Select (id/aria-label, h-11 trigger ≥44px, helper caption "Surah Juz 30 yang ditargetkan — progresnya tampil di Portal Wali.", Radix native scroll for 38 items); table Aktivitas cell gains amber chip shown only when set: Badge outline border-amber-200 bg-amber-100 text-amber-800 + Target icon size-2.5 + "Target: {name}" + transition-colors hover (matches neighbor count-badge style); exportCsv appends 'Target Hafalan' column (s.hafalanTarget ?? '').
   414→- hafalan-chart.tsx: HafalanProgress({ hafalans, target?: string | null }) — backward compatible. Added NORM_ALIASES (alkausar→alkautsar for the target-Select spelling, allahab→almasad for Al-Lahab, aththariq→attariq for the hafalan-admin datalist variant) + resolveNorm(); setoranCount keys now resolveNorm (strictly additive — all existing matches unchanged, verified DB surahNames still match). Target logic: targetInfo = findIndex in JUZ30_SURAHS via resolveNorm (null → strip skipped entirely, e.g. non-Juz-30 target); reached = distinct setoran among list entries slice(0, position) (position = idx+1, before-or-equal); amber strip first child of "Peta Hafalan Juz 30" CardContent: rounded-xl border-amber-200 bg-amber-50, Target tile + "Target: {name}" + emerald-100 CheckCircle2 "Tercapai" chip when the target surah itself has setoran, right-aligned "{reached}/{position} surah", thin Progress h-1.5 bg-amber-200 [&>div]:bg-amber-500 + "{pct}% progres menuju target", role="status" + aria-labels; tile map target chip gets ring-2 ring-amber-400 on top of existing variant + tiny amber Target icon overlay + "· TARGET hafalan" in title; legend gains "Target hafalan" ring swatch when target set. Hydration-safe: everything derived from props via useMemo (no Date/random).
   415→- child-panel.tsx: one-line edit — `<HafalanProgress hafalans={child.hafalans} target={child.hafalanTarget} />`.
   416→- Dev-server note (no manual start/stop): after db:push the RUNNING next-server kept the pre-change Prisma Client (old DMMF → PUT 400 "Gagal memperbarui santri", GET rows lacked the key). Verified a temp db.ts global-bypass did NOT help (module graph stale, reverted). Cleanest non-invasive fix: touched next.config.ts (added then removed a comment line) — Next 16 dev performed its own managed restart both times ("Found a change in next.config.ts. Restarting the server to apply the changes... ✓ Ready"), same process tree `bun run dev` intact; new client loaded; .env also byte-restored after probing (Next 16 did not restart on .env touch). Server healthy (GET / 200, /api/stats 200) before/after.
   417→- curl evidence (dev.log lines 814/834/840 = PUT 200×3): T1 PUT {id: cmtufisd1000sq12wyuomvtbq (Muhammad Rizky Ramadhan), hafalanTarget:"An-Naba"} → {"fullName":"Muhammad Rizky Ramadhan","nis":"DJ-2024-001","hafalanTarget":"An-Naba"}; T2 GET /api/portal/parent?userId=cmtufiscv000lq12wlmr3cq2v (budi) → students:[{Muhammad Rizky Ramadhan, hafalanTarget:"An-Naba", hafalanCount:5},{Alika Syakira, hafalanTarget:null}]; T3 PUT hafalanTarget:null → hafalanTarget:null; T4 PUT hafalanTarget:"" → null; T5 GET /api/students?parentId=<budi> → both children hafalanTarget:null; T6 GET /api/portal/parent → both null (baseline restored). Pre-fix 400s (dev.log 560/636/678/679) document the stale-client diagnosis.
   418→- bun -e simulation against Rizky's real 5 setoran (Al-Fatihah×2, An-Nas, Al-Falaq, Al-Ikhlas) with target An-Naba → position 2, reached 1/2 = 50%, targetDone false (strip shows 1/2 + 50%, no Tercapai chip) — matches component logic.
   419→
   420→Stage Summary:
   421→- Contract: Student.hafalanTarget String? (surah NAME string, e.g. "An-Naba", null = no target). POST /api/students accepts hafalanTarget (default null); PUT accepts hafalanTarget (undefined = keep, null/'' = clear); GET /api/students + GET /api/portal/parent?userId= return hafalanTarget on every student/child. ParentPortalData.students[] + Student types now include hafalanTarget: string | null.
   422→- UI contract: StudentsAdmin form field value uses 'none' sentinel → null; HafalanProgress accepts optional target?: string | null (all existing call sites unaffected). Admin Select stores the task's exact 38-name spelling (Al-Kausar/Al-Lahab/An-Nazi'at); chart resolves via NORM_ALIASES (alkausar/allahab/aththariq) so it also matches the chart's canonical list AND existing hafalan-admin datalist variants — resolveNorm is now used for setoran matching too (pure superset of previous behavior).
   423→- Files changed (7): prisma/schema.prisma, src/app/api/students/route.ts, src/app/api/portal/parent/route.ts, src/lib/types.ts, src/components/admin/students-admin.tsx, src/components/parent/hafalan-chart.tsx, src/components/parent/child-panel.tsx (1 line). Transient & fully reverted: src/lib/db.ts, next.config.ts, .env (see note above). NOT touched: attendance-recap.tsx and everything else.
   424→- QA: db:push OK; lint clean; bunx tsc --noEmit (minus examples/skills) empty; 6/6 curl scenarios pass incl. set→parent-visible→reset-null; DB baseline restored (all targets null); dev server running & healthy on :3000; chart math simulated against real data (An-Naba → 1/2 = 50%).
   425→- Decisions: target chip lives in the Aktivitas badge group (first position, amber-100 per task); CSV column appended last ('Target Hafalan') to avoid shifting existing column indices; HafalanProgress strip renders only when target resolves inside Juz 30 (graceful skip otherwise); child-panel keeps its existing `hafalans.length > 0` guard around the analytics block (task asked for the one-line wiring only).
   426→
   427→---
   428→Task ID: 12-c
   429→Agent: ppdb-check (full-stack-developer)
   430→Task: Public "Cek Status Pendaftaran" — secure no-login PPDB status lookup (new anti-enumeration check API + ppdb-section UI).
   431→
   432→Work Log:
   433→- Read worklog.md (head 120 + tail 150, re-read tail before append — 12-a landed in parallel), api/registrations/route.ts, prisma/schema.prisma (Registration model), ppdb-section.tsx (fully), api-client.ts, src/lib/api.ts (ok/bad), admin overview.tsx statusBadgeClass (PENDING amber / VERIFIKASI teal / DITERIMA emerald / DITOLAK red) and ppdb's own STATUS_STYLES/STATUS_LABELS (same hue mapping); probed lucide 0.525 (FileSearch/Search/Quote/ShieldCheck/RotateCcw all true).
   434→- NEW src/app/api/registrations/check/route.ts: GET ?regNumber&phone, doc-comment contract header. regNumber trim+uppercase → findUnique (stored "PPDB-2025-XXXX" uppercase, so uppercased compare = case-insensitive); phone digits via replace(/\D/g,'') — match requires input ≥5 digits AND last-5 of input === last-5 of stored (full or partial phone accepted). 400 'Nomor pendaftaran dan nomor HP wajib diisi' when either missing; 404 GENERIC 'Pendaftaran tidak ditemukan. Periksa kembali nomor Anda.' when regNumber unknown OR phone mismatch (never reveals which field failed); 200 SAFE FIELDS ONLY {regNumber, childName, parentName, status, reviewNote, createdAt, updatedAt}; try/catch → 400 'Gagal memeriksa status'.
   435→- EDIT src/components/public/ppdb-section.tsx (only this file): (1) section header gains centered emerald-outline pill toggle "Sudah mendaftar? Cek Status" (FileSearch, min-h-11 = 44px, aria-expanded/aria-controls) toggling checkOpen; (2) NEW collapsible card #ppdb-status-check below the header (rounded-2xl border-emerald-200 bg-white p-6/md:p-8 max-w-3xl) with 2-field form — Nomor Pendaftaran (mono uppercase, placeholder "PPDB-2025-0001") + No. HP (type/inputMode tel, maxLength 20, placeholder "5 digit terakhir nomor HP"), labels htmlFor, error-clears-on-type, full-width emerald submit "Periksa Status" (min-h-11, Loader2 "Memeriksa Status…" state); anti-enumeration note text-[11px] text-stone-500 + ShieldCheck "Masukkan nomor pendaftaran dan 5 digit terakhir nomor HP yang digunakan saat mendaftar."; (3) results region wrapped aria-live="polite": role="alert" red inline error (surfacing the API's 400/404 message); success = rounded-xl border card: Calon Santri "An. {childName}" + "Wali: {parentName}", status Badge reusing existing STATUS_STYLES+STATUS_LABELS (admin-mapping: PENDING amber/VERIFIKASI teal/DITERIMA emerald/DITOLAK red), 3-col info grid (mono regNumber, "Diajukan" + "Diperbarui" via formatShortDate id-ID), DITOLAK → prominent red panel (ShieldAlert "Pendaftaran Belum Dapat Kami Terima" + quoted reviewNote), any other status with reviewNote → amber border-l-4 quote block ("Catatan Pengurus", Quote icon), DITERIMA → emerald line "Alhamdulillah! Silakan menunggu informasi kelas dan akun Portal Wali via WhatsApp.", PENDING/VERIFIKASI → mini 3-step timeline (CHECK_STEPS Diajukan→Verifikasi→Keputusan, size-4 dots: done = solid emerald + Check, current = emerald + ring-4 emerald halo, upcoming = stone outline; connecting lines emerald-500 when passed else stone-200; simple flex -mt-2 alignment); footer h-11 outline "Periksa Lagi" (RotateCcw) → resetCheck clears number/phone/result/error; (4) REMOVED the old right-column "Cek Status" widget + its single-field insecure logic (it fetched the FULL /api/registrations list and matched client-side — leaks all parents' phone/email/address to any visitor and is superseded by the phone-verified API; single-field UX cannot satisfy the new phone guard). Right column keeps the "Butuh Bantuan?" card; header container mb-12→mb-8 to seat the toggle (grid spacing preserved via card mb-12). Success dialog / form / other sections untouched.
   436→- QA: baseline GET /api/registrations → PPDB-2025-0004/081500000099 DITERIMA, -0003 DITERIMA, -0002 VERIFIKASI, -0001 PENDING. curl suite: (a) no params & only regNumber → 400 "Nomor pendaftaran dan nomor HP wajib diisi"; (b) PPDB-2025-9999+00099 → 404 generic; (c) right regNumber+12345 → 404 generic (lowercase regNumber variant too); <5-digit phone → 404 generic; (d) PPDB-2025-0004+00099 → 200 safe fields; (d2) "+62 815-0000-0099" (non-digits stripped) → 200; (e) full 081500000099 → 200; (f) lowercase ppdb-2025-0001+00001 → 200 PENDING. Leak grep over all 200 bodies for "phone|email|address|documents|note|id|gender|birthDate" → 0 matches. Browser E2E (agent-browser): toggle opens/closes panel (a11y snapshot shows labeled textboxes + Periksa Status), DITERIMA flow renders badge+dates+WA line, wrong-phone shows role=alert generic message, PENDING flow renders "Menunggu Verifikasi" badge + 3-step timeline; fresh-session page load + full interaction → errors:[] zero console/page errors (transient mid-edit HMR "SearchCheck is not defined" traces were from my own two-batch edit window — final served chunk contains no SearchCheck, verified via curl of the chunk + grep of source). dev.log: every /api/registrations/check line shows expected 400/404/200, no runtime errors; unrelated PUT /api/students 400 lines are 12-a's concurrent QA. bun run lint clean; bunx tsc --noEmit | grep -E "ppdb|registrations" → empty.
   437→- Note: browser E2E was run read-only against existing demo registrations — no data created/deleted; no test rows to clean.
   438→
   439→Stage Summary:
   440→- New API: GET /api/registrations/check?regNumber&phone → 200 {regNumber, childName, parentName, status, reviewNote, createdAt, updatedAt} | 400 (missing params / catch-all) | 404 generic (no match). Anti-enumeration: BOTH required, phone matched by last-5 digits ignoring non-digits (min 5 digits), case-insensitive regNumber, generic 404 never reveals failing field, response excludes phone/email/address/documents/note/gender/birthDate/id.
   441→- UI contract: PpdbSection header has a persistent emerald-outline "Sudah mendaftar? Cek Status" toggle (aria-expanded/controls="ppdb-status-check") revealing an inline collapsible card with the 2-field check form, aria-live results region, admin-consistent status badge colors, Indonesian dates (formatShortDate), quoted reviewNote (red-prominent for DITOLAK), DITERIMA next-step line, PENDING/VERIFIKASI 3-step timeline (Diajukan→Verifikasi→Keputusan), and "Periksa Lagi" reset. No Dialog used (inline card matches the section's card rhythm; Escape-safety N/A).
   442→- Files: NEW src/app/api/registrations/check/route.ts; EDIT src/components/public/ppdb-section.tsx only. registrations/route.ts, parent/*, admin/* all untouched.
   443→- QA results: 9/9 curl scenarios pass incl. leakage grep (0 hits); lint clean; tsc clean in scope; browser E2E zero errors; dev server untouched & healthy on :3000.
   444→- Decisions: collapsible inline card over Dialog (section's visual rhythm is inline cards; better mobile flow on the tall section, keeps result visible next to the form); old right-column single-field widget REMOVED as redundant + privacy-leaking (full-list fetch defeats anti-enumeration; it cannot satisfy the new phone requirement); STATUS_STYLES/STATUS_LABELS reused from the file itself (already replicates admin statusBadgeClass mapping) for visual continuity; header button styled rounded-full emerald-outline to echo the section's eyebrow pills; timeline shows only for PENDING/VERIFIKASI per spec (DITERIMA/DITOLAK get dedicated outcome blocks instead).
   445→
   446→---
   447→Task ID: 12 (cron review round)
   448→Agent: main (Z.ai Code) + 3 subagents (12-a/12-b/12-c)
   449→Task: Round-12 — QA sweep, hafalan target planning, parent monthly attendance recap, public PPDB status check, privacy fix
   450→
   451→Work Log:
   452→- Reviewed worklog (round 11 complete). QA sweep: lint/tsc clean, dev.log clean, browser E2E public portal + console zero errors. Verified round-11 candidate "PPDB accept → auto-create User+Student" was ALREADY implemented in round 1 (registrations PUT DITERIMA path) → picked 3 new features.
   453→- [12-a subagent] Target Hafalan planning: schema Student.hafalanTarget String? (+db:push); students API POST/PUT accept it; portal/parent maps it; types.ts Student + parent child-summary typed; students-admin "Target Hafalan" Select (38 Juz-30 names, 'none' sentinel, edit prefill, h-11, helper caption) + amber "Target: X" chip in table rows + CSV "Target Hafalan" column; HafalanProgress({hafalans, target?}) — amber target strip (Target icon, "reached/position surah", amber Progress %, emerald "Tercapai" chip when target itself has setoran), ring-2 ring-amber-400 + icon overlay on target tile, new legend swatch, NORM_ALIASES (alkausar→alkautsar etc.) bridging spelling variants; child-panel passes child.hafalanTarget. Transient dev-restart (Prisma client reload) documented; historical "Fast Refresh" warnings at dev.log 562/564 are from that, not a current issue.
   454→- [12-b subagent] NEW src/components/parent/attendance-recap.tsx (standalone): exports AttendanceRecap({attendances}) + AttendancePoint + aggregateMonthly (tested: empty/unsorted/8-month cap/invalid dates). Card "Rekap Kehadiran Bulanan" — per-month stacked bars (segment hues verbatim from child-panel status colors: HADIR emerald-500/IZIN amber-500/SAKIT orange-500/ALPA red-500), flexGrow∝count min-w-2px, tooltips "HADIR 12 · IZIN 1…", rate chip (≥85 emerald/≥70 amber/else red) + "n/total", legend only present statuses, 6-month cap, null when empty, hydration-deterministic YYYY-MM keys.
   455→- [ain wiring] child-panel.tsx: import + render <AttendanceRecap attendances={child.attendances} /> between Riwayat Kehadiran and Progres Hafalan cards; types.ts ParentPortalData attendances status narrowed string → 'HADIR'|'IZIN'|'SAKIT'|'ALPA'.
   456→- [12-c subagent] NEW src/app/api/registrations/check/route.ts: GET ?regNumber&phone → safe fields only {regNumber, childName, parentName, status, reviewNote, createdAt, updatedAt}; 400 missing, 404 generic (never reveals which field wrong); regNumber trim+uppercase; phone matched by last-5 digits (formatted phones accepted). ppdb-section.tsx: "Sudah mendaftar? Cek Status" toggle + inline check card (2 fields, mono regNumber input, Loader2 submit, role=alert inline error, aria-live result); success panel = status badge + An. {childName} + Wali + NOMOR/DIAJUKAN/DIPERBARUI + status-specific guidance + DITOLAK red reviewNote panel + 3-step timeline for PENDING/VERIFIKASI + "Periksa Lagi" reset. SECURITY FIX: removed the old right-column "Cek Status" widget that fetched the FULL /api/registrations list client-side (leaked all parents' phone/email/address to any visitor).
   457→- [ain demo data] Set realistic targets via API: Rizky DJ-2024-001 → "An-Naba" (1/2, 50%), Alika DJ-2023-021 → "Abasa" (3/4, 75% + Tercapai) — kept as demo data.
   458→- E2E verified in browser: PPDB check UI (valid last-5 + formatted phone → Diterima panel; wrong phone → role=alert error; Periksa Lagi resets); parent portal Muhammad (recap card Sep 2026 stacked bar 33% red chip + legend; target strip "An-Naba 1/2 50%" + amber-ringed tile + legend "Target hafalan") and Alika ("Abasa" strip 3/4 75% + Tercapai chip + ✓ tiles); admin Santri table shows "Target: Abasa"/"Target: An-Naba" chips, edit dialog prefills Target Hafalan select w/ helper caption; zero console/page errors; lint + tsc clean after integration.
   459→
   460→Stage Summary:
   461→- Round 12 shipped: Student.hafalanTarget end-to-end (schema→API→admin form/chip/CSV→parent target strip), parent Rekap Kehadiran Bulanan (stacked monthly bars), public PPDB Cek Status (guarded lookup + result panel), and a privacy fix (removed public widget that leaked registrations data).
   462→- New API: GET /api/registrations/check?regNumber&phone → {regNumber, childName, parentName, status, reviewNote, createdAt, updatedAt} | 400 | 404-generic.
   463→- Contracts: HafalanProgress takes optional target?: string|null; AttendanceRecap({attendances: AttendancePoint[]}) null-safe; PUT/POST /api/students accept hafalanTarget ('none'→null at UI).
   464→- Demo data: Rizky target An-Naba, Alika target Abasa (kept); registrations 4 rows incl. 1 DITERIMA (Salman) for check demos (regNumber PPDB-2025-0004 + last5 00099).
   465→- Next-step candidates (priority order): (1) hafalan-admin form shows student's current target inline ("Target santri: An-Naba") + warn when setoran passes the target, (2) guru-overview "santri mendekati target" nudge card, (3) admin students detail drawer (per-student hafalan/absensi/tagihan tabs), (4) dark-mode audit, (5) WhatsApp log filters (search by phone/status/date + export CSV).
   466→
   467→---
   468→Task ID: 13-b
   469→Agent: wa-log-filters (frontend-styling-expert)
   470→Task: WhatsApp log — search, status filter chips with counts, combined result info, filtered CSV export, empty-filter state
   471→
   472→Work Log:
   473→- Read worklog.md (head 120 + tail 150, re-read tail before append — unchanged at 465 lines), whatsapp-log.tsx fully (239 lines), verified shared helpers in overview.tsx (csvCell quotes+escapes ; / quotes / newlines, csvDate dd/mm/yyyy, csvFileStamp YYYY-MM-DD, downloadCsv = BOM \uFEFF + ';' separator + a.click()); read sibling idioms (payments/students toolbar search: relative wrapper + Search left-3 + rounded-xl border-stone-200 bg-white pl-9 + outline Ekspor CSV with Loader2/Download swap + disabled loading||exporting||filtered.length===0; registrations-admin Tabs filter with per-status text-[10px] counts); confirmed /api/notifications GET returns NotificationLog[] (userId, phone, message, status, createdAt — NO recipient name) and GET /api/users returns AppUser[] (id+name) for the Penerima mapping; probed lucide 0.525: Search/X/Download/Loader2/SearchX/RotateCcw all exported.
   474→- Edited src/components/admin/whatsapp-log.tsx ONLY (239→~360 lines): (1) toolbar search Input h-11 rounded-xl border-stone-200 bg-white pl-9 pr-11 aria-label "Cari pesan" placeholder "Cari penerima, pesan, atau nomor…", wrapper w-full sm:min-w-52 sm:flex-1 (mobile full-width then filters wrap), clear = ghost size="icon" Button absolute right-0.5 size-11 (=44px target, twMerge overrides size-9) rounded-full X icon, rendered only when search non-empty, aria-label "Hapus pencarian"; (2) segmented status chips (module-level STATUS_CHIPS: SEMUA 'Semua'/SENT/PENDING/FAILED) as raw buttons mirroring buttonVariants base (h-9 rounded-full border px-3.5 text-sm font-medium transition-all outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]), active = emerald solid (border-emerald-700 bg-emerald-700 text-white hover:bg-emerald-800), inactive = border-stone-200 bg-white text-stone-600 hover:border-emerald-300 hover:bg-emerald-50/60, aria-pressed, count badge rounded-full px-1.5 py-0.5 text-[10px] font-semibold (inactive tinted per status: stone/emerald-100/amber-100/red-100 — matches statusBadgeClass hues; active bg-white/20 text-white), role="group" aria-label "Filter status pesan"; (3) combined info row below toolbar: "Menampilkan X dari Y pesan" text-xs text-stone-500 + "Bersihkan filter" ghost sm Button (RotateCcw) when hasFilter (search.trim() || statusFilter!=='SEMUA') → resetFilters(); right side ml-auto keeps ALL prior actions (Kirim Pesan Manual emerald, refresh icon "Muat ulang") plus new outline "Ekspor CSV"; (4) exportCsv mirrors round-9 exactly (setExporting guard, 200ms delay, downloadCsv, finally reset): filename `log-whatsapp-${csvFileStamp()}.csv`, headers [Tanggal, Penerima, No. HP, Pesan, Status], rows from `filtered` (same state the cards render), Tanggal=csvDate(createdAt), Penerima=usersById.get(userId) ?? '', Pesan/phone sanitized by shared csvCell inside downloadCsv; toast {title:'Ekspor CSV berhasil', description: `${filtered.length} data pesan tersimpan di ${filename}.`}; (5) NEW empty-filter state (filtered.length===0 && messages.length>0): Card border-2 border-dashed border-stone-300 bg-stone-50/50 + SearchX + "Tidak ada pesan yang cocok dengan filter." + emerald-outline "Bersihkan Filter" reset — existing Inbox "Belum ada pesan" card untouched for truly-zero messages; list maps `filtered` instead of `messages`; card header now also shows recipient name "· {name}" (text-xs stone-500) next to the phone when userId resolves; (6) users fetched once on mount (isAdmin branch, apiGet /api/users .then(setUsers).catch(() => setUsers([])) — silent fallback so log still renders/searches phone+message if users fetch fails); usersById useMemo Map; counts useMemo; filtered useMemo (status gate + q across recipient name / phone / message, case-insensitive, trim). All new hooks (usersById/counts/filtered) placed BEFORE the !isAdmin early return (rules-of-hooks safe).
   475→- Note: this section never had a "Tandai semua dibaca" action (that lives in the parent portal inbox) — existing actions here = Kirim Pesan Manual dialog + Muat ulang refresh, both preserved verbatim.
   476→- QA: `bun run lint` → clean (eslint . exit 0); `bunx tsc --noEmit 2>&1 | grep whatsapp` → empty; full tsc output = only the 4 pre-existing examples/+skills/ errors. Dev server NOT run; overview.tsx and all other files untouched; no new deps.
   477→
   478→Stage Summary:
   479→- File changed (only): src/components/admin/whatsapp-log.tsx. WhatsAppLog now has: h-11 clearable search (aria-label "Cari pesan") across recipient name+phone+message, SEMUA/SENT/PENDING/FAILED chip filter with live counts (emerald-solid active), "Menampilkan X dari Y pesan" + conditional "Bersihkan filter" ghost, "Ekspor CSV" outline (log-whatsapp-<stamp>.csv, BOM+';', filtered rows only, disabled when loading/exporting/empty, Loader2 swap, round-9 toast wording), dashed-border empty-filter card with reset (distinct from the zero-data Inbox card), recipient name shown on cards when resolvable via /api/users map.
   480→- Decisions: recipient name sourced client-side from GET /api/users (notifications API returns userId only; section is ADMIN-guarded; silent catch → search degrades to phone+message) — no API changes allowed in scope; chips implemented as custom buttons with the exact buttonVariants focus-visible/ring idiom instead of Tabs because the task mandates "active chip = emerald solid" + badge counts (registrations' TabsList can't do emerald-solid without overriding its internals anyway); chip count badges reuse the status hue map from statusBadgeClass (stone/emerald/amber/red) so chips visually key into the card badges; search input h-11 (task spec) vs siblings' default h-9 — deliberate to seat the 44px clear target flush inside the field; export reads `filtered` (exports exactly what the user sees, round-9 contract).
   481→- QA results: lint clean; tsc clean in scope (4 pre-existing unrelated errors remain); no dev server, no other files modified, no data/API changes.
   482→
   483→---
   484→Task ID: 13-a
   485→Agent: target-workflow (frontend-styling-expert)
   486→Task: Target-aware hafalan workflow for guru/admin — shared hafalan-utils lib, inline target strip + live surah hint + riwayat target markers in hafalan-admin, "Progres Target Santri" nudge card in guru-overview.
   487→
   488→Work Log:
   489→- Read worklog.md (head 120 + tail 180; round 12 shipped Student.hafalanTarget + parent target strip) and read hafalan-chart.tsx, hafalan-admin.tsx, guru-overview.tsx fully; confirmed /api/students rows carry hafalanTarget (Student type) and hafalan-admin loads /api/hafalan rows incl. h.student + h.studentId; verified lucide 0.525 exports Crosshair/Target/Sparkles/CheckCircle2; dev server healthy on :3000 (no restarts).
   490→- NEW src/lib/hafalan-utils.ts: extracted verbatim from hafalan-chart.tsx — JUZ30_SURAHS (38 entries), norm(), NORM_ALIASES (alkausar→alkautsar, allahab→almasad, aththariq→attariq), resolveNorm(), JUZ30_NORM — plus NEW pure targetProgress(hafalans, target): null when target falsy/blank or outside Juz-30 (resolveNorm lookup); position = 1-based list index; reached = distinct list surahs at index ≤ position with ≥1 setoran (Set of resolveNorm(surahName)); percent = round(reached/position*100); targetReached = target surah itself has setoran; targetName = canonical list name. All exports: JUZ30_SURAHS, norm, NORM_ALIASES, resolveNorm, JUZ30_NORM, targetProgress (+ TargetProgress interface). No React, no fetch, hydration-safe.
   491→- REFACTOR src/components/parent/hafalan-chart.tsx (minimal): deleted the local 38-entry list + norm/NORM_ALIASES/resolveNorm/JUZ30_NORM, replaced with `import { JUZ30_NORM, JUZ30_SURAHS, norm, resolveNorm } from '@/lib/hafalan-utils'`. All computation logic (setoranCount, targetInfo, targetReached, targetPercent, targetDone) untouched → parent strip behavior identical.
   492→- bun -e verification of targetProgress (post-refactor):
   493→  Rizky  target An-Naba : {"valid":true,"position":2,"reached":1,"percent":50,"targetReached":false,"targetName":"An-Naba"}
   494→  Alika  target Abasa   : {"valid":true,"position":4,"reached":3,"percent":75,"targetReached":true,"targetName":"Abasa"}
   495→  PASS: both fixtures match expected values (also spot-checked: null target → null, non-Juz-30 target "Al-Baqarah" → null).
   496→- EDIT src/components/admin/hafalan-admin.tsx: (1) inline amber strip (rounded-xl border-amber-200 bg-amber-50 p-3, role="status", aria-label) rendered right after the Santri field when the selected student has hafalanTarget: amber-600 Target icon + "Target santri: **{name}**" (bold) + "{reached}/{position} surah ({percent}%)" + Progress h-1.5 bg-amber-200 [&>div]:bg-amber-500 + emerald CheckCircle2 chip "Target sudah tercapai — pertahankan!" when targetReached; computed via targetProgress over hafalans filtered to studentId; degraded copy "Target di luar peta Juz 30 — progres tidak dihitung." if target unresolvable (parity with parent strip hiding). (2) Live surah hint under the "Nama Surah" datalist input in a persistent aria-live="polite" container id="surah-target-hint": typed surah resolveNorm-matches target → emerald Sparkles line "Surat ini adalah target santri — setoran ini akan menyelesaikan target!" (or amber "Surat target sudah tercapai sebelumnya." when targetReached); silent when no target/no match. (3) Riwayat rows: tiny amber Target icon (title/aria "Target tercapai") appended next to surah text when resolveNorm(h.surahName) === resolveNorm(target of h.studentId) via a precomputed targetHitIds Set (per-row owner's target, not the selected one). No-target students → nothing renders (backward compatible).
   497→- EDIT src/components/admin/guru-overview.tsx: added full-width "Progres Target Santri" Card AFTER WeeklySchedule — CardHeader with Crosshair in size-9 rounded-lg bg-amber-100 text-amber-700 tile (matches WeeklySchedule header pattern), description "Ketekunan setoran dibanding target hafalan tiap santri."; body rows = ownStudents with a target that resolves in Juz-30 via targetProgress over ownHafalan grouped by studentId (no new fetches — reuses loaded students+hafalans state; invalid/no targets skipped); each li: emerald initials avatar (new initialsOf helper, max 2 letters) + name + class · Target name, amber mini progress track (h-1.5 rounded-full bg-stone-100, amber-500 fill width=percent, role="progressbar" aria-valuenow/min/max), "{reached}/{position}" font-mono tabular-nums, status chip: targetReached||percent>=100 → emerald CheckCircle2 "Target tercapai"/"Tercapai" (targetReached wins), percent>=60 → emerald "Mendekati", else amber "Progres {percent}%"; sorted targetReached-first then percent desc; rows non-interactive with title tooltip "{name} — target {targetName}, {reached}/{position} surah"; role="list"/"listitem", flex-wrap mobile-safe, max-h-64 thin-scrollbar (existing SCROLL_AREA) when >4 rows; empty state = dashed note "Belum ada santri dengan target hafalan. Atur melalui menu Santri." with no CTA.
   498→- QA: `bun run lint` → clean (exit 0); `bunx tsc --noEmit 2>&1 | grep -vE "examples/|skills/"` → empty (exit 1 = zero matches); both bun -e fixtures pass post-refactor; GET /api/hafalan | head -c 300 still returns student.className-shaped rows (contract unchanged); git scope check confirms only the 4 allowed files touched (students-admin/whatsapp-log/student-detail-drawer changes visible in git belong to parallel agents, untouched by me).
   499→- Note: worklog tail re-read immediately before append (still 465 lines, round-12 end) — no concurrent changes to merge.
   500→
   501→Stage Summary:
   502→- New lib contract: src/lib/hafalan-utils.ts exports JUZ30_SURAHS (38), norm, NORM_ALIASES, resolveNorm, JUZ30_NORM, targetProgress(hafalans: {surahName}[], target: string|null|undefined) → TargetProgress|null {valid, position, reached, percent, targetReached, targetName}. Single source of truth for target math — parent chart and guru/admin UIs can never drift.
   503→- hafalan-chart.tsx is now a thin consumer (imports only); its strip/coverage-map behavior is byte-equivalent in logic. hafalan-admin gains: target strip (only when selected student has target), aria-live surah-target hint, per-row amber Target markers. guru-overview gains the "Progres Target Santri" card (no new API calls; rows only for own-class students with resolvable targets; targetReached-first sorting).
   504→- Styling kept emerald/amber/stone, rounded-xl/2xl, aria labels/roles/live regions throughout; no Date.now/random in render paths (all target math pure from fetched props/state).
   505→- QA results: lint clean; tsc clean (minus examples/skills); targetProgress fixtures 2/2 exact (Rizky 2/1/50%/false, Alika 4/3/75%/true); GET /api/hafalan contract verified unchanged; dev server untouched on :3000.
   506→- Files changed (4, within scope): NEW src/lib/hafalan-utils.ts; EDIT src/components/parent/hafalan-chart.tsx (imports only), src/components/admin/hafalan-admin.tsx, src/components/admin/guru-overview.tsx.
   507→
   508→---
   509→Task ID: 13-c
   510→Agent: student-drawer (frontend-styling-expert)
   511→Task: Per-student detail drawer for admin — right-side Sheet from each Santri row's Aksi dropdown with Hafalan / Absensi / Tagihan tabs.
   512→
   513→Work Log:
   514→- Read worklog.md (head 120 + tail, re-read tail before append — 13-a/whatsapp-log landed in parallel), students-admin.tsx fully (555 lines), types.ts, api-client.ts, ui/sheet.tsx + ui/tabs.tsx, statusBadgeClass in overview.tsx, type/grade/status color maps in hafalan-admin + child-panel + payments-admin, prisma schema (Attendance has NO own date; session holds date+topic but API strips it).
   515→- curl shape verification (dev server untouched, SID = Rizky cmtufisd1000sq12wyuomvtbq from GET /api/students):
   516→  * /api/hafalan?studentId= → [{id, studentId, surahName, ayatRange, type, grade (int|null), teacherNote (string|null), createdAt, student:{id, fullName, nis, className}}] — 5 rows for Rizky.
   517→  * /api/attendance?studentId= → [{id, sessionId, studentId, status, note (string|null), createdAt, student:{...}, className}] — confirmed NO date/topic keys (session: undefined is dropped by JSON; className mapped) → coded date from createdAt, secondary line = className + note. 3 rows.
   518→  * /api/payments?studentId= → [{id, invoiceNo, studentId, title, amount (float), method (string|null), status, paidAt (string|null), createdAt, student:{id, fullName, nis, parent:{id, name, phone}}}] — 3 rows (1 PENDING 75000, 2 SUCCESS 75000).
   519→- NEW src/components/admin/student-detail-drawer.tsx: export StudentDetailDrawer({student, open, onOpenChange}) — shadcn Sheet side="right" w-full sm:max-w-md p-0 flex-col. Header (stone-50/60, border-b): emerald-700 initials avatar size-12 + SheetTitle fullName + sr-only SheetDescription + chips (NIS font-mono stone, class emerald-50 outline, status badge via copied studentStatusBadge mapping (AKTIF emerald/LULUS violet/NONAKTIF stone — students-admin's local fn is not exported), amber "Target: X" chip when hafalanTarget); identity dl divide-y stone-100 rows with lucide icons (User jenis kelamin, Cake tanggal lahir formatShortDate, MapPin alamat, Phone wali+phone); WhatsApp-style demo note (emerald-50 bubble rounded-2xl rounded-bl-sm, MessageCircle, NOT a link) when parent.phone exists. Tabs (Radix, keyboard-navigable): TabsList grid-cols-3 with h-11 triggers (≥44px targets), per-tab content min-h-0 flex-1 overflow-y-auto + [scrollbar-width:thin] emerald thumb. Fetch: single useEffect on [open, studentId] — fires only when open && studentId; resets tab to 'hafalan' + clears data/errors and bumps per-tab genRef (stale-response cancel guard) each open/student change; fetches all three endpoints in parallel via apiGet; per-tab loading skeleton (3×h-16), per-tab destructive Alert + h-11 "Coba Lagi" retry, Inbox empty states ("Belum ada setoran hafalan." / "Belum ada data absensi." / "Belum ada tagihan."). Hafalan tab: summary "X setoran · rata-rata Y" (avg of non-null grades), rows = grade circle (>=85 emerald-600 / >=70 amber-500 / else red-500, null stone-300 "—" — child-panel thresholds), "QS {surah} ayat {range}", type badge matching hafalan-admin exactly (TAHFIDZ emerald / TAHSHIN amber / MURAJAAH teal — task said "stone" but its own tie-breaker "match hafalan-admin" wins; hafalan-admin uses teal), italic truncated teacherNote w/ title, formatShortDate createdAt; role=list/listitem, rounded-xl border-stone-100 hover:bg-stone-50 transitions. Absensi tab: summary chips H/I/S/A with status dots (emerald/amber/orange/red) + rate chip (>=85 emerald / >=70 amber / else red; rate = hadir/total), rows = date + className + note + status badge via imported statusBadgeClass (HADIR emerald/IZIN amber/SAKIT orange/ALPA red — matches parent portal). Tagihan tab: two stat tiles "Total dibayar" (emerald, SUCCESS sum) / "Tunggakan" (amber, PENDING sum — same rule as portal/parent billing.outstanding), rows = mono invoiceNo + status badge (statusBadgeClass: SUCCESS emerald/PENDING amber/FAILED red — payments-admin uses the same import), title + formatRupiah(amount), "Dibayar {paidAt}" else "Dibuat {createdAt}".
   520→- EDIT src/components/admin/students-admin.tsx ONLY (5 spots): +IdCard lucide import; +import StudentDetailDrawer; +detailStudent/detailOpen state; "Detail" DropdownMenuItem (IdCard emerald icon) as FIRST item of the per-row Aksi menu (before Edit); drawer mounted before the Hapus AlertDialog. Nothing else touched (git diff verified).
   521→- Browser E2E (agent-browser, admin session): Aksi menu shows Detail first; drawer opens for Alika (chips NIS/kelas/AKTIF/"Target: Abasa", WhatsApp note, "6 setoran · rata-rata 85", type labels Tahshin/Murajaah/Tahfidz, grade circles) and Rizky (Hafalan "5 setoran · rata-rata 86"; Absensi "H 1 I 1 S 1 A 0" + "33% kehadiran" + rows w/ class+note+badges; Tagihan "Total dibayar Rp 150.000 · Tunggakan Rp 75.000" + 3 invoice rows with correct badges); network log shows exactly 3 API GETs per open (hafalan/attendance/payments?studentId=, all 200) only while open; Escape closes (sheet-content removed); zero page errors.
   522→
   523→Stage Summary:
   524→- Contract: StudentDetailDrawer({student: Student | null, open: boolean, onOpenChange: (o: boolean) => void}) in src/components/admin/student-detail-drawer.tsx; internally keys fetches by student.id + open (fetch only when open && student; full state reset per open → always-fresh data), per-tab genRef stale-guard, no new deps, hydration-safe (no Date/random in render; all dates via formatShortDate).
   525→- Response shapes coded against (curl-verified): hafalan rows {surahName, ayatRange, type, grade|null, teacherNote|null, createdAt, student.className}; attendance rows {status, note|null, createdAt, className} — NO date/topic (session stripped by API; createdAt is the session-day date, className replaces topic); payment rows {invoiceNo, title, amount, status, paidAt|null, createdAt, student.parent}.
   526→- Wiring notes for main agent: nothing further needed — students-admin already mounts the drawer; drawer is ADMIN/GURU-agnostic (renders whatever student row it is given); Color maps: status via shared statusBadgeClass('./overview'), student status copied (not exported in students-admin), hafalan type matches hafalan-admin (MURAJAAH=teal, documented deviation from the brief's "stone" hint), grade/attendance-rate thresholds match child-panel/attendance-recap.
   527→- Files changed: NEW src/components/admin/student-detail-drawer.tsx (~430 lines); EDIT src/components/admin/students-admin.tsx (+13 lines, 5 spots). No API/schema/other files touched; no new packages; dev server not started/stopped.
   528→- QA results: bun run lint → clean (exit 0); bunx tsc --noEmit | grep -E "student-detail|students-admin" → empty; curl evidence head -c 300 captured for all three endpoints (see Work Log); browser E2E all 3 tabs + a11y (dialog name = student, sr-only description, tablist keyboard focusable, Escape close, role=list lists, 44px tab/retry targets) pass with zero console/page errors.
   529→
   530→---
   531→Task ID: 13 (cron review round)
   532→Agent: main (Z.ai Code) + 3 subagents (13-a/13-b/13-c)
   533→Task: Round-13 — QA sweep, target-aware hafalan workflow (shared lib + admin form + guru nudge card), WhatsApp log filters/export, per-student detail drawer
   534→
   535→Work Log:
   536→- Reviewed worklog (round 12 complete). QA sweep: lint/tsc clean, dev.log clean, browser E2E + console zero errors. STABLE → features. Verified /api/hafalan & /api/attendance & /api/payments already accept ?studentId (no API changes needed this round).
   537→- [13-a subagent] (1) NEW src/lib/hafalan-utils.ts: JUZ30_SURAHS/norm/NORM_ALIASES/resolveNorm/JUZ30_NORM + targetProgress() extracted from hafalan-chart.tsx (parent) — hafalan-chart refactored to import (behavior identical; fixtures re-proven: Rizky An-Naba → 1/2 50% notReached, Alika Abasa → 3/4 75% reached). (2) hafalan-admin.tsx: amber "Target santri" strip in Catat Setoran form when selected student has target (name, reached/position, %, amber Progress, emerald "Target sudah tercapai" chip when reached; degraded note for non-Juz-30 targets), aria-live surah hint under Nama Surah ("Surat ini adalah target santri — setoran ini akan menyelesaikan target!" emerald Sparkles / "sudah tercapai sebelumnya" amber), amber Target markers in Riwayat rows whose surah hits that row's student target (targetHitIds set). (3) guru-overview.tsx: "Progres Target Santri" card after WeeklySchedule — rows for own-class students w/ resolvable targets (initials avatar, name + "Kelas · Target: X", amber mini progress bar, mono "r/p", chip: targetReached wins emerald "Target tercapai" / ≥100 "Tercapai" / ≥60 "Mendekati" / amber "Progres n%"), sorted reached-first-then-percent, role=list, max-h-64 thin scrollbar, dashed empty note.
   538→- [13-b subagent] whatsapp-log.tsx (+164 net): toolbar search (Search icon wrapper, clearable 44px X, aria-label "Cari pesan") across name/phone/message; segmented status chips SEMUA/SENT/PENDING/FAILED with live count badges (active emerald solid, aria-pressed); "Menampilkan X dari Y pesan" + "Bersihkan filter" reset; "Ekspor CSV" mirroring round-9 (filtered rows, BOM+';', log-whatsapp-<stamp>.csv, Loader2 swap, toast); dashed SearchX empty-filter state distinct from zero-data Inbox state; message cards show "· {recipient}" via /api/users id→name map (silent fallback); all existing actions preserved.
   539→- [13-c subagent] NEW src/components/admin/student-detail-drawer.tsx (~430 lines): StudentDetailDrawer({student, open, onOpenChange}) — Sheet side=right sm:max-w-md; header initials + NIS/class/status chips + amber target chip; identity rows (gender/birth/address/wali+phone) + WhatsApp info note; 3 lazy tabs (fetch-all-on-open, per-tab genRef stale guards, skeletons, error+retry, empty states): Hafalan (surah+ayat, type badge TAHFIDZ emerald/TAHSHIN amber/MURAJAAH teal, grade circle, "X setoran · rata-rata Y"), Absensi (date/className/note rows + status badges + H/I/S/A chips + rate), Tagihan (invoiceNo mono, formatRupiah, status badges, "Total dibayar · Tunggakan"); students-admin.tsx: "Detail" as FIRST Aksi menuitem (IdCard icon) + drawer mount + state (5 edit spots only).
   540→- [ain demo data] Salman DJ-2025-009 → target Al-Fatihah (new registrant, first surah), Hamzah DJ-2024-005 (Iqra 4, Fatimah's student) → target Al-Fatihah (0/1 Progres 0% — has no setoran yet). Kept as demo.
   541→- E2E verified in browser: hafalan form — Rizky selected → strip "Target santri: An-Naba 1/2 surah (50%)", typing "An-Naba" → emerald Sparkles hint; guru Ringkasan — nudge card empty state (before Hamzah had target) then populated row (Hamzah · Iqra 4 · Target: Al-Fatihah · 0/1 · Progres 0%) after reload; WA log — chips Semua 13/SENT 13/PENDING 0/FAILED 0, search "Zahra" → "Menampilkan 2 dari 13 pesan"; student drawer (Salman) — identity + Hafalan empty state + Tagihan "Total dibayar Rp 0 · Tunggakan Rp 75.000" + INV-2025-1010 PENDING row; parent portal re-verified post-refactor (target strip An-Naba 50% + amber-ringed tile intact); zero console/page errors; lint + tsc clean after integration.
   542→
   543→Stage Summary:
   544→- Round 13 shipped: shared hafalan-utils lib (single source of Juz-30 target math across admin+guru+parent), target-aware setoran form (strip + live hint + riwayat markers), guru "Progres Target Santri" nudge card, WA log search/status-filters/CSV export, per-student detail drawer (Hafalan/Absensi/Tagihan) wired into Santri Aksi menu.
   545→- Contracts: targetProgress() in '@/lib/hafalan-utils'; StudentDetailDrawer({student, open, onOpenChange}) from '@/components/admin/student-detail-drawer'; no API changes.
   546→- Demo data: Hamzah + Salman targets Al-Fatihah (both 0/1, Progres 0%) alongside Rizky An-Naba 50% / Alika Abasa 75% Tercapai.
   547→- Known notes: hafalan GET take:100 cap still applies (fine for demo scale); WA recipient names resolved client-side via /api/users (silent fallback keeps search working if it fails).
   548→- Next-step candidates (priority order): (1) hafalan POST → auto "target reached" WhatsApp celebration to parent when setoran completes the student's target, (2) admin dashboard announcement composer preview (WA-style preview before broadcast), (3) PPDB stats mini-chart on admin overview (registrations per week), (4) dark-mode audit, (5) session QR history archive view.
   549→