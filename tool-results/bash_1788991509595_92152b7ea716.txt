
Work Log:
- Read worklog.md (head 120 + tail 150; re-read tail before append — 14-a celebration entry landed mid-flight at lines 550-568; its hafalan route/admin changes are that agent's scope, untouched by me). Read src/components/admin/overview.tsx fully (statusBadgeClass hues, KpiCard tile idiom, LineChart conventions: CartesianGrid #e7e5e4 "3 3" vertical=false, tick 11px #78716c, stroke #d6d3d1, margin {top:8,right:12,bottom:0,left:-8}, Tooltip contentStyle borderRadius 12 / border #e7e5e4 / fontSize 12), src/app/api/registrations/route.ts GET (findMany orderBy createdAt desc, no cap → Registration[] with createdAt+status), src/app/api/stats/route.ts, src/lib/types.ts; verified lucide-react TrendingUp/TrendingDown and recharts Bar/BarChart exports exist.
- EDIT src/components/admin/overview.tsx ONLY: (1) imports +useMemo, +TrendingDown/TrendingUp, +Bar/BarChart, +Registration type; (2) module-level PPDB_SERIES config (PENDING #d97706 "Menunggu", VERIFIKASI #0d9488 "Verifikasi", DITERIMA #059669 "Diterima", DITOLAK #dc2626 "Ditolak" — hexes matched to the statusBadgeClass hue families amber/teal/emerald/red) + WeekBucket interface + pure helpers pad2/dayKey/mondayOf/buildWeekBuckets; (3) load() extended to Promise.all([apiGet /api/stats, apiGet /api/registrations]) with Array.isArray guard, SAME single try/catch → destructive Alert + "Coba Lagi" (registrations failure fails the section like stats failure — file's existing error/retry pattern); (4) useMemo weekBuckets + derived totals + ppdbAriaLabel (all pure functions of fetched data, no Date.now/wall-clock in render → hydration-safe); (5) one extra <Skeleton h-64> added to the loading skeleton for the new card slot; (6) new full-width Card inserted directly after "Tren Kehadiran 7 Hari" (trend cards grouped, all existing cards intact): header = ClipboardList in emerald pastel tile (size-9 rounded-xl bg-emerald-100 text-emerald-700) + title "PPDB — Pendaftar 6 Minggu" + description "Tren pendaftaran santri baru per minggu."; stacked recharts BarChart (4× Bar stackId="a", maxBarSize 36, NO radius — per-bar radius on 4 stacked series renders broken caps) in h-40 sm:h-48 container; XAxis Senin-start dd/MM labels interval={0} 11px #78716c, YAxis allowDecimals={false}, stone-tinted hover cursor, Tooltip labelFormatter → "Minggu ke-N · dd/MM–dd/MM" + Indonesian series labels, contentStyle matching the LineChart; inline legend of 4 status dots (size-2.5, STATUS_DOTS idiom); stat row "Total: N pendaftar · Menunggu: X · Diterima: Y" with tiny amber/emerald dots; momentum chip: weekRising (latest>0 && latest>=prev) → emerald TrendingUp "Momentum naik (N minggu terakhir)" else stone TrendingDown "Menurun (...)"; zero total → dashed empty state "Belum ada pendaftar dalam 6 minggu terakhir." (same pattern as sibling empty states); chart container role="img" aria-label summarizing total + per-status counts.
- Aggregation logic: window anchored at Monday (Senin-start, local midnight, dayKey = y*10000+m*100+d → DST/tz-safe comparisons) of the LATEST registration createdAt — not Date.now — giving 6 buckets oldest→newest labeled Minggu ke-1..ke-6; each registration bucketed by dayKey(mondayOf(createdAt)); registrations older than the window are excluded; unexpected statuses still count toward total.
- Week-bucket sanity (bun -e, TZ=Asia/Jakarta, exact copy of component logic): Minggu ke-1 29/09–05/10 {PENDING:2} (incl. Sabtu 4 Okt row), ke-2 06/10 {VERIFIKASI:1} ← 2025-10-05T20:00Z (Sunday UTC) correctly rolls to SENIN 6 Okt WIB, ke-3 13/10 {0}, ke-4 20/10 {DITERIMA:2}, ke-5 27/10 {0}, ke-6 03/11 {DITOLAK:1} — Senin-start aligned, oldest→newest; + older-than-window row (1 Agu) excluded (sum stays 6); [] → []; single old registration slides the whole window to its own week. PASS.
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit 2>&1 | grep overview → empty (exit 1, zero matches); full tsc = only the 4 pre-existing examples/+skills errors; git scope: src diff = overview.tsx (mine) + hafalan route/admin (14-a, parallel agent); dev server NOT run; no new deps (recharts/lucide already installed).

Stage Summary:
- File changed (only): src/components/admin/overview.tsx (~+160 lines). New full-width "PPDB — Pendaftar 6 Minggu" card sits between the Tren Kehadiran chart and the recent-lists grid; every existing card and the skeleton layout preserved (one skeleton added for the new slot).
- Contract (in-file, exported nothing new): buildWeekBuckets(Registration[]) → WeekBucket[] of exactly 6 Senin-start weeks anchored on the latest data week, each {label "dd/MM", tooltip "Minggu ke-N · dd/MM–dd/MM", PENDING/VERIFIKASI/DITERIMA/DITOLAK/total}; fetch = Promise.all(stats, registrations) inside the existing load(); no API/schema changes.
- Decisions: bucket anchor = latest createdAt instead of Date.now (task's hydration rule) → the newest bucket is the most recent week WITH activity, so the chip says "minggu terakhir" rather than "minggu ini"; rising requires latest.total > 0 (avoids "Momentum naik" on a 0-vs-0 tie); stacked bars skip radius (task's "only if simple" guard); full-width placement groups it with the attendance trend card; icon = ClipboardList (already the file's PPDB icon, imported); status hexes taken from statusBadgeClass hue families for cross-card consistency.
- QA results: lint clean; tsc clean in scope; bun -e week-bucket fixtures 4/4 pass (Senin-start, UTC-Sunday→Senin-WIB rollover, oldest→newest ordering, window exclusion, empty + single-reg edges); role="img" a11y label verified in code; only overview.tsx touched.

---
Task ID: 14-b
Agent: wa-preview (frontend-styling-expert)
Task: WhatsApp-style live preview inside the announcement composer — admin sees the exact broadcast message on a mock WA phone before sending.

Work Log:
- Read worklog.md (head 120 + tail 150; re-read tail before append — 14-a/14-c landed mid-flight, now 585 lines, no overlap with my file), src/app/api/announcements/route.ts (broadcast gate = `b.priority === 'PENTING' && b.broadcast`; message = `*${b.title}*\n${b.content}\n— TPQ Darul Jinan`), src/components/admin/content-admin.tsx fully (composer state annTitle/annContent/annPriority/annBroadcast, submit sends broadcast: annPriority==='PENTING' && annBroadcast, checkbox only rendered for PENTING; dialog is single-column sm:max-w-lg).
- EDIT src/components/admin/content-admin.tsx ONLY: (1) imports +MoonStar/+CheckCheck/+useSyncExternalStore/+Separator; (2) module-level WA_SIGNATURE const + composeBroadcastMessage(title, content) pure helper replicating route.ts composition character-for-character, + hydration-safe client clock: getWaClock() with per-minute cached waClockStore consumed via useSyncExternalStore (getServerSnapshot → '' so SSR renders empty; 15s subscribe interval keeps "HH.MM" fresh) — avoids the new react-hooks/set-state-in-effect lint error that a plain mount-effect setState triggered; (3) NEW file-local WaBroadcastPreview({title, content, willBroadcast}): <section aria-label="Pratinjau pesan WhatsApp" aria-live="polite"> containing (a) status line — willBroadcast ? amber strip (Megaphone + "Pesan ini akan disiarkan ke semua wali via WhatsApp." + amber PENTING badge) : stone strip (MessageCircle + "Disimpan sebagai pengumuman portal (tanpa broadcast WhatsApp).") — (b) phone mock: w-full max-w-sm mx-auto rounded-2xl border-stone-200 overflow-hidden; WA contact header row (size-9 emerald-700 avatar circle + MoonStar size-4, "TPQ Darul Jinan" text-sm font-semibold, "notifikasi simulasi" text-[11px] text-stone-400); chat backdrop bg-stone-200 (= #e7e5e4) with subtle radial-dot pattern via Tailwind arbitrary properties [background-image:radial-gradient(rgba(87,83,78,0.14)_1px,transparent_1px)] [background-size:14px_14px]; received bubble = max-w-[85%] rounded-2xl rounded-tl-sm bg-white p-2.5 shadow-sm; body <p whitespace-pre-line text-sm leading-relaxed text-stone-800> rendering the composed message with asterisks parsed bold via split('*') (codebase idiom from whatsapp-log/parent-portal: segments[1] → <strong> title, segments[2] → content body, signature re-appended after signature-stripped tail) — empty fields swap in muted text-stone-400 placeholders "Judul pengumuman…"/"Isi pengumuman…"; time row bottom-right = clock text-[11px] text-stone-400 + CheckCheck size-3.5 text-emerald-500; all icons aria-hidden; (4) composer wiring: derived const annWillBroadcast = annPriority === 'PENTING' && annBroadcast declared once and reused by submit payload, success-toast ternary AND preview prop (single source of truth, zero drift vs API gate); preview inserted below the fields after a Separator + small label "Pratinjau WhatsApp"; announcement DialogContent gains max-h-[88vh] overflow-y-auto (posts dialog idiom) so the taller dialog scrolls on short/mobile screens.
- Fidelity proof: temp bun script extracted both template literals from route.ts and content-admin.tsx, evaluated with identical sample title/content (multiline) → IDENTICAL: true (incl. em-dash signature); script deleted (scripts-tmp removed).
- QA: bun run lint → clean (fixed 1 self-introduced react-hooks/set-state-in-effect error, final exit 0); bunx tsc --noEmit 2>&1 | grep content-admin → empty; remaining tsc errors only the 4 pre-existing examples/+skills ones; dev server NOT run; no other files touched; no new deps.

Stage Summary:
- File changed (only): src/components/admin/content-admin.tsx (+~105 lines net). The Buat Pengumuman dialog now shows a live WhatsApp preview under the form fields: amber "akan disiarkan" strip + PENTING badge only when (PENTING && broadcast), stone "tanpa broadcast" strip otherwise, phone mock (contact header TPQ Darul Jinan / notifikasi simulasi, stone-200 dotted chat backdrop, white rounded-2xl rounded-tl-sm received bubble with shadow-sm) whose bubble text is the EXACT broadcast composition `*{title}*\n{content}\n— TPQ Darul Jinan` with *…* rendered bold, newlines preserved, muted placeholders when empty, client-only "HH.MM" timestamp + emerald CheckCheck ticks.
- Preview logic: derived from the same annTitle/annContent/annPriority/annBroadcast state the form uses (trim applied to mirror submit); broadcast coupling via shared annWillBroadcast const = exact API gate; aria-live="polite" region announces typing changes (no debounce needed at demo scale); hydration-safe clock via useSyncExternalStore (SSR '', per-minute cached snapshot).
- Decisions: single-column dialog → preview below fields separated by Separator + "Pratinjau WhatsApp" label (matches dialog idiom); bold parsing done on the composed message string with split('*') so the preview literally consumes the same string shape the API sends (title pair → strong, tail → content + signature) rather than hand-built JSX; max-h-[88vh] overflow-y-auto added to the announcement dialog (parity with the posts dialog) because the preview makes it taller than most viewports; stone/emerald palette only, no transitions needed (live text).
- QA results: lint clean; tsc clean in scope (grep content-admin empty); template-literal equivalence API vs preview → IDENTICAL:true; only content-admin.tsx modified.

---
Task ID: 14 (cron review round)
Agent: main (Z.ai Code) + 3 subagents (14-a/14-b/14-c)
Task: Round-14 — QA sweep, target-reached WA celebration, WhatsApp broadcast preview, PPDB 6-week trend chart

Work Log:
- Reviewed worklog (round 13 complete). QA sweep: lint/tsc clean, dev.log 400s all historical (low line numbers, from rounds 11-12 QA), browser loads + console zero errors. STABLE → features.
- [14-a subagent] Target-reached celebration: POST /api/hafalan now computes BEFORE/AFTER targetProgress (student list excl./incl. new row) with resolveNorm gate; justReached = valid && !before.targetReached && after.targetReached; isolated try/catch so celebration can never break creation; response +{targetJustReached, targetName}; on justReached sends WA "🎉 MasyaAllah, tabarakallah! {fullName} telah MENYEMPURNAKAN target hafalan *{targetName}* ({reached}/{position} surah Juz 30)…" to parent. hafalan-admin.tsx: success toast upgraded + one-shot celebration Dialog (PartyPopper in emerald-100 circle, "MasyaAllah, {name}!", target line, amber WA note, 44px "Alhamdulillah" button, role=status aria-live, resets on all close paths). curl E2E: Hamzah (target Al-Fatihah, 0 rows) → POST → targetJustReached:true + WA row created; repeat → false; An-Nas → false/null; test rows+notifications cleaned to baseline (0 hafalan / 13 notifications).
- [14-b subagent] WA broadcast preview in content-admin announcement dialog: WaBroadcastPreview({title, content, willBroadcast}) — phone-bubble mock (emerald avatar + "TPQ Darul Jinan"/"notifikasi simulasi" header, stone backdrop w/ dot pattern, white bubble rounded-tl-sm shadow-sm), live body = composeBroadcastMessage() replicating route.ts VERBATIM (fidelity proven by extracting both template literals into a temp bun script → IDENTICAL:true), *bold* parsed via split('*'), whitespace-pre-line, client-only HH.MM + CheckCheck via useSyncExternalStore (hydration-safe), muted placeholders when empty; willBroadcast = PENTING && broadcast (shared const → submit payload, toast, preview — zero drift): broadcast strip amber (Megaphone + "Pesan ini akan disiarkan ke semua wali via WhatsApp." + PENTING badge) else stone "tanpa broadcast" line; dialog got max-h-[88vh] scroll; aria-live region + aria-hidden icons.
- [14-c subagent] overview.tsx: NEW full-width "PPDB — Pendaftar 6 Minggu" card after Tren Kehadiran (all existing cards intact + skeleton slot): load() extended with /api/registrations (same error/retry path); buildWeekBuckets() useMemo — Monday-anchored 6 ISO weeks ending at latest registration's week (hydration-safe, no Date.now in render; tz/DST-safe dayKey math; bun -e test incl. UTC-Sunday→WIB-Monday boundary case); recharts stacked BarChart (PENDING #d97706 / VERIFIKASI #0d9488 / DITERIMA #059669 / DITOLAK #dc2626, maxBarSize 36, h-40 sm:h-48, grid/tooltip matching existing LineChart, labelFormatter "Minggu ke-N · dd/MM–dd/MM"); inline 4-dot legend + "Total/Menunggu/Diterima" stat row + momentum chip (TrendingUp emerald "Momentum naik" requires latest.total>0, else TrendingDown stone); dashed empty state at zero; role="img" aria-label summary.
- E2E verified in browser (admin): overview shows PPDB card w/ stacked bar (07/09 bucket amber+teal+emerald), tooltip breakdown, "Total: 4 pendaftar · Menunggu: 1 · Diterima: 2", emerald "Momentum naik (4 minggu terakhir)" chip; Konten → Pengumuman → Buat: preview bubble renders live bold title + content + signature + HH.MM ticks; NORMAL → stone "tanpa broadcast" line; PENTING → amber broadcast toggle block appears; broadcast ON → amber "Pesan ini akan disiarkan…" strip + PENTING badge in preview; dialog cancelled without saving; zero console/page errors; lint + tsc clean after integration.

Stage Summary:
- Round 14 shipped: POST /api/hafalan target-just-reached detection + WA celebration + UI celebration Dialog; WA-style live broadcast preview in announcement composer (API-verbatim composition); PPDB 6-week stacked trend card on admin overview with momentum chip.
- Contracts: POST /api/hafalan response +{targetJustReached: boolean, targetName: string|null}; WaBroadcastPreview internal to content-admin; overview load() now also fetches /api/registrations.
- Demo data: unchanged (14-a curl tests fully reverted; no new rows).
- Known notes: celebration math runs per POST (2 extra small queries — fine at demo scale); preview clock is client-only via useSyncExternalStore (SSR renders empty time — intended).
- Next-step candidates (priority order): (1) guru "quick setoran" inline sheet from guru-overview nudge card (pre-fills selected student), (2) PPDB accept dialog shows generated credentials (email + temp password) after DITERIMA for admin copy, (3) parent portal announcement read-state ("baru" badges per announcement), (4) dark-mode audit, (5) materials public section search/filter chips.

---
Task ID: 15-b
Agent: ppdb-credentials (full-stack-developer)
Task: "Credentials hasil penerimaan" panel — PUT /api/registrations DITERIMA now returns generated wali credentials and the Terima dialog switches to a copy-friendly success state.

Work Log:
- Read worklog.md (head 120 + tail 200), src/app/api/registrations/route.ts fully, registrations-admin.tsx fully; verified lucide-react exports Copy/Check/KeyRound/Info (runtime check) and that users/students APIs support DELETE ?id= for QA cleanup.
- [API] EDIT src/app/api/registrations/route.ts PUT only (GET/POST/DELETE + try/catch bad('Gagal memperbarui pendaftaran') contract untouched): DITERIMA + !existingStudent branch response extended to { registration, student, credentials: { nis, email: parent.email, tempPassword: 'ortu123' } } (parent may have pre-existed via findUnique — actual parent.email returned either way; inline comment documents it). DITERIMA + existingStudent (duplicate childName) falls through to the shared return which is now ok({ registration: reg, credentials: null }) — explicit null documented via comment: credentials null = no new account created (duplicate student OR non-DITERIMA status); UI keys the amber panel off credentials === null.
- [UI] EDIT src/components/admin/registrations-admin.tsx only: +AcceptCredentials/AcceptResult interfaces (no any), +file-local CredentialRow (label + font-mono text-sm value + outline copy Button min-h-11 with Copy icon → Check icon "Tersalin" for 2s, aria-label "Salin {label}", emerald outline hover emerald-100), +acceptResult/copiedKey state, +copyCredential() using navigator.clipboard.writeText with textarea+document.execCommand('copy') fallback (both try/catch; silent-fail → toast "Gagal menyalin…"), +resetAcceptState()/finishAccept() (reset acceptTarget/acceptClassId/acceptResult/copiedKey then await load()).
  - submitAccept(): PUT typed apiSend<AcceptResult>; on success toast kept concise ("{childName} resmi diterima. Notifikasi WhatsApp terkirim ke wali." — credential sentence removed since panel shows it) and instead of closing, setAcceptResult({registration, credentials ?? null}) → SAME dialog switches to success state: emerald CheckCircle2 header "Pendaftaran Diterima" + "{childName} resmi menjadi santri TPQ Darul Jinan."; credentials card rounded-xl border-emerald-200 bg-emerald-50/60 p-4 with 3 rows (NIS Santri / Email Wali / Kata Sandi Awal 'ortu123', all mono); KeyRound note line text-xs stone-500 "Kredensial juga telah dikirim ke wali via WhatsApp."; credentials === null → amber panel (Info icon, border-amber-200 bg-amber-50/60): "Santri dengan nama serupa sudah terdaftar — tidak ada akun baru dibuat." + status pendaftaran badge via shared statusBadgeClass; single emerald "Selesai" button min-h-11 (w-full on mobile) closing + reset + load(). Dialog onOpenChange close path also resets state and void load() if a success result was showing (X/Esc/overlay never leave a stale table). Reject/delete flows untouched; table untouched (no bonus additions per scope).
  - Palette: emerald/amber/stone only; rounded-xl/2xl; 44px targets on Salin + Selesai; a11y: aria-labels on copy buttons, aria-hidden icons, role=group + aria-label "Kredensial akun santri".
- curl E2E: baseline registrations 4 / students 9 / users 12 / notifications 13 → POST /api/registrations (QA-Cred-Anak, phone 081999887766) → PPDB-2025-0005 → PUT {status:'DITERIMA'} (classId omitted) → response credentials {nis:"DJ-2025-010", email:"081999887766@wali.daruljinan.id", tempPassword:"ortu123"} (exact-match assertion PASS, NIS regex PASS) + student/parentId ids returned; GET /api/notifications top row = "Selamat! Pendaftaran *QA-Cred-Anak* DITERIMA. NIS: *DJ-2025-010*. Akun Portal Wali: 081999887766@wali.daruljinan.id / ortu123. — TPQ Darul Jinan" (PASS). Duplicate-path (credentials:null) verified by code reading only per task scope.
- Cleanup: DELETE /api/registrations?id / /api/students?id / /api/users?id (users DELETE supported, ORANG_TUA not admin-guarded) → {"success":true} x3; scripts-tmp/cleanup-qa-notifs.ts (Prisma deleteMany by phone 081999887766) removed the 2 QA WA rows, then scripts-tmp dir deleted; re-verified baseline registrations 4 / students 9 / users 12 / notifications 13 with 0 QA rows anywhere; tmp files removed.

Stage Summary:
- API contract: PUT /api/registrations DITERIMA → { registration, student, credentials: { nis, email, tempPassword } } when a new santri+account are created; { registration, credentials: null } when no new account (duplicate childName or non-DITERIMA status). GET/POST/DELETE + error contract unchanged.
- UI contract: submitAccept no longer closes the dialog on success — the "Terima Pendaftaran" dialog switches to a success state driven by acceptResult (emerald credentials card w/ 3 copyable rows, or amber duplicate notice when credentials === null); "Selesai"/X/Esc/overlay all reset state and refresh the table via load(); copy fallback execCommand with toast on total failure.
- Files changed (exactly 2): src/app/api/registrations/route.ts (+3 lines/comments), src/components/admin/registrations-admin.tsx (+~120/-20). No other files; no new packages; dev server not started/stopped.
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit 2>&1 | grep -vE "examples/|skills/" → empty; curl E2E assertions PASS (credentials shape + WA log) with full baseline restoration (4/9/12/13, zero QA residue); dev.log clean, GET / → 200.

---
Task ID: 15-c
Agent: ann-readstate (frontend-styling-expert)
Task: Parent portal hero announcement read-state — localStorage-backed "Baru" badges, per-card mark-read buttons, "n pengumuman baru" line + "Tandai semua dibaca".

Work Log:
- Read worklog.md (head 120 + tail 200; re-read tail immediately before append — 641 lines now, PPDB-credentials entry landed in parallel, no file overlap), src/components/parent/parent-portal.tsx fully (422 lines pre-edit), types.ts (Announcement: id/title/content/priority/createdAt — cuid id safe as storage value), page.tsx deferred localStorage pattern (setTimeout in effect — the established workaround for the react-hooks/set-state-in-effect rule that a plain mount-effect setState triggers), probed lucide 0.525 (Sparkles/BellDot both exported) and confirmed dev server healthy on :3000.
- Edited src/components/parent/parent-portal.tsx ONLY (no API/backend changes — portal contract untouched): (1) module consts READ_ANNOUNCEMENTS_KEY='simadji.readAnnouncements' + READ_ANNOUNCEMENTS_CAP=100; pure helpers readStoredReadAnnouncements() (getItem + JSON.parse in try/catch → corrupt/missing/unavailable storage = [] ; Array.isArray + typeof string filter, no any) and persistReadAnnouncements(ids) (read-merge-dedupe via Set, slice(-100) drops OLDEST when exceeding cap, setItem in try/catch best-effort; both documented as never-call-during-render); (2) readAnnouncementIds state init [] → SSR + first client render identical (hydration-safe), hydrated in a dedicated useEffect via setTimeout(10ms)+clearTimeout cleanup (page.tsx pattern); (3) hero announcement cards converted div → <button type="button"> keeping ALL existing classes verbatim (flex gap-2.5 rounded-xl border p-3 + amber-50/white-10 variants) and appending w-full cursor-pointer text-left transition-colors focus-visible:ring-2 ring-white/80 + subtle hovers (hover:bg-white/15 / hover:border-amber-400 hover:bg-amber-100/80); single behavior: click = mark read (persist + state) — no toggle, no dimming; aria-label = "{title} (pengumuman baru, klik untuk menandai sudah dibaca)" ONLY while unread, else title only; (4) amber "Baru" mini-badge (inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-bold + Sparkles size-3 aria-hidden) with per-context variants: bg-amber-400 text-amber-950 on gradient cards, bg-emerald-600 text-white on PENTING amber-50 cards; badge disappears immediately on read (state update), card stays full opacity; (5) above the list when ≥1 unread among the 2 shown: "{n} pengumuman baru" text-[11px] font-medium text-amber-300 + BellDot size-3 aria-hidden, and a supplementary "Tandai semua dibaca" text button (text-[11px] px-1 py-1, underline-offset-2 hover:underline hover:text-white, focus-visible ring) that marks ALL shown announcements read; count line + button unmount entirely when count hits 0.
- Browser E2E (agent-browser, parent login via Masuk → "Wali — Budi Santoso" quick-fill → Masuk): fresh visit (localStorage pre-cleared) → "2 pengumuman baru" + both cards show "Baru" (DOM probe: badgeTexts ["Baru","Baru"], btnCount 3 in hero section); computed-style probe: normal-card badge bg lab(80.2 16.6 99.2)≈amber-400 / text amber-950, PENTING-card badge bg lab(55.0 -49.9 15.9)≈emerald-600 / text rgb(255,255,255) ✓; click "Pembayaran Iuran Bulan Ini" → its badge + aria-label suffix gone, count line → "1 pengumuman baru", localStorage simadji.readAnnouncements = ["cmtufise6003zq12wiqmc7b1i"]; "Tandai semua dibaca" → all badges gone, line/button unmounted, storage = both ids ["cmtufise6003zq12wiqmc7b1i","cmtufise5003xq12w9ikoqh03"]; reload → badges stay gone (persisted); eval localStorage.clear() + reload (session also cleared → re-login) → "2 pengumuman baru" + both "Baru" badges return (new-visitor state verified). Screenshot /tmp/ann-baru-badges.png. agent-browser errors → EMPTY; console → only React-DevTools info + HMR logs, zero errors/warnings (no hydration mismatch). No data modified (login POST + reads only).
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit 2>&1 | grep -vE "examples/|skills/" → empty (only the 4 pre-existing scaffold errors filtered). Deterministic: ids are stable cuids, no Date.now/Math.random in render, no localStorage access during render (mount timer + handlers only).

Stage Summary:
- File changed (only): src/components/parent/parent-portal.tsx (+~90/-12). Public portal untouched; no API/schema/data changes; no new packages.
- Behavior contract: read-state is client-only under localStorage key `simadji.readAnnouncements` (JSON array of announcement ids, dedup, cap 100 dropping oldest); hydration-safe (SSR/first render = empty, hydrated via deferred timer); unread hero announcement = "Baru" badge (amber-400/amber-950 on gradient, emerald-600/white on PENTING) + "(pengumuman baru, klik untuk menandai sudah dibaca)" aria-label suffix; clicking a card or "Tandai semua dibaca" removes badges instantly without dimming cards and persists across reloads; "{n} pengumuman baru" amber-300 line with BellDot appears only while ≥1 shown announcement is unread; corrupt storage values degrade to empty (try/catch).
- QA results: lint clean; tsc filtered clean; full browser E2E pass (fresh badges → per-card read → mark-all → reload persistence → storage-clear restore) with zero console/page errors.

---
Task ID: 15-a
Agent: quick-setoran (frontend-styling-expert)
Task: "Quick Setoran" Sheet dari kartu Progres Target di guru-overview — catat setoran tanpa navigasi, konsumsi kontrak targetJustReached (Task 14-a).

Work Log:
- Read worklog.md (head 120 + tail 200), src/components/admin/guru-overview.tsx FULLY, student-detail-drawer.tsx (Sheet idiom: side=right, sm:max-w-md, header stone-50/60, sr-only description), hafalan-admin.tsx (datalist surah + toast wording idiom), src/lib/hafalan-utils.ts (JUZ30_SURAHS/TargetProgress), src/lib/api-client.ts (apiSend), src/app/api/hafalan/route.ts (POST contract: surah+ayatRange required server-side), ui primitives (sheet/select/input/textarea/label/badge/button).
- EDIT src/components/admin/guru-overview.tsx ONLY: (1) header comment updated; imports +Loader2/+PencilLine/+Target, +apiSend, +JUZ30_SURAHS/+type TargetProgress, +Input/Label/Textarea/Sheet*/Select*; (2) NEW file-local QuickSetoranSheet (mounted once inside main return, before closing div): Sheet side=right flex w-full flex-col gap-0 p-0 sm:max-w-md (exact drawer idiom), SheetTitle "Catat Setoran Cepat" + sr-only SheetDescription, student header (size-12 emerald-700 initials avatar, fullName, emerald kelas chip, amber Target chip "Target: {targetName} · {reached}/{position}" — tp diturunkan dari data TERKINI parent, bukan snapshot); form: Nama Surah* Input list="quick-surah-list" (38 option dari JUZ30_SURAHS, key=s.no, idiom datalist hafalan-admin), Rentang Ayat* Input placeholder "1-7", Jenis Select TAHFIDZ/TAHSHIN/MURAJAAH default TAHFIDZ, Nilai (opsional) number 0-100 (clamp+round+NaN-safe), Catatan ustadz (opsional) Textarea rows=2, submit full-width emerald-700 min-h-11 "Simpan Setoran" dengan Loader2 pending; (3) reset-on-open effect ala drawer (guard !open||!student, deps [open, student?.id]) → tiap buka selalu form bersih, state tetap tampil selama animasi tutup; (4) validasi: surah+ayatRange required → submit disabled saat kosong, helper amber inline "wajib diisi" (aria-live polite) setelah field diisi lalu dikosongkan; (5) submit → apiSend POST /api/hafalan {studentId, surahName, ayatRange, type, grade: number|undefined, teacherNote: string|undefined}; sukses → tutup sheet + toast "Setoran tercatat" (nama santri + QS surah ayat) atau saat response.targetJustReached===true toast perayaan "🎉 Target tercapai!" / "MasyaAllah! {nama} menyempurnakan target {targetName}." (wording idiom hafalan-admin), lalu onSaved → load(true); gagal → toast "Gagal" + pesan error, sheet TETAP terbuka; (6) load() diberi param quiet=false (default) — quiet=true melewati skeleton/error penuh & hanya setData saat sukses (refresh senyap, idiom parent-portal) sehingga bar progres nudge card update instan tanpa flash halaman; (7) per-row trigger: tombol ghost icon PencilLine size-9 shrink-0 text-stone-400 hover:bg-emerald-100 hover:text-emerald-700 aria-label "Catat setoran {nama}" di ujung tiap li target row; (8) state quickStudentId + derive quickStudent (data.students.find)/quickTp (targetProgress dari hafalanByStudent) → data sheet selalu segar setelah reload (poin 5 task).
- Keputusan: ukuran tombol baris size-9 (36px) sesuai spesifikasi eksplisit task ("size-8 or size-9 icon button") & kepadatan baris — bukan min-h-11 umum; icon submit BookMarked (idiom tombol hafalan-admin), trigger baris PencilLine.

Stage Summary:
- Files changed (exactly 1): src/components/admin/guru-overview.tsx (+~291 lines: QuickSetoranSheet + wiring). Tidak ada file lain/API/schema/package baru.
- UI contract: QuickSetoranSheet file-local ({student, tp, open, onOpenChange, onSaved}); open diturunkan dari quickStudentId!==null; onSaved=() => void load(true); sheet mengonsumsi kontrak POST /api/hafalan Task 14-a ({targetJustReached: boolean, targetName: string|null}); datalist id="quick-surah-list"; semua kontrol ≥min-h-11 kecuali icon-button baris (size-9 per spec task); palette emerald/amber/stone; aria-label pada icon button; role=dialog otomatis dari Radix; hydration-safe (tanpa Date.now/Math.random di render).
- QA: bun run lint → clean (exit 0); bunx tsc --noEmit | grep -vE "examples/|skills/" → kosong (exit 1, zero matches); GET /api/hafalan → 200, baseline Hamzah (cmtufisd70016q12wwgsqzies) = 0 rows; POST bukti jalur sukses 1 row {Al-Fatihah, 1-7, TAHFIDZ} → {"id":"cmtumx2co000mq15lllgc5tty","targetJustReached":true,"targetName":"Al-Fatihah"} (kontrak perayaan terbukti); DELETE ?id= → {"success":true}; baseline pulih (Hamzah 0 rows, total 19); GET / → 200, dev.log bersih; git status src/: hanya guru-overview.tsx milik saya — registrations route/admin + parent-portal adalah pekerjaan agen paralel in-flight, tidak saya sentuh.

---
Task ID: 15 (cron review round)
Agent: main (Z.ai Code) + 3 subagents (15-a/15-b/15-c)
Task: Round-15 — QA sweep, guru quick-setoran sheet, PPDB accept credentials panel, parent announcement read-state badges

Work Log:
- Reviewed worklog (round 14 complete, 621 lines). QA sweep: lint clean, tsc clean (only pre-existing examples/skills errors), dev.log clean, GET / + /api/stats 200. Browser E2E baseline: admin overview (PPDB 6-week chart intact), guru portal (nudge card), parent portal (Rekap Kehadiran + Target strip) — ALL zero console errors. STABLE → features.
- [15-a subagent] Quick Setoran Sheet in guru-overview.tsx (+~291): every "Progres Target Santri" row gains ghost PencilLine icon-button (aria-label "Catat setoran {nama}") opening Sheet (side=right sm:max-w-md, student-detail-drawer idiom) with student header (initials, class chip, amber "Target: X · r/p" chip) + compact form (surah Input + 38-name datalist, ayat, Jenis Select, nilai 0-100 clamped, catatan) + min-h-11 emerald submit (disabled until surah+ayat). POST /api/hafalan via apiSend; success closes sheet + toast "Setoran tercatat", targetJustReached:true → celebration toast "🎉 Target tercapai!" ("MasyaAllah! {nama} menyempurnakan target {target}."); load(true) quiet-refresh updates the nudge progress bar in place. Form resets per open; inline amber helpers aria-live.
- [15-b subagent] PPDB accept credentials: PUT /api/registrations DITERIMA-new-student response now {registration, student, credentials:{nis, email: parent.email, tempPassword:'ortu123'}}; duplicate-childName path returns credentials:null (documented). registrations-admin.tsx submitAccept keeps dialog open and swaps to success state: emerald CheckCircle2 "Pendaftaran Diterima" + credentials card (border-emerald-200 bg-emerald-50/60) with mono NIS/Email/Sandi rows each with min-h-11 copy button (Copy→Check "Tersalin" 2s, clipboard API + execCommand fallback), KeyRound note "Kredensial juga telah dikirim ke wali via WhatsApp.", credentials:null → amber "Santri dengan nama serupa sudah terdaftar" panel; single emerald "Selesai" resets all state + load(). Reject/delete untouched.
- [15-c subagent] Parent announcement read-state (parent-portal.tsx only, +~90/−12): localStorage key simadji.readAnnouncements (cuid array, dedup, cap 100, hydration-safe read-after-mount in setTimeout(10ms) w/ cleanup + try/catch). Hero announcement divs → real <button type=button> (text-left, focus-visible ring, hover) = click to mark read; unread cards show "Baru" mini-badge (gradient cards bg-amber-400/amber-950, PENTING amber-50 cards bg-emerald-600/white, Sparkles size-3) + aria-label "(pengumuman baru, klik untuk menandai sudah dibaca)"; "{n} pengumuman baru" count line (BellDot, amber-300 on gradient) + "Tandai semua dibaca" text button when ≥1 unread; badges vanish instantly, cards never dim, persists across reloads.
- [ain integration E2E] Guru: logged in as Fatimah → nudge row button "Catat setoran Hamzah Abdurrahman" → sheet opens w/ Target chip + disabled submit until filled → Al-Fatihah 1-7 TAHFIDZ → submit → celebration toast "🎉 Target tercapai! … menyempurnakan target Al-Fatihah." + progressbar 0%→100% + "Target tercapai" chip in row; zero console errors. PPDB (admin): temp reg QA-Integ-Anak (PPDB-2025-0005) created via curl → Terima dialog → Terima Santri → success panel renders "Kredensial akun santri" group w/ Salin NIS/Email/Sandi + WA note → Selesai closes + table shows DITERIMA; zero console errors. Parent: fresh storage → "2 pengumuman baru" + both Baru badges → click marks read (count 2→1) → reload persists (read one stays read) → zero errors.
- [ain cleanup] Deleted browser-E2E setoran row (Hamzah back to 0) + its 2 WA rows; deleted temp PPDB reg/student/user via API DELETE + 2 QA WA rows via temp bun script (scripts-tmp removed). Baseline re-verified: registrations 4 / students 9 / users 12 / notifications 13 / Hamzah hafalan 0. Final lint + tsc clean after all 3 agents; dev.log clean.

Stage Summary:
- Round 15 shipped: guru "Catat Setoran Cepat" Sheet from target nudge rows (with live target-progress refresh + celebration toast), PPDB accept credentials result panel (copyable NIS/email/temp-password, duplicate-safe), parent announcement read-state ("Baru" badges + count line + mark-read, localStorage-persisted, hydration-safe).
- Contracts: POST /api/hafalan response unchanged (targetJustReached consumed by guru sheet too); PUT /api/registrations DITERIMA now returns credentials:{nis,email,tempPassword}|null; localStorage key simadji.readAnnouncements = string[] of announcement ids (cap 100).
- Files changed (3 + 1 API): src/components/admin/guru-overview.tsx, src/app/api/registrations/route.ts (PUT only), src/components/admin/registrations-admin.tsx, src/components/parent/parent-portal.tsx. No schema changes, no new deps.
- QA: lint clean; tsc clean (only pre-existing examples/skills errors); full browser E2E of all 3 features with zero console errors; DB fully restored to pre-round baseline after QA (verified counts).
- Next-step candidates (priority order): (1) guru attendance quick-mark from sessions card (mirroring quick setoran), (2) parent portal export child report (print-friendly recap), (3) admin Santri drawer "add setoran" shortcut, (4) dark-mode audit, (5) public materials search/filter chips.