831 /home/z/my-project/worklog.md
Task: Kartu "Rekap Absensi Bulanan" — agregasi per santri + ekspor CSV di view Absensi ADMIN/GURU (attendance-admin.tsx saja, tanpa perubahan API/schema).

Work Log:
- Baca worklog.md (idiom round 11-15), attendance-admin.tsx penuh (494 baris), overview.tsx (csvCell/csvDate/csvFileStamp/downloadCsv + STATUS_DOTS + statusBadgeClass), hafalan-admin.tsx (idiom CSV: filename + rows + toast), student-detail-drawer.tsx (kompromi bucketing createdAt + initials + gradeCircleClass), ui/progress.tsx & ui/table.tsx & ui/card.tsx (CardAction), api attendance/students routes (kontrak terverifikasi).
- EDIT src/components/admin/attendance-admin.tsx SAJA (+323/−3, kini 814 baris): (1) import +useMemo, +BarChart3/ClipboardList/Inbox/Download, +Progress, +CardAction, overview import ditambah downloadCsv; (2) helper modul: STATUS_COUNT_KEY (map status→kolom), RECAP_CHIPS (dot bg-emerald-500/amber-500/orange-500/red-500 = STATUS_DOTS overview), recapPctBadgeClass (>=85 emerald, >=70 amber, else red — ambang grade-badge), formatPct (id-ID desimal koma 1dp → "83,3"), initials(), interface RecapRow; (3) state rekap terpisah dari form Buka Sesi: recapClassId/recapMonth ('none')/recapData{records,roster}/recapLoading/recapError/recapRetry; (4) monthOptions useMemo([]) — 6 bulan terakhir dihitung SEKALI per mount, label toLocaleDateString('id-ID',{month:'long',year:'numeric'}) ("September 2026"), value "2026-09", tanpa pembacaan wall-clock saat render hasil (hydration-safe, kompromi sama dgn useState(todayISO()) yang sudah ada); (5) SATU effect [recapClassId,recapMonth,recapRetry] guard keduanya !=='none' → Promise.all(apiGet /api/attendance?classId=, apiGet /api/students?classId=) + flag cancelled, Skeleton 5×h-12 saat loading, Alert destructive+Coba Lagi (recapRetry++) saat gagal — idiom persis file ini; (6) agregasi useMemo: filter records createdAt.slice(0,7)===bulan (session dilepas API — kompromi student-detail-drawer), totals per status, pertemuan=Set(sessionId).size, rows = SEMUA santri kelas (AKTIF dulu, lalu nama A-Z; tanpa catatan = nol), pct per santri = hadir/pertemuan (1dp), rate = hadir/totalRecords; (7) UI: Card full-width rounded-2xl border-stone-200 shadow-sm SETELAH kartu Riwayat; CardHeader = tile BarChart3 size-9 rounded-xl bg-emerald-100 text-emerald-700 + judul/deskripsi + CardAction tombol outline "Unduh CSV" (min-h-11, Download, aria-label "Unduh rekap CSV", disabled saat tabel kosong); CardContent: controls flex-col sm:flex-row gap-3 (Select kelas placeholder "Pilih kelas" + Select bulan placeholder "Pilih bulan", dua-duanya min-h-11 w-full sm:w-60, aria-label "Kelas/Bulan rekap absensi"); empty state tanpa pilihan → dashed box ClipboardList text-stone-300 "Pilih kelas dan bulan untuk melihat rekap"; zero catatan bulan → dashed box Inbox "Belum ada catatan kehadiran pada bulan ini"; strip aria-live="polite" 4 chip (dot warna + count text-lg font-bold + label text-xs text-stone-500, border-stone-100 bg-stone-50/60 px-3 py-2) + chip ke-5 "Tingkat Kehadiran" dgn Progress emerald (className="[&>div]:bg-emerald-600", track bg-stone-200); tabel Table min-w-[640px]: Santri (avatar inisial size-8 emerald-100 + nama + NIS mono), Hadir/Izin/Sakit/Alpa (center), % Hadir (Badge recapPctBadgeClass, format "83,3%"), row hover:bg-stone-50/60; thead+th sticky top-0 z-10 bg-white; area scroll = kontainer internal Table via arbitrary-variant wrapper "[&_[data-slot=table-container]]:max-h-80 overflow-y-auto pr-1 [scrollbar-width:thin] [&::-webkit-scrollbar]:w-1.5 thumb:rounded-full bg-stone-300" (WAJIB: div bawaan shadcn Table ber-data-slot itu scrollportnya — sticky di wrapper luar tidak bekerja karena table-container punya overflow-x-auto→jadi scroll container tersendiri); (8) exportRecapCsv: slug kelas (lowercase, non-alnum→'-'), filename rekap-absensi-{slug}-{YYYY-MM}.csv (YYYY-MM = bulan terpilih, idiom csvFileStamp: nama bertanggal via helper), kolom NIS;Nama;Kelas;Hadir;Izin;Sakit;Alpa;Total Pertemuan;Persentase Hadir (%), persentase formatPct → "83,3"; downloadCsv(filename, rows) + toast "Ekspor CSV berhasil".
- Catatan implementasi: escape per sel DILAKUKAN downloadCsv (rows.map(csvCell) internal, ';'+BOM+quote-doubling) — persis idiom hafalan-admin round 11; TIDAK membungkus sel dgn csvCell manual utk menghindari double-escape (nilai berisi ';'/'"' akan rusak jika lolos csvCell dua kali). csvDate tidak dipakai (rekap bulanan tanpa kolom tanggal).

Stage Summary:
- File changed (exactly 1): src/components/admin/attendance-admin.tsx (+323/−3 → 814 baris). Tidak ada perubahan API/schema/dep; guru-overview.tsx & materials-section.tsx & db/custom.db adalah pekerjaan paralel agen lain, tidak disentuh.
- UI contract: kartu Rekap Absensi Bulanan muncul di bawah Riwayat Kehadiran Terbaru (view Absensi ADMIN & GURU); fetch hanya saat kelas+bulan dipilih (1 effect, Promise.all); konsumsi GET /api/attendance?classId= → AttendanceRecord[] (take 200, orderBy createdAt desc, tanpa session date) + GET /api/students?classId= → Student[] (semua santri kelas tampil, nol bila tanpa catatan); angka rekap = fungsi murni useMemo (records,roster,bulan); CSV: rekap-absensi-{kelas-slug}-{YYYY-MM}.csv via downloadCsv (csvCell per sel, ';', BOM); tanpa Date.now/Math.random di render; bulan = useMemo sekali per mount.
- QA: bun run lint → clean; bunx tsc --noEmit | grep -vE "examples/|skills/" → kosong; curl (GET only, tanpa tulis DB): /api/classes → 4 kelas; /api/attendance?classId= & /api/students?classId= utk 4 kelas → array (Tahfidz A: 4 record/2 sesi/2 santri bulan 2026-09; Iqra 4: 2/1/2; Al-Qur'an A: 2/1/2; Iqra 1: 0-1 — ada agen paralel yang menulis row); dev.log 30 baris terakhir = 200 + prisma:query, tanpa error. Browser E2E (agent-browser, login admin quick-fill): pilih Tahfidz A + September 2026 → strip Hadir 3/Izin 0/Sakit 0/Alpa 1/Tingkat Kehadiran 75% (progressbar emerald, transform -25% — cocok hitung manual 3/4); tabel 2 santri (Fathan 1-0-0-1 → 50% badge merah; Alfatih 2-0-0-0 → 100% badge emerald), urut nama, AKTIF semua; computed style: table-container max-height 320px + overflow-y auto + scrollbar-width thin, thead/th sticky top-0 z-10 bg-white, sticky terverifikasi mekanis (inject 8 baris + scrollTop 200 → thead top == container top); Agustus 2026 → Inbox "Belum ada catatan kehadiran pada bulan ini" + tombol ekspor disabled; klik Unduh CSV → toast "Ekspor CSV berhasil · Rekap 2 santri tersimpan di rekap-absensi-tahfidz-a-2026-09.csv"; opsi bulan September→April 2026 (id-ID); zero console/page errors; manipulasi DOM uji sticky dipulihkan, tanpa perubahan data.

---
Task ID: 16 (cron review round, integration)
Agent: main (Z.ai Code) + 3 subagents (16-a/16-b/16-c)
Task: Round-16 — QA sweep, guru quick-absen sheet, monthly attendance recap + CSV, public materials search/sort

Work Log:
- Reviewed worklog (round 15 complete, 692 lines). QA sweep: lint clean, tsc clean (only pre-existing examples/skills errors), dev.log clean, GET / + /api/stats 200, baseline APIs verified (9 students / 19 hafalan / 4 registrations / 13 notifications / 2 active sessions). Browser E2E baseline: public landing, admin (Ringkasan/PPDB/Hafalan/Keuangan), guru (Fatimah), parent (Budi, read-state badges + Rapor Santri print dialog intact) — ALL zero console errors. NOTE: parent "Rapor Santri" print feature and WA-log filter/CSV + admin student drawer already existed from earlier rounds (candidates 2/3/5 of round-15 list were done) → chose remaining gaps.
- [16-a subagent] QuickAbsenSheet in guru-overview.tsx (+281): per active-session row second icon button UserCheck "Absen cepat {className}" opens Sheet (right, sm:max-w-md) — prefill from GET /api/attendance?sessionId= (skeleton rows; amber aria-live hint on prefill failure), student rows with 4-segment status selector (exact attendance-admin on/off color families: HADIR emerald-700 / IZIN amber-500 / SAKIT orange-500 / ALPA red-600, aria-pressed + aria-label, h-9), note Input (h-9) revealed only when status≠HADIR, sticky footer with aria-live summary ("n Hadir · m belum diisi") + min-h-11 emerald submit; POST /api/attendance {sessionId, records[]} → toast "Absensi tersimpan · n santri · kelas" → load(true) quiet refresh (hadir/total badge updates in place).
- [16-b subagent] "Rekap Absensi Bulanan" card in attendance-admin.tsx (+323/−3): kelas + bulan (last 6 months, useMemo([]) — hydration-safe) Selects → Promise.all(GET attendance?classId + students?classId) with file's skeleton/alert idioms → aria-live summary strip (4 status-dot chips + Tingkat Kehadiran emerald Progress), per-santri sticky-thead table (all class students, zeros included; % Hadir badge ≥85 emerald / ≥70 amber / else red), CSV export rekap-absensi-{kelas}-{YYYY-MM}.csv (NIS;Nama;Kelas;Hadir;Izin;Sakit;Alpa;Total Pertemuan;Persentase Hadir % — id-ID comma decimals via shared downloadCsv). Month bucket = createdAt.slice(0,7) (API strips session date — documented).
- [16-c subagent] materials-section.tsx (+124/−21): text search Input (pl-9 Search icon, clear X refocuses, aria-label "Cari materi"), id-ID case-insensitive match over title+description+category, sort Select (Terbaru/Terlama/Judul A-Z/Z-A via localeCompare 'id'), aria-live "{n} materi ditemukan" count, search-specific empty state ("Tidak ada materi yang cocok…" + "Hapus pencarian" reset button), responsive wrap.
- [ain integration E2E] Guru quick-absen as Ustadz Yusuf (Tahfidz A owner): sheet prefill correct (Alfatih HADIR, Fathan ALPA) → Fathan→Hadir → submit → toast "Absensi tersimpan 2 santri · Tahfidz A" + card badge 1/2→2/2 via quiet refresh → restored Fathan→Alpa → API verified exact baseline (HADIR/ALPA, notes null). Recap card as guru: Tahfidz A + September 2026 → chips 3/0/0/1 + Tingkat Kehadiran 75% + per-santri rows (Fathan 50% red, Alfatih 100% emerald) — matches manual math. Materials as public: "tajwid" → 3 cards + "3 materi ditemukan" + X visible. Zero console errors throughout.
- [ain cleanup] Deleted 6 QA-created WA notification rows (4 from integration E2E + 2 left by 16-a QA) via temp bun script (scripts-tmp removed); notifications back to 13. Attendance/status baselines verified unchanged. Final lint + tsc clean across all 3 agents; dev.log clean; git status shows exactly the 3 intended files.

Stage Summary:
- Round 16 shipped: guru "Absen Cepat" Sheet from active-session card (prefill + upsert + live badge refresh), admin/guru "Rekap Absensi Bulanan" (per-santri monthly aggregation + % badges + CSV export), public materials search/sort/count polish.
- Contracts unchanged: GET/POST /api/attendance, GET /api/students?classId, GET /api/materials — frontend-only round, no API/schema/deps changes.
- Files changed (3): src/components/admin/guru-overview.tsx, src/components/admin/attendance-admin.tsx, src/components/public/materials-section.tsx.
- QA: lint clean; tsc clean (only pre-existing examples/skills errors); full browser E2E of all 3 features zero console errors; DB fully restored to baseline (13 notifications, attendance rows identical).
- Next-step candidates (priority order): (1) dark-mode audit across all portals, (2) guru classes view polish (per-class detail summary), (3) parent-side monthly recap link into attendance CSV/print, (4) admin hafalan target bulk-edit (set target for whole class), (5) public PPDB brochure download section.
---
Task ID: 17-a
Agent: sub-17a-bulktarget (general-purpose)
Task: BULK hafalan-target assignment per class — new POST /api/students/bulk-target endpoint + "Target Kelas" dialog in admin/guru Hafalan view.

Work Log:
- Read worklog.md tail, hafalan-admin.tsx fully (567 lines), api/students/route.ts, api/classes/route.ts, api/hafalan/route.ts (JSDoc header idiom), lib/api.ts, lib/api-client.ts (apiSend signature), lib/hafalan-utils.ts (JUZ30_SURAHS), students-admin.tsx target-Select idiom (JUZ30_TARGET_OPTIONS + 'none' sentinel, aria-label "Target hafalan surah Juz 30"), ui/dialog.tsx, prisma schema.
- VERIFIED: Student.status is String 'AKTIF' | 'NONAKTIF' | 'LULUS' (NOT 'KELUAR' as the task brief guessed) → updateMany filters status:'AKTIF'.
- NEW src/app/api/students/bulk-target/route.ts (39 lines): POST {classId, hafalanTarget:string|null}; validates classId exists (bad 'Kelas wajib dipilih' / 'Kelas tidak ditemukan'), target null OR trimmed non-empty ≤60 chars ('Target hafalan tidak valid' / 'tidak boleh kosong' / 'maksimal 60 karakter'); effect db.student.updateMany({where:{classId,status:'AKTIF'},data:{hafalanTarget}}); returns ok({success:true,count,hafalanTarget}); JSDoc contract header per /api/hafalan idiom.
- EDIT src/components/admin/hafalan-admin.tsx (567→738 lines): toolbar "Target Kelas" outline size-sm min-h-11 button (Users icon, aria-label "Atur target hafalan per kelas") left of Ekspor CSV in Riwayat header; Dialog rounded-2xl sm:max-w-md with kelas Select (fetched via GET /api/classes on open, classesLoading disables trigger) + target Select (first option value "none" "Tanpa target (kosongkan)" → null, then JUZ30_SURAHS) both h-11 with aria-labels; inline preview rounded-xl border-stone-100 bg-stone-50/60 p-3 text-sm aria-live — Loader2 "Memuat santri kelas…", then "{n} santri AKTIF akan diatur ke: {surah}" / "…target akan dikosongkan." / prompt-to-pick variant, plus amber warning "{k} santri memiliki target berbeda — akan ditimpa."; submit min-h-11 w-full rounded-2xl bg-emerald-700 hover:bg-emerald-800 disabled until class+preview+target ready, apiSend POST with hafalanTarget null for 'none'; success toast "Target kelas diperbarui" desc "{count} santri · {className}", close+reset+await load() (refreshes target chips); failure toast "Gagal" + message, dialog stays open. Form fully reset on open/close (handleTargetOpenChange); preview fetch races guarded via previewSeq useRef; all fetches in event handlers (no new useEffect → no set-state-in-effect lint risk); hydration-safe (no Date.now/Math.random); no new deps.
- QA: bun run lint → clean (exit 0). bunx tsc --noEmit → only 4 pre-existing examples/+skills/ errors (filtered per QA rule → empty). Dev :3000 healthy, GET / → 200, dev.log tail 30 lines: 0 errors.
- E2E curl on class cmtufiscu000kq12wa5d9jpcw ("Al-Qur'an A", 2 santri both AKTIF): 4 validation paths returned exact Indonesian errors; POST An-Naba → {"success":true,"count":2,"hafalanTarget":"An-Naba"}; POST null → count 2 null (kosongkan path); GET /api/students?classId= confirmed both hafalanTarget='An-Naba'.
- RESTORATION: baseline captured BEFORE test (scripts-tmp/baseline.json, dir deleted after): Alika Syakira 'Abasa', Khadija Ayu Safira null. Restored via PUT /api/students {id,hafalanTarget} (PUT supports partial update; null passes b.hafalanTarget!==undefined → null||null=null) and re-verified via GET: both match baseline, RESTORATION PASS. Zero QA residue.

Stage Summary:
- Contract POST /api/students/bulk-target: body {classId, hafalanTarget: string|null} → 200 {success:true, count:number, hafalanTarget} | 400/404 {error:string-id}. Only status:'AKTIF' students of the class are updated; NONAKTIF/LULUS untouched.
- Hafalan view now has bulk class-target assignment: toolbar button → dialog (kelas + Juz-30 target + live AKTIF preview + overwrite warning) → updateMany → toast → data reload. Reuses JUZ30_SURAHS/lib + students-admin Select idiom.
- Files changed (exactly 2): src/app/api/students/bulk-target/route.ts (new, +39), src/components/admin/hafalan-admin.tsx (+171/-... 567→738). No other files, no new deps, server untouched.

---
Task ID: 17-b
Agent: det-kelas (general-purpose)
Task: Add "Detail Kelas" Sheet to classes-admin — roster + stats strip + recent sessions per class.

Work Log:
- Read worklog.md (head + tail; re-checked tail immediately before append — 780 lines, 17-a bulk-target entry landed in parallel, touches hafalan-admin/students API: no overlap with my file), classes-admin.tsx fully (352 lines pre-edit), student-detail-drawer.tsx (Sheet idiom: initials avatars, Skeleton rows, Alert+retry, scrollbar const, side="right" sm:max-w-md), types.ts (SessionItem: date/topic/isActive/hadir/total; AttendanceRecord: createdAt only, session date stripped; Student: status/hafalanTarget/parent), overview.tsx statusBadgeClass (HADIR→emerald … AKTIF falls to stone default — reused as-is per spec), api-client (formatShortDate id-ID), eslint.config (react-hooks/purity off, exhaustive-deps off).
- Verified via curl (GET only, dev server :3000): /api/classes 200 (4 classes); per-class row counts — Al-Qur'an A: 2 students / 2 attendance / 1 session / 7 roster-matched hafalan; Iqra 1: 3/6/4/5; Iqra 4: 2/2/1/1; Tahfidz A: 2/4/2/6; /api/hafalan global 19 (no classId filter → matched client-side via roster id Set).
- Edited src/components/admin/classes-admin.tsx ONLY (+286/−3, 352→638 lines): (1) imports +useMemo, +Eye/QrCode/Target, +AttendanceRecord/Hafalan/SessionItem/Student types, +formatShortDate, +Sheet family, +statusBadgeClass from './overview'; (2) file-local initials() helper (copy of drawer's); (3) card footer: third button "Detail" (outline sm, Eye size-3.5, subtle text-emerald-700 hover:bg-emerald-50 hover:border-emerald-300, aria-label "Detail kelas {name}") placed BEFORE Edit; (4) detailClassId state + detailKlass derived via classes.find() — reload that drops the class closes the sheet naturally (open={!!detailKlass}); (5) file-local ClassDetailSheet({klass, open, onOpenChange}): ONE Promise.all [students?classId, attendance?classId, sessions, hafalan] inside inner async loadDetail() (attendance-admin cancelled-flag idiom, deps [open, classId, reloadKey]); 4 Skeleton h-12 rounded-xl rows while loading; destructive Alert + "Coba Lagi" (reloadKey++) on failure; (6) header: level-colored size-9 rounded-xl BookOpen tile reusing levelBadgeClass hue, title=class name, "{schedule} · {room}" line + teacher line + level label Badge; (7) stats strip aria-live="polite" grid-cols-3 gap-2 chips (rounded-xl border-stone-100 bg-stone-50/60 px-3 py-2): Santri AKTIF count, Kehadiran 30 Hari % (HADIR/total with createdAt ≥ now−30d, '—' when zero records), Setoran Hafalan count (hafalans ∩ roster ids); (8) roster "Santri ({n})": max-h-72 overflow-y-auto divide-y, pr-1 thin stone-300 scrollbar, initials avatar size-8 rounded-full (emerald-100/emerald-700 when AKTIF, stone otherwise), name truncate + NIS font-mono text-[11px], right: hafalanTarget amber chip (Target size-3, max-w-[120px] truncate, title attr) + statusBadgeClass Badge text-[10px]; dashed Users empty box "Belum ada santri di kelas ini"; (9) "Sesi Terbaru": up to 5, client-side classId filter + date desc (localeCompare, sort on filtered copy — state array untouched), rows = topic truncate + formatShortDate(date) + AKTIF badge (isActive, emerald) + hadir chip "{n}/{roster}" from HADIR attendance rows per sessionId (title attr for a11y); dashed QrCode empty box "Belum ada sesi"; (10) hydration-safety: 30-day window via new Date() INSIDE the data-only useMemo (new Date() in render path never runs until fetched data exists); no Date.now/Math.random elsewhere; Indonesian UI, no new deps, no other files touched.

Stage Summary:
- Classes view now has a "Detail Kelas" Sheet per card: roster (status-colored avatars, NIS, hafalan target chips, status badges), aria-live stats strip (Santri AKTIF / Kehadiran 30 Hari / Setoran Hafalan), and 5 most recent sessions with AKTIF badge + per-session hadir/{roster} chip.
- Contracts used (all GET, unchanged): /api/students?classId= → Student[] (incl. parent/class); /api/attendance?classId= → AttendanceRecord[] (take 200, createdAt desc, session date stripped → 30-day window + hadir-per-session computed from createdAt/sessionId client-side); /api/sessions → SessionItem[] (classId filter + date desc client-side per spec); /api/hafalan → Hafalan[] (latest 100 global, matched to roster via studentId Set).
- QA: bun run lint clean; bunx tsc --noEmit (filtered examples/|skills/) empty; GET / 200 + "✓ Compiled"; dev.log tail 30: no new errors (only 17-a bulk-target traffic + my GET probes); API row counts noted above.
- Files changed (exactly 1): src/components/admin/classes-admin.tsx (+286/−3 → 638 lines).
---
Task ID: 17-c
Agent: struk-pembayaran (general-purpose subagent)
Task: Printable payment receipt ("Struk Pembayaran") for the parent portal — post-payment success step + re-printable from riwayat pembayaran history (child-panel.tsx only, no API/schema/dep changes).

Work Log:
- Read worklog.md (tail 120 — idioms: hydration-safe no-Date-in-render, min-h-11 touch targets, aria-label icon buttons, RAPOR scoped-print pattern), child-panel.tsx fully (963 lines pre-edit: PaymentDialog L128-283, PAY_METHODS L122, RAPOR_PRINT_CSS L294-324, riwayat table L893-944), ui/button.tsx (icon=size-9, ghost variant), types.ts (Payment.method/paidAt string|null), dev.log baseline; read-only Prisma probe: 5 baseline SUCCESS payments (INV-1001/1002/1003/1005/1008, all with method QRIS/GOPAY/VA_BCA; seed rows with method:null exist only on PENDING — fallback kept as documented safety).
- Edited src/components/parent/child-panel.tsx ONLY (+228/−7 → 1183 lines): (1) module-level formatPaidTimestamp(date) → "9 September 2026 · 19.11 WIB" (id-ID long date + 2-digit time + ' WIB'); (2) STRUK_PRINT_CSS — verbatim mirror of RAPOR_PRINT_CSS mechanics with anchor #struk-pembayaran-print (body>*:not(:has()) removal, visibility trick, dialog-overlay/close/.no-print removal, dialog-content chrome strip, print-color-adjust exact); (3) ReceiptContent({payment,studentName,studentNis}) — receipt-paper card (rounded-2xl border-stone-200 bg-white p-5): emerald Landmark tile size-9 rounded-xl + "TPQ Darul Jinan" + "Sistem Informasi Manajemen — Struk Pembayaran", dashed divider, "STRUK PEMBAYARAN" tracking-[0.2em] + LUNAS badge (border-emerald-200 bg-emerald-100 text-emerald-800), dl-like rows (label text-xs text-stone-500 / value text-sm font-semibold): No. Invoice (font-mono), Santri + NIS (font-mono text-[11px]), Keterangan, Metode (PAY_METHODS label ?? raw ?? 'QRIS'), Tanggal Bayar, prominent TOTAL row (dashed top border, text-lg font-bold text-emerald-700 formatRupiah), footer "Terima kasih — jazakumullahu khairan. Bukti pembayaran simulasi (demo)."; paid date captured ONCE per mount via useState lazy initializer `payment.paidAt ? new Date(payment.paidAt) : new Date()` (stale-null fallback post-payment; keyed by payment.id so re-opens recapture); (4) ReceiptDialog — Dialog sm:max-w-md, <style dangerouslySetInnerHTML STRUK_PRINT_CSS> inside content only, no-print DialogHeader (ReceiptText title + description, keeps Radix a11y), footer no-print grid grid-cols-2 gap-2: "Cetak" (outline min-h-11 Printer → window.print()) + "Tutup" (emerald solid min-h-11); (5) PaymentDialog: +studentNis prop, +receiptOpen state, success step now grid grid-cols-2 gap-2 with "Cetak Struk" (outline min-h-11 Printer) beside "Selesai" (min-h-11); ReceiptDialog mounted as last child of PaymentDialog's DialogContent (sibling portal dialog) with payment patched `{...payment, method: method ?? payment.method}` — the dialog object is stale after the simulated PUT (server writes method+paidAt), so the locally chosen method is patched in and paidAt falls back to "now"; (6) ChildPanel: +receiptTarget state; riwayat SUCCESS rows now render CheckCircle2 + ghost size-9 icon Button (aria-label "Struk {invoiceNo}", title, text-stone-400 hover:bg-emerald-50 hover:text-emerald-700 transition-colors) as last action → opens ReceiptDialog (child.fullName/child.nis); ReceiptDialog rendered next to PaymentDialog with open=!!receiptTarget, onOpenChange resets target.
- Method-fallback decision: `PAY_METHODS.find(...)?.label ?? (payment.method || 'QRIS')` — known methods show their label (QRIS/GoPay/Virtual Account BCA), unknown raw strings pass through, null → 'QRIS' (portal's primary channel), documented with comments at both fallback sites. No new deps; Landmark/Printer/ReceiptText/formatRupiah/ReactNode all already imported.

Stage Summary:
- Files changed (exactly 1): src/components/parent/child-panel.tsx (+228/−7 → 1183 lines). No API/schema/deps touched; db/custom.db + classes-admin/hafalan-admin modifications in git status are PARALLEL agents' work (17-a/b), not mine.
- Two entry points: (1) PaymentDialog success step "Cetak Struk" button (receipt shows the just-paid invoice with locally chosen method; paidAt fallback "now" since dialog object is stale); (2) ghost printer button on every SUCCESS row of the riwayat pembayaran table (aria-label "Struk INV-…").
- Print CSS: STRUK_PRINT_CSS mounted only inside ReceiptDialog; hides dialog chrome + page background so only #struk-pembayaran-print prints; footer buttons + header carry .no-print.
- QA: bun run lint → clean; bunx tsc --noEmit | grep -vE "examples/|skills/" → empty (only 4 pre-existing examples/skills errors in raw output); dev.log last 30 lines = 200s + prisma queries, no errors. Browser E2E (agent-browser, parent login Budi quick-fill): table SUCCESS rows show "Struk INV-…" printer buttons; click → receipt renders complete (letterhead, LUNAS, mono invoice, Santri+NIS, Metode label, id-ID "1 September 2026 · 18.25 WIB" date, "Rp 75.000" total, demo footer); Tutup unmounts anchor (per-mount date recapture verified), second receipt (INV-1002) opens correctly; Playwright print-PDF of open receipt → single page containing ONLY receipt content (pdftotext probes: Keterangan/QRIS/date/total/footer present; zero leaks of Rapor/Kehadiran/table rows/Cetak/Tutup), behavior byte-identical to the established RAPOR print baseline measured the same way; zero console/page errors. NO payment mutations: Payment rows verified identical to pre-QA snapshot (11 rows, statuses/methods/paidAt unchanged); login + reads only.

---
Task ID: 17 (cron review round, integration)
Agent: main (Z.ai Code) + 3 subagents (17-a/17-b/17-c)
Task: Round-17 — QA sweep, bulk hafalan target per class, class detail sheet, parent payment receipt (struk)

Work Log:
- Reviewed worklog (round 16 complete, 762 lines). QA sweep: lint clean, tsc clean (only pre-existing examples/skills errors), dev.log clean, 7 baseline APIs 200, DB baseline verified (9 students / 19 hafalan / 1 pending reg / 13 notifications / targets intact). Browser sweep of ALL admin views (Ringkasan/PPDB/Santri/Guru/Kelas/Absensi/Hafalan/Materi/Keuangan/Konten/Pengguna/WhatsApp/Pengaturan) + public PPDB & Berita — zero console errors. Payment reminders already existed; gaps confirmed: no bulk target, no class detail, no payment receipt.
- [17-a subagent] NEW API src/app/api/students/bulk-target/route.ts (+39): POST {classId, hafalanTarget: string|null} → validation (kelas wajib/ada; target ≤60 chars atau null) → db.student.updateMany AKTIF-only → {success, count, hafalanTarget}; JSDoc contract header. hafalan-admin.tsx (+171): toolbar "Target Kelas" button (left of Ekspor CSV) → Dialog: kelas Select (GET /api/classes on open) + target Select (students-admin idiom, first option "Tanpa target (kosongkan)"→null) + aria-live preview ("2 santri AKTIF akan diatur ke: An-Naba" + amber "N santri memiliki target berbeda — akan ditimpa." with fetch-race useRef seq guard) + emerald submit → toast "Target kelas diperbarui · {count} santri" → existing load() refresh. curl E2E all validation paths + set/verify/RESTORE baseline (Alika 'Abasa', Khadija null re-verified).
- [17-b subagent] classes-admin.tsx (+286/−3): "Detail" button (Eye, aria-label "Detail kelas {name}") per card → ClassDetailSheet (side right, drawer idiom): Promise.all(students/attendance/sessions/hafalan by class, cancelled-flag) + skeletons + destructive Alert retry; header level-tile + schedule·room·teacher; aria-live stats strip (Santri AKTIF / Kehadiran 30 Hari % with '—' zero-guard / Setoran Hafalan roster-matched); roster scroll list (initials avatar, name+NIS mono, amber Target chip, statusBadgeClass badge, dashed empty state); Sesi Terbaru ≤5 (date, topic, AKTIF badge, {hadir}/{roster} chips, dashed empty state).
- [17-c subagent] child-panel.tsx (+228/−7): STRUK_PRINT_CSS (verbatim RAPOR_PRINT_CSS mirror, anchor #struk-pembayaran-print) + ReceiptContent (thermal-receipt card: emerald Landmark letterhead, dashed dividers, STRUK PEMBAYARAN caps + LUNAS badge, dl rows — mono invoice, Santri+NIS mono, Keterangan, Metode (PAY_METHODS label ?? raw ?? 'QRIS' fallback documented), Tanggal Bayar (per-mount useState date, stale-paidAt→now fallback), dashed-top TOTAL text-lg emerald, demo footer) + ReceiptDialog (Cetak window.print + Tutup, .no-print chrome) — entry point 1: PaymentDialog success step "Cetak Struk" outline button beside Selesai (stale dialog payment patched with chosen method); entry point 2: riwayat SUCCESS rows ghost size-9 Printer button (aria-label "Struk {invoiceNo}"). Browser E2E incl. actual print-PDF = single page receipt-only, zero chrome leaks; NO DB mutations.
- [ain integration E2E] Admin → Hafalan: "Target Kelas" dialog opens, Al-Qur'an A → preview "2 santri AKTIF di kelas ini — pilih surah target" → An-Naba → "2 santri AKTIF akan diatur ke: An-Naba" + amber overwrite warning, submit enabled — closed WITHOUT submit (submit+restore already curl-proven by 17-a). Admin → Kelas: Tahfidz A Detail sheet — stats 2/75%/6 correct, roster w/ NIS, 2 recent sessions w/ hadir chips (1/2 AKTIF, 2/2). Parent (Budi) → riwayat: Struk INV-2025-1001 dialog — LUNAS, QRIS, "9 September 2026 · 19.11 WIB", TOTAL Rp 75.000, Cetak/Tutup. Zero console errors throughout.
- [ain baseline] Final: 9 students, targets intact (Alika 'Abasa' / Khadija null / Rizky 'An-Naba'), 19 hafalan, 13 notifications, dev.log clean; git shows exactly 3 modified files + 1 new API dir.

Stage Summary:
- Round 17 shipped: bulk hafalan-target per class (NEW POST /api/students/bulk-target + "Target Kelas" dialog with live preview + overwrite warning), class detail Sheet (roster + 30-day attendance % + hafalan count + recent sessions), parent payment struk (printable, two entry points: post-payment + riwayat reprint).
- New contract: POST /api/students/bulk-target {classId, hafalanTarget|null} → {success, count, hafalanTarget}. All other contracts unchanged; frontend + 1 small API, no schema/dep changes.
- Files changed: src/app/api/students/bulk-target/route.ts (NEW), src/components/admin/hafalan-admin.tsx, src/components/admin/classes-admin.tsx, src/components/parent/child-panel.tsx.
- QA: lint clean; tsc clean (pre-existing examples/skills only); browser E2E all 3 features zero console errors; DB baseline fully preserved (17-a restored its test data; integration round opened dialogs only).
- Next-step candidates (priority order): (1) dark-mode audit across portals, (2) parent attendance CSV/print shortcut in Rekap card, (3) guru Kelas view = classes-admin already shows all — consider own-class filter for GURU role, (4) public PPDB brochure/download section, (5) admin dashboard quick-actions row (create sesi/tagihan shortcuts).